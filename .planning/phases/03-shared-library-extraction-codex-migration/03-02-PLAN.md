---
phase: 03-shared-library-extraction-codex-migration
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - probe_wham_usage.py
  - json_usage_provider.py
  - codex_balance_widget_chrome.py
autonomous: true
requirements: [CODEX-01]
must_haves:
  truths:
    - "cd /d/00_Projects/codex_balance_widget && py -3 -m unittest test_probe_wham_usage test_json_usage_provider test_codex_balance_widget_chrome -v passes, exit 0, exactly 64 tests, 0 failures/errors — and none of the three test files were modified to get there (Success Criterion 3)"
    - "probe_wham_usage.ProbeError is usage_widget_common.errors.FetchError (same class object via alias) — no locally-defined ProbeError class remains in probe_wham_usage.py (Success Criterion 2)"
    - "json_usage_provider.JsonUsageProvider._fetch_with_retry delegates to usage_widget_common.retry.fetch_with_retry_once — no hand-rolled 'for attempt in range(2)' retry loop remains in json_usage_provider.py (Success Criterion 2)"
    - "codex_balance_widget_chrome.plan_fetch_outcome calls usage_widget_common.fetch_decision.decide_fetch_source for its chrome-fallback branch selection, with all 10 TestPlanFetchOutcome cases producing byte-identical output to before this plan"
    - "probe_wham_usage.REDACT_KEYS (the Codex-specific denylist) still lives in probe_wham_usage.py, not in the shared package — the domain-specific list does not move into a separately-versioned file"
  artifacts:
    - path: "probe_wham_usage.py"
      provides: "ProbeError alias to usage_widget_common.errors.FetchError; redact()/redaction_clean() delegating to usage_widget_common.redaction"
      contains: "usage_widget_common"
    - path: "json_usage_provider.py"
      provides: "_fetch_with_retry delegating to usage_widget_common.retry.fetch_with_retry_once"
      contains: "fetch_with_retry_once"
    - path: "codex_balance_widget_chrome.py"
      provides: "plan_fetch_outcome delegating its 3-way branch selection to usage_widget_common.fetch_decision.decide_fetch_source"
      contains: "decide_fetch_source"
  key_links:
    - from: "probe_wham_usage.py"
      to: "usage_widget_common.errors.FetchError"
      via: "from usage_widget_common.errors import FetchError; ProbeError = FetchError"
      pattern: "ProbeError = FetchError"
    - from: "json_usage_provider.py"
      to: "usage_widget_common.retry.fetch_with_retry_once"
      via: "outcome = await fetch_with_retry_once(_do_fetch, retry_delay=self.retry_delay)"
      pattern: "fetch_with_retry_once\\("
    - from: "codex_balance_widget_chrome.py"
      to: "usage_widget_common.fetch_decision.decide_fetch_source"
      via: "decision = decide_fetch_source(primary_ok=False, fallback_ok=fallback_ok, has_existing_data=has_existing_data)"
      pattern: "decide_fetch_source\\("
---

<objective>
**Depends on Plan 03-01** — the `usage_widget_common` package must already exist at `d:/00_Projects/usage_widget_common` (a sibling repo, own git history) before this plan runs. Unlike Plan 03-01, every file this plan edits lives in THIS repo (`d:/00_Projects/codex_balance_widget`) — standard single-repo commit protocol applies. This plan only reaches across to the sibling repo at Python import time, via `sys.path` insertion — it does not write or commit anything inside `d:/00_Projects/usage_widget_common`.

Migrate `probe_wham_usage.py`, `json_usage_provider.py`, and `codex_balance_widget_chrome.py` onto the shared package's retry-once, error-classification, redaction, and fetch-decision primitives, replacing local copies, with zero observable behavior change.

Purpose: close CODEX-01 and prove the shared abstraction against Codex's own already-tested, production code before Phase 4's Claude widget depends on it — completing Roadmap Phase 3 Success Criteria 2 and 3.

Output: all three Codex files import from `usage_widget_common` instead of maintaining local copies; all 64 pre-existing tests pass, unmodified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-shared-library-extraction-codex-migration/03-CONTEXT.md
@.planning/phases/03-shared-library-extraction-codex-migration/03-01-SUMMARY.md

@probe_wham_usage.py
@json_usage_provider.py
@codex_balance_widget_chrome.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire probe_wham_usage.py + json_usage_provider.py onto the shared package</name>
  <files>probe_wham_usage.py, json_usage_provider.py</files>
  <read_first>
    - D:/00_Projects/codex_balance_widget/probe_wham_usage.py — lines 19-31 (imports; `sys` and `Path` already imported), lines 33-48 (`REDACT_KEYS` + constants — MUST stay exactly here, unmoved), lines 51-61 (`class ProbeError` — deleted, replaced by alias), lines 238-260 (`redact`/`redaction_clean` — deleted, replaced by a thin wrapper / direct re-export)
    - D:/00_Projects/codex_balance_widget/json_usage_provider.py — lines 20-27 (imports + `RETRY_DELAY_SECONDS` — no `sys`/`Path` imported yet, must be added), lines 57-89 (`_fetch_with_retry` — replaced by a call into the shared retry engine)
    - D:/00_Projects/usage_widget_common/usage_widget_common/errors.py, redaction.py, retry.py (Plan 03-01 output — read from the sibling repo path) — exact public signatures: `FetchError(message, *, retryable=False)`, `redact(obj, keys=frozenset())`, `redaction_clean(text, forbidden_substrings=DEFAULT_FORBIDDEN_SUBSTRINGS)`, `fetch_with_retry_once(fetch_once, *, retry_delay=1.0) -> RetryOutcome(status, value, error, retried)`
    - D:/00_Projects/codex_balance_widget/test_probe_wham_usage.py and test_json_usage_provider.py — read in full to confirm no call site requires a different signature after migration (expected: zero edits needed to either test file)
  </read_first>
  <action>
    Both files edited here live in THIS repo — normal single-repo commit protocol applies (unlike Plan 03-01). Both need to reach the sibling repo `usage_widget_common` at import time via `sys.path`, since neither project has packaging/venv infrastructure (per 03-CONTEXT.md discretion).

    In `probe_wham_usage.py`: immediately after the last import line (`from typing import Any`) and before the `USAGE_URL = ...` constant, insert a sys.path bootstrap: `_SIBLING_COMMON = Path(__file__).resolve().parent.parent / "usage_widget_common"` then `if str(_SIBLING_COMMON) not in sys.path: sys.path.insert(0, str(_SIBLING_COMMON))`, followed by `from usage_widget_common.errors import FetchError` and `from usage_widget_common.redaction import redact as _redact_generic, redaction_clean`. (`sys` and `Path` are already imported in this file — no new stdlib imports needed.) Delete the existing `class ProbeError(RuntimeError):` definition (its `__init__` body and docstring) and replace it with a single line `ProbeError = FetchError` in its place, with a one-line comment noting it is now an alias for `usage_widget_common.errors.FetchError` (Phase 3 extraction) — every existing `probe_wham_usage.ProbeError(...)` / `isinstance(x, probe_wham_usage.ProbeError)` / `except probe_wham_usage.ProbeError` call site (in this file and in both test files) keeps working unchanged because it is the identical class object. Delete the existing `def redact(obj: Any) -> Any:` function body (the recursive implementation) and replace it with a thin wrapper: `def redact(obj: Any) -> Any: return _redact_generic(obj, REDACT_KEYS)`, keeping its one-line docstring reference to `REDACT_KEYS`. Do NOT move, rename, or edit `REDACT_KEYS` (currently lines 39-48) — it stays exactly where it is; it is the Codex-specific denylist and must not migrate into the separately-versioned shared package (the redaction-denylist-drift concern named in this phase's threat model). Delete the local `def redaction_clean(text: str) -> bool:` function entirely — the name imported directly from `usage_widget_common.redaction` in the bootstrap block above replaces it, and its default `forbidden_substrings=("eyJ", "@")` matches the deleted local implementation exactly, so every existing `redaction_clean(text)` call site (single positional argument) keeps working unchanged.

    In `json_usage_provider.py`: add `import sys` immediately after `import asyncio`, and `from pathlib import Path` immediately after `from dataclasses import dataclass`. After `import probe_wham_usage` (and before `RETRY_DELAY_SECONDS = 1.0`), insert the same sys.path bootstrap pattern as above (`_SIBLING_COMMON = Path(__file__).resolve().parent.parent / "usage_widget_common"`; `if str(_SIBLING_COMMON) not in sys.path: sys.path.insert(0, str(_SIBLING_COMMON))`), followed by `from usage_widget_common.retry import fetch_with_retry_once`. Rewrite `JsonUsageProvider._fetch_with_retry` (currently the manual `for attempt in range(2):` loop) to delegate entirely to the shared engine: define a local `async def _do_fetch() -> dict: payload = await self._fetch_payload(access_token, account_id); return probe_wham_usage.extract_fields(payload)` closure inside `_fetch_with_retry`, then `outcome = await fetch_with_retry_once(_do_fetch, retry_delay=self.retry_delay)`, then `return JsonFetchResult(outcome.status, fields=outcome.value, error=outcome.error, retried=outcome.retried)`. Remove the old manual loop body entirely — no `for attempt in range(2)` should remain anywhere in this file. Do not change `JsonUsageProvider.__init__`, `fetch`, or `_fetch_payload` — only `_fetch_with_retry`'s body changes.

    Do not edit `test_probe_wham_usage.py` or `test_json_usage_provider.py` in this task — both are expected to pass completely unmodified given the alias/signature-preserving design above. If (and only if) running the verify command below reveals an actual signature mismatch, treat that as a Rule 1 bug per the executor's deviation rules: fix the migration code (not the tests) until it matches, and document the discrepancy in the Summary.
  </action>
  <verify>
    <automated>cd /d/00_Projects/codex_balance_widget && py -3 -m unittest test_probe_wham_usage test_json_usage_provider -v</automated>
  </verify>
  <acceptance_criteria>
    - `cd /d/00_Projects/codex_balance_widget && py -3 -m unittest test_probe_wham_usage test_json_usage_provider -v` exits 0, exactly 43 tests (37 + 6), 0 failures/errors
    - `git diff --stat -- test_probe_wham_usage.py test_json_usage_provider.py` shows no changes to either test file
    - `grep -c "class ProbeError(RuntimeError)" probe_wham_usage.py` equals 0 and `grep -c "ProbeError = FetchError" probe_wham_usage.py` equals 1
    - `grep -c "REDACT_KEYS" probe_wham_usage.py` is at least 2 (the constant's own definition plus its use inside the new `redact()` wrapper) — proves the denylist stayed local
    - `grep -c "for attempt in range(2)" json_usage_provider.py` equals 0 and `grep -c "fetch_with_retry_once" json_usage_provider.py` is at least 1
    - `py -3 -c "import probe_wham_usage, json_usage_provider"` exits 0 with no traceback (sys.path bootstrap resolves correctly from both files)
  </acceptance_criteria>
  <done>`probe_wham_usage.py` and `json_usage_provider.py` both import their error-classification/redaction/retry primitives from `usage_widget_common` via `sys.path` insertion; `REDACT_KEYS` remains local; all 43 pre-existing tests pass with zero test-file edits.</done>
</task>

<task type="auto">
  <name>Task 2: Wire codex_balance_widget_chrome.py's plan_fetch_outcome onto decide_fetch_source, run full regression</name>
  <files>codex_balance_widget_chrome.py</files>
  <read_first>
    - D:/00_Projects/codex_balance_widget/codex_balance_widget_chrome.py — lines 13-35 (imports — no `sys` present yet), line 48 (`import json_usage_provider` — anchor point for the new import), lines 533-538 (`FetchOutcome` dataclass — unchanged), lines 541-604 (`plan_fetch_outcome` — rewritten by this task)
    - D:/00_Projects/usage_widget_common/usage_widget_common/fetch_decision.py (Plan 03-01 output, read from the sibling repo path) — exact signature `decide_fetch_source(*, primary_ok: bool, fallback_ok: bool, has_existing_data: bool) -> FetchDecision(source: str)`; four possible `source` values: `"primary"`, `"fallback"`, `"retain_existing"`, `"none"`
    - D:/00_Projects/codex_balance_widget/test_codex_balance_widget_chrome.py — lines 95-300 (`TestPlanFetchOutcome`, 10 tests) and lines 302-397 (`TestFetchOnceJsonOkNoUsageDataFallback`, 3 tests) — every expected `outcome.source`/`outcome.balance`/`outcome.status_message`/`outcome.log_line` value in these 13 tests MUST remain byte-for-byte identical after this task's refactor
  </read_first>
  <action>
    This task edits `codex_balance_widget_chrome.py` in THIS repo — normal single-repo commit protocol. It also reaches the sibling `usage_widget_common` repo via `sys.path`, same pattern as Task 1.

    Add `import sys` to the stdlib import block between `shutil` and `threading` (alphabetical order already used in this file). Immediately after `import json_usage_provider` (and before `APP_VERSION = ...`), insert the sys.path bootstrap block (identical pattern to Task 1: `_SIBLING_COMMON = Path(__file__).resolve().parent.parent / "usage_widget_common"`; `if str(_SIBLING_COMMON) not in sys.path: sys.path.insert(0, str(_SIBLING_COMMON))` — `Path` is already imported in this file), followed by `from usage_widget_common.fetch_decision import decide_fetch_source`.

    Rewrite `plan_fetch_outcome` (lines 541-604) to delegate its chrome-fallback branch selection to `decide_fetch_source`, preserving every existing message string and log line EXACTLY. Keep the first block unchanged: `if json_status == "ok": balance = build_balance_from_json_fields(json_fields or {}); if balance.has_usage_data: ...` (including the weekly-exhausted-check message logic) `return FetchOutcome("json", balance, message, "Balance updated (source: json)")`, followed by the existing fallthrough `json_status = "error"; json_error = "json response had no recognizable usage fields"`. Keep the `if not chrome_attempted:` early-return block completely unchanged — this branch never calls `decide_fetch_source` (it is a 4th case outside the primary/fallback/retain/none skeleton, exactly as it behaves today, regardless of `has_existing_data`). Keep the `suffix` and `fallback_log` line computations unchanged.

    Then, in place of the current `if chrome_status == "ok" and chrome_text: ... / if has_existing_data: ... / elif chrome_status == "browser_error": ... / elif chrome_status == "login_required": ... / else: ...` chain: compute `chrome_balance = BalanceParser.parse(chrome_text) if (chrome_status == "ok" and chrome_text) else None` and `fallback_ok = chrome_balance is not None and chrome_balance.has_usage_data`. Call `decision = decide_fetch_source(primary_ok=False, fallback_ok=fallback_ok, has_existing_data=has_existing_data)` (`primary_ok` is always `False` here — this code path is only reached once `json_status` has already been established as failed above). Branch on `decision.source`:
    - `"fallback"` -> `balance = chrome_balance`; build `message` exactly as today (weekly-exhausted check + `suffix`, or plain success message + `suffix`); `return FetchOutcome("chrome", balance, message, "Balance updated (source: chrome)")`.
    - `"retain_existing"` -> if `chrome_balance is not None` (chrome succeeded but had no usable fields): `message = tr(language, "Showing last saved data · new data not recognized", "Показаны последние данные · новые не распознаны") + suffix`; else (chrome itself failed to produce any parsed balance): `message = tr(language, "Showing last saved data · refresh failed", "Показаны последние данные · обновить не удалось") + suffix`; both branches `return FetchOutcome("none", None, message, fallback_log)`.
    - `"none"` (i.e. `has_existing_data` is `False`) -> if `chrome_balance is not None`: `message = tr(language, "Data not recognized", "Данные не распознаны") + suffix`; elif `chrome_status == "browser_error"`: existing message text, NO `suffix` appended; elif `chrome_status == "login_required"`: existing message text, NO `suffix` appended; else: `message = tr(language, "Usage data timed out. Click Refresh.", "Не дождался данных Usage. Нажмите Обновить.") + suffix`; all four sub-branches `return FetchOutcome("none", None, message, fallback_log)`.

    Do not change `FetchOutcome`, `BalanceParser`, `build_balance_from_json_fields`, `is_weekly_limit_exhausted`, or any function outside `plan_fetch_outcome`'s body.

    Do not edit `test_codex_balance_widget_chrome.py` in this task — all 21 tests (the 10-test `TestPlanFetchOutcome` class and the 3-test `TestFetchOnceJsonOkNoUsageDataFallback` integration class included) are expected to pass completely unmodified. If any assertion fails, that is a Rule 1 bug in the refactor (fix the refactor, not the test) — re-derive the failing branch from the original lines 541-604 logic (see read_first) until every `TestPlanFetchOutcome` case matches exactly.

    Finish with the full-repo regression: run all three test modules together (see verify), then stage `probe_wham_usage.py json_usage_provider.py codex_balance_widget_chrome.py` (Task 1's edits plus this task's) and commit with message `refactor(03-02): migrate probe_wham_usage/json_usage_provider/codex_balance_widget_chrome onto usage_widget_common (retry, error classification, redaction, fetch-decision) — no behavior change`.
  </action>
  <verify>
    <automated>cd /d/00_Projects/codex_balance_widget && py -3 -c "import codex_balance_widget_chrome" && py -3 -m unittest test_probe_wham_usage test_json_usage_provider test_codex_balance_widget_chrome -v</automated>
  </verify>
  <acceptance_criteria>
    - Both commands in `<verify>` exit 0; the unittest run reports exactly 64 tests total (37 + 6 + 21), 0 failures/errors
    - `git diff --stat -- test_probe_wham_usage.py test_json_usage_provider.py test_codex_balance_widget_chrome.py` (against the commit before this plan started) shows no changes to any of the three test files
    - `grep -c "decide_fetch_source" codex_balance_widget_chrome.py` is at least 1
    - `grep -c "chrome_status == \"ok\" and chrome_text" codex_balance_widget_chrome.py` equals 0 — the old inline branch condition inside `plan_fetch_outcome` has been fully replaced by the `decide_fetch_source` call (the one exception is the `chrome_balance = ... if (chrome_status == "ok" and chrome_text) else None` assignment line itself, which computes the input to `decide_fetch_source` rather than branching directly — adjust the grep target if needed to confirm the branching chain, not the input computation, was removed)
    - `py -3 -c "import sys; sys.path.insert(0, r'd:/00_Projects/usage_widget_common'); import usage_widget_common; import probe_wham_usage, json_usage_provider, codex_balance_widget_chrome; print('all import clean')"` prints `all import clean` with no traceback
  </acceptance_criteria>
  <done>`codex_balance_widget_chrome.plan_fetch_outcome` delegates its 3-way fallback decision to `usage_widget_common.decide_fetch_source` with zero observable behavior change (all 10 `TestPlanFetchOutcome` cases + 3 `fetch_once` integration tests pass unmodified); full 64-test regression suite (`probe_wham_usage` + `json_usage_provider` + `codex_balance_widget_chrome`) passes; Roadmap Phase 3 Success Criteria 1-5 are all satisfied (1, 4, 5 by Plan 03-01; 2, 3 by this plan).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `probe_wham_usage.py` / `json_usage_provider.py` / `codex_balance_widget_chrome.py` -> `usage_widget_common` (sibling repo) | Same code-execution boundary introduced in Plan 03-01, now actually crossed by the live widget on every run via three separate `sys.path` bootstrap blocks |
| `codex_balance_widget_chrome.py` -> `chatgpt.com/backend-api` (unchanged from Phase 1/2) | Unchanged external trust boundary — this migration introduces no new external I/O |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-04 | Tampering | `sys.path` bootstrap duplicated in 3 files (`probe_wham_usage.py`, `json_usage_provider.py`, `codex_balance_widget_chrome.py`) | mitigate | Each bootstrap block is idempotent (`if str(path) not in sys.path`) and computes a fixed relative sibling location from `__file__`, not from any user/network input — no injection vector; identical pattern in all three files, verified by grep in each task's acceptance criteria. |
| T-3-05 | Information Disclosure | `plan_fetch_outcome` refactor accidentally changing which text reaches the tray tooltip/status bar/log | mitigate | The full existing `TestPlanFetchOutcome` suite (10 tests) plus the `TestFetchOnceJsonOkNoUsageDataFallback` integration suite (3 tests) are re-run completely unmodified and must pass byte-for-byte, catching any message/branch regression before this plan is considered done. |
| T-3-06 | Denial of Service | A single implementation bug in the shared `retry.py`/`redaction.py`/`fetch_decision.py` now surfaces in the JSON fetch path AND (via Phase 4) the Claude widget, instead of being isolated to one widget | accept | `usage_widget_common`'s own unit tests (Plan 03-01) directly cover every attempt-count/error-chaining edge case; this plan additionally reruns the full 64-test Codex regression suite, proving no observable change before Phase 4 begins depending on the same code. |
| T-3-SC | Tampering | npm/pip/cargo installs | mitigate | No new installs — no `requirements.txt` change, no new third-party import in any of the three modified files (verified via grep of import statements in Task 1/2 acceptance criteria). |
</threat_model>

<verification>
- `cd /d/00_Projects/codex_balance_widget && py -3 -m unittest test_probe_wham_usage test_json_usage_provider test_codex_balance_widget_chrome -v` — exit 0, 64 tests total, full Phase 1+2+3 regression suite
- `git diff --stat -- test_probe_wham_usage.py test_json_usage_provider.py test_codex_balance_widget_chrome.py` (relative to the commit before this plan started) — empty, no test files touched
- `git -C /d/00_Projects/usage_widget_common status --porcelain` — empty; this plan touches ONLY `codex_balance_widget`, zero changes in the sibling repo
</verification>

<success_criteria>
- Roadmap Phase 3 Success Criterion 2: `json_usage_provider.py` and `probe_wham_usage.py` import retry/classification/redaction logic from the shared package instead of local copies — satisfied.
- Success Criterion 3: all existing Codex widget/provider tests still pass unmodified in intent (and, in this design, unmodified in fact) — satisfied.
- Combined with Plan 03-01 (Success Criteria 1, 4, 5), all 5 Roadmap Phase 3 Success Criteria are satisfied at the close of this plan.
</success_criteria>

<output>
Create `.planning/phases/03-shared-library-extraction-codex-migration/03-02-SUMMARY.md` when done.
</output>
