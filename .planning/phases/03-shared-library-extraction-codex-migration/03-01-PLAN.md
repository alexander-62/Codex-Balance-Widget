---
phase: 03-shared-library-extraction-codex-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - d:/00_Projects/usage_widget_common/.gitignore
  - d:/00_Projects/usage_widget_common/README.md
  - d:/00_Projects/usage_widget_common/usage_widget_common/__init__.py
  - d:/00_Projects/usage_widget_common/usage_widget_common/errors.py
  - d:/00_Projects/usage_widget_common/usage_widget_common/redaction.py
  - d:/00_Projects/usage_widget_common/usage_widget_common/retry.py
  - d:/00_Projects/usage_widget_common/usage_widget_common/fetch_decision.py
  - d:/00_Projects/usage_widget_common/tests/test_errors.py
  - d:/00_Projects/usage_widget_common/tests/test_redaction.py
  - d:/00_Projects/usage_widget_common/tests/test_retry.py
  - d:/00_Projects/usage_widget_common/tests/test_fetch_decision.py
autonomous: true
requirements: [SHARED-01, SHARED-02, SHARED-03, SHARED-04]
must_haves:
  truths:
    - "`cd /d/00_Projects/usage_widget_common && py -3 -m unittest discover -s tests -v` passes with 0 failures/errors, exercising FetchError, redact/redaction_clean, fetch_with_retry_once, and decide_fetch_source"
    - "`usage_widget_common` is its own independent git repository (`git -C /d/00_Projects/usage_widget_common rev-parse --is-inside-work-tree` prints `true`) with at least 4 commits, entirely separate from `codex_balance_widget`'s git history (Success Criterion 1)"
    - "Every `import`/`from` line across `usage_widget_common/*.py` resolves to a stdlib module or another `usage_widget_common` submodule — no third-party package name appears anywhere (Success Criterion 4)"
    - "`decide_fetch_source` is callable with only `primary_ok`, `fallback_ok`, `has_existing_data` — no Codex/JSON/Chrome-specific parameter name exists on its signature, proving it is source-agnostic (Success Criterion 5)"
    - "`py -3 -c \"import usage_widget_common\"` succeeds standalone, with no widget code, no codex_balance_widget repo on sys.path, and no network access — proving independent importability (Success Criterion 1)"
  artifacts:
    - path: "d:/00_Projects/usage_widget_common/usage_widget_common/errors.py"
      provides: "FetchError(message, *, retryable=False) — generalized from probe_wham_usage.ProbeError"
      contains: "class FetchError(RuntimeError)"
    - path: "d:/00_Projects/usage_widget_common/usage_widget_common/redaction.py"
      provides: "redact(obj, keys=frozenset()) / redaction_clean(text, forbidden_substrings=...) — generalized from probe_wham_usage.redact/redaction_clean, caller supplies its own denylist"
      contains: "def redact"
    - path: "d:/00_Projects/usage_widget_common/usage_widget_common/retry.py"
      provides: "fetch_with_retry_once(fetch_once, *, retry_delay=1.0) -> RetryOutcome — generalized from json_usage_provider.JsonUsageProvider._fetch_with_retry"
      contains: "async def fetch_with_retry_once"
    - path: "d:/00_Projects/usage_widget_common/usage_widget_common/fetch_decision.py"
      provides: "decide_fetch_source(*, primary_ok, fallback_ok, has_existing_data) -> FetchDecision — generalized from codex_balance_widget_chrome.plan_fetch_outcome's 3-way choice"
      contains: "def decide_fetch_source"
    - path: "d:/00_Projects/usage_widget_common/usage_widget_common/__init__.py"
      provides: "public re-export surface for the whole package"
      contains: "FetchError"
  key_links:
    - from: "usage_widget_common/retry.py"
      to: "usage_widget_common/errors.py"
      via: "except FetchError: — distinguishes retryable from non-retryable failures inside the shared retry loop"
      pattern: "except FetchError"
    - from: "tests/test_retry.py"
      to: "usage_widget_common.retry.fetch_with_retry_once"
      via: "unittest.IsolatedAsyncioTestCase calling fetch_with_retry_once with AsyncMock side_effect sequences"
      pattern: "fetch_with_retry_once\\("
    - from: "usage_widget_common/__init__.py"
      to: "usage_widget_common/errors.py, redaction.py, retry.py, fetch_decision.py"
      via: "from .errors import FetchError (and siblings) — package-root re-exports"
      pattern: "from \\.errors import FetchError"
---

<objective>
**TWO-REPOSITORY PLAN — READ THIS FIRST.** Every file this plan creates, and every git commit this plan makes, lives in a brand-new sibling repository at `d:/00_Projects/usage_widget_common` — NOT inside `d:/00_Projects/codex_balance_widget` (the repo where `.planning/` lives and where this PLAN.md itself is tracked). The two repos are siblings under `d:/00_Projects/`, not nested. This plan does not modify a single file inside `d:/00_Projects/codex_balance_widget` (Plan 03-02 does that). Per user decision (03-CONTEXT.md, "Shared Package Location"): own repo, own git history, consumed by both widgets via `sys.path` insertion — no PyPI publish, no `pip install`.

Extract the retry-once, error-classification, redaction, and fetch-decision primitives proven in Codex's `probe_wham_usage.py` / `json_usage_provider.py` / `codex_balance_widget_chrome.py` (Phase 1/2) into this new, independent, stdlib-only package, generalized so a source-agnostic caller (any widget) supplies its own semantics.

Purpose: give Plan 03-02 (Codex migration) and Phase 4 (Claude widget adoption) a single, tested, versioned home for logic currently duplicated only inside the Codex widget — proven against Codex's own already-tested behavior before any other widget depends on it.

Output: `d:/00_Projects/usage_widget_common` — a new git repo containing the `usage_widget_common` Python package (`errors.py`, `redaction.py`, `retry.py`, `fetch_decision.py`, `__init__.py` re-exporting all four) plus a full `tests/` suite, entirely stdlib-only, zero dependency on any widget code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-shared-library-extraction-codex-migration/03-CONTEXT.md

# Extraction sources (read-only in THIS plan — not edited here; Plan 03-02 edits them)
@probe_wham_usage.py
@json_usage_provider.py
@test_probe_wham_usage.py
@test_json_usage_provider.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bootstrap sibling repo + FetchError + generic redaction</name>
  <files>d:/00_Projects/usage_widget_common/.gitignore, d:/00_Projects/usage_widget_common/README.md, d:/00_Projects/usage_widget_common/usage_widget_common/__init__.py, d:/00_Projects/usage_widget_common/usage_widget_common/errors.py, d:/00_Projects/usage_widget_common/usage_widget_common/redaction.py, d:/00_Projects/usage_widget_common/tests/test_errors.py, d:/00_Projects/usage_widget_common/tests/test_redaction.py</files>
  <read_first>
    - D:/00_Projects/codex_balance_widget/.planning/phases/03-shared-library-extraction-codex-migration/03-CONTEXT.md — "Shared Package Location" (repo path is a locked user decision, not up for reinterpretation) and "Claude's Discretion" (module layout, naming, import mechanism)
    - D:/00_Projects/codex_balance_widget/probe_wham_usage.py — lines 51-61 (`class ProbeError(RuntimeError)`) and lines 238-260 (`redact`/`redaction_clean`) — the exact extraction source. Do NOT edit this file in this task; it is read-only context here (Plan 03-02 migrates it).
    - D:/00_Projects/codex_balance_widget/test_probe_wham_usage.py — lines 156-189 (`TestRedact`) and lines 308-316 (`TestProbeErrorRetryable`) — the exact behaviors the new shared primitives must replicate.
  </read_first>
  <behavior>
    - `FetchError("msg")` -> `exc.retryable is False` (default)
    - `FetchError("msg", retryable=True)` -> `exc.retryable is True`, `str(exc) == "msg"`
    - `FetchError` is a subclass of `RuntimeError` (`issubclass(FetchError, RuntimeError)`)
    - `redact({"email": "e@x.com", "user_id": "u1", "access_token": "tok", "nested": {"email": "y@x.com"}}, keys=frozenset({"email", "user_id"}))` -> `email`/`user_id`/`access_token`/nested `email` all become `"<redacted>"` (access_token via the "token" substring rule, not via `keys`); original dict is untouched after the call
    - `redact(...)` on a list recurses into every element; on any non-dict/non-list value returns it unchanged
    - `redaction_clean("token eyJhbGci")` -> `False`; `redaction_clean("user@example.com")` -> `False`; `redaction_clean("clean text")` -> `True`
  </behavior>
  <action>
    CRITICAL — repo targeting: every Bash command in this task must run with `cd /d/00_Projects/usage_widget_common` (create the directory first with `mkdir -p` if absent) BEFORE any git or test command. Do NOT run this project's standard per-task commit protocol (no `gsd-sdk query commit`, no `commit-to-subrepo` — `sub_repos` is not configured in `.planning/config.json` for this project, and that mechanism is designed for child directories inside one repo, not a true sibling repo one level up). Use plain `git init` / `git add` / `git commit` directly inside `d:/00_Projects/usage_widget_common` instead. This repo's own SUMMARY.md (written later, inside `d:/00_Projects/codex_balance_widget/.planning/`) must record the sibling repo's resulting commit hashes, since `git log` inside `codex_balance_widget` will never show them.

    Step 1 — bootstrap: `cd /d/00_Projects/usage_widget_common` (creating it if needed), run `git init`, then `git branch -M main` (matches this project's branch-naming convention). Create `.gitignore` with three lines: `__pycache__/`, `*.pyc`, `.pytest_cache/`. Create `README.md`: state the package name `usage_widget_common`, that it is stdlib-only, shared by `codex_balance_widget` (`d:/00_Projects/codex_balance_widget`) today and by `claude_balance_widget_v1` (`d:/00_Projects/claude_balance_widget_v1`) from Phase 4 onward, consumed via `sys.path` insertion (no PyPI publish, no `pip install`), and list the four modules this phase creates: `errors.py` (`FetchError`), `redaction.py` (`redact`/`redaction_clean`), `retry.py` (`fetch_with_retry_once`), `fetch_decision.py` (`decide_fetch_source`). Create the package directory `usage_widget_common/` (so the repo root holds both the package dir and the repo's own `.git`) with `usage_widget_common/__init__.py` containing only a one-line module docstring: `"""Stdlib-only primitives shared by codex_balance_widget and claude_balance_widget_v1."""` (re-exports are added in Task 2). Create an empty `tests/` directory.

    Step 2 (RED — test-first, matching this project's established convention) — create `tests/test_errors.py` (`from __future__ import annotations`, stdlib `unittest`) with class `TestFetchError(unittest.TestCase)` covering the first three `<behavior>` points above (default `retryable=False`; explicit `retryable=True` + `str(exc)`; `issubclass(FetchError, RuntimeError)`). Create `tests/test_redaction.py` with class `TestRedact(unittest.TestCase)` covering the denylist + "token"-substring + no-mutation + nested/list-recursion behavior, and class `TestRedactionClean(unittest.TestCase)` covering the three `redaction_clean` cases. Run `cd /d/00_Projects/usage_widget_common && py -3 -m unittest discover -s tests -v` and confirm both files FAIL with `ModuleNotFoundError` (expected RED — `usage_widget_common.errors`/`usage_widget_common.redaction` do not exist yet). Stage `tests/test_errors.py tests/test_redaction.py`, commit `test: add failing tests for FetchError and generic redaction primitives`.

    Step 3 (GREEN) — create `usage_widget_common/errors.py` (`from __future__ import annotations`, no imports) defining `class FetchError(RuntimeError):` with `def __init__(self, message: str, *, retryable: bool = False) -> None:` calling `super().__init__(message)` then `self.retryable = retryable`; docstring: generalizes `probe_wham_usage.ProbeError` (Phase 1/2) — any fetch operation raises this, `retryable` distinguishes transient (network/429) from permanent (401/403/malformed) failures. Create `usage_widget_common/redaction.py` (`from __future__ import annotations`, `from typing import Any`) defining `REDACTED_PLACEHOLDER = "<redacted>"`, `DEFAULT_FORBIDDEN_SUBSTRINGS: tuple[str, ...] = ("eyJ", "@")`, `def redact(obj: Any, keys: frozenset[str] = frozenset()) -> Any:` (dict -> new dict, redact values whose key is in `keys` OR contains `"token"` case-insensitively via `"token" in key.lower()`, else recurse into the value; list -> new list with every element recursed; anything else -> returned unchanged) and `def redaction_clean(text: str, forbidden_substrings: tuple[str, ...] = DEFAULT_FORBIDDEN_SUBSTRINGS) -> bool:` returning `all(s not in text for s in forbidden_substrings)`. Docstrings on both must state they generalize `probe_wham_usage.redact`/`redaction_clean` (Phase 1) — the caller supplies its OWN domain-specific denylist via `keys`; the `"token"`-substring rule and the `eyJ`/`@` post-check are the fixed, shared parts (this is deliberate: it prevents the domain-specific denylist from ever being centralized where it could silently drift out of sync between widgets — see this plan's threat model). Re-run the Step 2 test command; both files must now PASS (GREEN). Stage `usage_widget_common/errors.py usage_widget_common/redaction.py`, commit `feat: add FetchError and generic redaction primitives`.
  </action>
  <verify>
    <automated>cd /d/00_Projects/usage_widget_common && py -3 -m unittest discover -s tests -v</automated>
  </verify>
  <acceptance_criteria>
    - `cd /d/00_Projects/usage_widget_common && py -3 -m unittest discover -s tests -v` exits 0, at least 8 tests total across `test_errors.py` + `test_redaction.py`, 0 failures/errors
    - `git -C /d/00_Projects/usage_widget_common log --oneline` shows exactly 2 commits so far, oldest starting with `test:`, newest with `feat:`
    - `grep -c "class FetchError(RuntimeError)" /d/00_Projects/usage_widget_common/usage_widget_common/errors.py` equals 1
    - `grep -c "REDACT_KEYS" /d/00_Projects/usage_widget_common/usage_widget_common/redaction.py` equals 0 — proves the Codex-specific denylist name was NOT copied into the shared package
    - `git -C /d/00_Projects/codex_balance_widget status --porcelain` shows no changes to `probe_wham_usage.py`, `json_usage_provider.py`, or `codex_balance_widget_chrome.py` — this task touched only the sibling repo
  </acceptance_criteria>
  <done>`FetchError` and generic `redact()`/`redaction_clean()` exist in the new independent sibling repo, fully unit-tested, with zero Codex-specific field names inside the shared package; the new repo has its own git history with test-first commits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: retry-once wrapper + fetch-decision skeleton + finalize public API</name>
  <files>d:/00_Projects/usage_widget_common/usage_widget_common/retry.py, d:/00_Projects/usage_widget_common/usage_widget_common/fetch_decision.py, d:/00_Projects/usage_widget_common/usage_widget_common/__init__.py, d:/00_Projects/usage_widget_common/tests/test_retry.py, d:/00_Projects/usage_widget_common/tests/test_fetch_decision.py</files>
  <read_first>
    - D:/00_Projects/codex_balance_widget/json_usage_provider.py — lines 57-89 (`_fetch_with_retry` — the exact retry-once loop being generalized: at most 2 attempts, sleep only before the 2nd, `first_error` chaining, retryable determined by `exc.retryable`, unexpected non-`ProbeError` exceptions treated as non-retryable)
    - D:/00_Projects/codex_balance_widget/test_json_usage_provider.py — all 6 tests (`TestJsonUsageProviderFetch`) — the exact attempt-count and chaining behaviors `fetch_with_retry_once` must reproduce generically
    - D:/00_Projects/codex_balance_widget/codex_balance_widget_chrome.py — lines 541-604 (`plan_fetch_outcome`) — the exact "primary ok / primary failed -> fallback / both failed -> retain existing data" branch order being generalized (see 03-CONTEXT.md and Roadmap Success Criterion 5)
    - D:/00_Projects/usage_widget_common/usage_widget_common/errors.py (this plan's Task 1 output) — `FetchError.__init__(message, *, retryable=False)`
  </read_first>
  <behavior>
    - `fetch_with_retry_once` — `fetch_once` succeeds immediately -> `RetryOutcome(status="ok", value=<v>, retried=False)`, awaited exactly once
    - `fetch_once` raises `FetchError("msg", retryable=False)` -> `status="error"`, `retried is False`, awaited once, `error` contains `"msg"`
    - `fetch_once` raises a plain `ValueError("boom")` (non-`FetchError`) -> `status="error"`, `retried is False`, awaited once, `error` contains `"ValueError"` and `"boom"`
    - `fetch_once` raises `FetchError(retryable=True)` then returns a value -> `status="ok"`, `value` equals the 2nd return, `retried is True`, awaited exactly twice
    - `fetch_once` raises `FetchError(retryable=True)` twice with different messages -> `status="error"`, `retried is True`, awaited exactly twice, `error` contains BOTH messages joined as `"{first}; retry: {second}"`
    - `decide_fetch_source(primary_ok=True, fallback_ok=<either>, has_existing_data=<either>)` -> `FetchDecision(source="primary")` regardless of the other two args
    - `decide_fetch_source(primary_ok=False, fallback_ok=True, has_existing_data=<either>)` -> `source == "fallback"` regardless of `has_existing_data`
    - `decide_fetch_source(primary_ok=False, fallback_ok=False, has_existing_data=True)` -> `source == "retain_existing"`
    - `decide_fetch_source(primary_ok=False, fallback_ok=False, has_existing_data=False)` -> `source == "none"`
  </behavior>
  <action>
    Continue in the same sibling repo — every command below runs with `cd /d/00_Projects/usage_widget_common` first. Do not touch anything in `d:/00_Projects/codex_balance_widget`.

    Step 1 (RED) — create `tests/test_retry.py` (`unittest.IsolatedAsyncioTestCase`, `from __future__ import annotations`, `unittest.mock.AsyncMock`) with class `TestFetchWithRetryOnce` covering the 5 `<behavior>` points for `fetch_with_retry_once`. Always pass `retry_delay=0` (no real `asyncio.sleep` wait), matching this project's existing `JsonUsageProvider(retry_delay=0)` test convention. Use `AsyncMock(side_effect=[...])` for the `fetch_once` callable to control return values/exceptions per call. Create `tests/test_fetch_decision.py` (`unittest.TestCase`) with class `TestDecideFetchSource` covering the 4 `<behavior>` points for `decide_fetch_source` (test the "primary wins regardless" and "fallback wins regardless of has_existing_data" claims with at least 2 argument combinations each). Run `cd /d/00_Projects/usage_widget_common && py -3 -m unittest discover -s tests -v`; both new files must FAIL with `ModuleNotFoundError` (RED). Stage `tests/test_retry.py tests/test_fetch_decision.py`, commit `test: add failing tests for retry-once wrapper and fetch-decision skeleton`.

    Step 2 (GREEN) — create `usage_widget_common/retry.py` (`from __future__ import annotations`, `import asyncio`, `from dataclasses import dataclass`, `from typing import Any, Awaitable, Callable`, `from .errors import FetchError`) defining `@dataclass class RetryOutcome:` with fields `status: str`, `value: Any | None = None`, `error: str | None = None`, `retried: bool = False`; and `async def fetch_with_retry_once(fetch_once: Callable[[], Awaitable[Any]], *, retry_delay: float = 1.0) -> RetryOutcome:` implementing exactly the loop read in `json_usage_provider._fetch_with_retry`: `first_error: str | None = None`; `for attempt in range(2):` — `await asyncio.sleep(retry_delay)` when `attempt == 1`; `try: value = await fetch_once(); return RetryOutcome("ok", value=value, retried=attempt == 1)`; `except FetchError as exc: error_text = str(exc); retryable = exc.retryable`; `except Exception as exc: error_text = f"{type(exc).__name__}: {exc}"; retryable = False`; then `if attempt == 0 and retryable: first_error = error_text; continue`; `if first_error is not None: error_text = f"{first_error}; retry: {error_text}"`; `return RetryOutcome("error", error=error_text, retried=attempt == 1)`; after the loop, `return RetryOutcome("error", error=first_error, retried=True)` (unreachable, kept for static analysis, matching the original's own comment). Docstring: mirrors `json_usage_provider.JsonUsageProvider._fetch_with_retry` (Phase 2); now the single shared implementation any widget calls.

    Create `usage_widget_common/fetch_decision.py` (`from __future__ import annotations`, `from dataclasses import dataclass`) defining `@dataclass class FetchDecision:` with field `source: str`, and `def decide_fetch_source(*, primary_ok: bool, fallback_ok: bool, has_existing_data: bool) -> FetchDecision:` implementing exactly: `if primary_ok: return FetchDecision("primary")`; `if fallback_ok: return FetchDecision("fallback")`; `if has_existing_data: return FetchDecision("retain_existing")`; `return FetchDecision("none")`. Docstring: pure fetch-decision skeleton generalizing `codex_balance_widget_chrome.plan_fetch_outcome`'s 3-way choice ("primary source ok" / "primary source failed -> fallback" / "both failed -> retain existing data"); callers define what "ok" means for their own primary/fallback source (e.g. "HTTP succeeded AND response had recognizable data") — this function only expresses the 3-way choice, not source-specific status parsing (source-specific statuses like Chrome's `login_required`/`browser_error` stay in the widget).

    Update `usage_widget_common/__init__.py` (replace Task 1's docstring-only placeholder) to re-export the full public API: `from .errors import FetchError`, `from .redaction import redact, redaction_clean, REDACTED_PLACEHOLDER`, `from .retry import RetryOutcome, fetch_with_retry_once`, `from .fetch_decision import FetchDecision, decide_fetch_source`, plus `__all__ = ["FetchError", "redact", "redaction_clean", "REDACTED_PLACEHOLDER", "RetryOutcome", "fetch_with_retry_once", "FetchDecision", "decide_fetch_source"]`. Keep the one-line module docstring above the imports.

    Run `cd /d/00_Projects/usage_widget_common && py -3 -m unittest discover -s tests -v` — all tests across all 4 test files must pass (GREEN). Run the standalone-importability smoke check: `cd /d/00_Projects/usage_widget_common && py -3 -c "import usage_widget_common as u; print(u.FetchError, u.fetch_with_retry_once, u.redact, u.redaction_clean, u.decide_fetch_source)"` — must print all five names with no traceback (Success Criterion 1). Run the stdlib-only audit: `grep -rhE "^(import |from )" usage_widget_common/*.py | grep -Ev "^(from __future__ import annotations|import asyncio$|from dataclasses import dataclass$|from typing import|from \.errors import|from \.redaction import|from \.retry import|from \.fetch_decision import)" | wc -l` must print `0` (Success Criterion 4 — no third-party import anywhere). Stage `usage_widget_common/retry.py usage_widget_common/fetch_decision.py usage_widget_common/__init__.py`, commit `feat: add retry-once wrapper, fetch-decision skeleton, and finalize public API`.
  </action>
  <verify>
    <automated>cd /d/00_Projects/usage_widget_common && py -3 -m unittest discover -s tests -v</automated>
  </verify>
  <acceptance_criteria>
    - `cd /d/00_Projects/usage_widget_common && py -3 -m unittest discover -s tests -v` exits 0, at least 19 tests total across all 4 test files, 0 failures/errors
    - `py -3 -c "import usage_widget_common as u; assert all([u.FetchError, u.fetch_with_retry_once, u.redact, u.redaction_clean, u.decide_fetch_source])"` exits 0
    - `grep -rhE "^(import |from )" usage_widget_common/*.py | grep -Ev "^(from __future__ import annotations|import asyncio$|from dataclasses import dataclass$|from typing import|from \.errors import|from \.redaction import|from \.retry import|from \.fetch_decision import)" | wc -l` equals `0`
    - `git -C /d/00_Projects/usage_widget_common log --oneline` shows 4 commits total (2 from Task 1 + 2 from this task), alternating `test:`/`feat:`
    - `grep -c "for attempt in range(2)" /d/00_Projects/usage_widget_common/usage_widget_common/retry.py` equals 1 (the shared engine now owns the ONLY copy of this loop)
  </acceptance_criteria>
  <done>`usage_widget_common` package is complete: `FetchError`, `redact`/`redaction_clean`, `fetch_with_retry_once`, `decide_fetch_source` all implemented, tested, and re-exported from the package root; proven importable standalone; proven stdlib-only; 4-commit git history in its own independent repo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local filesystem (`d:/00_Projects/usage_widget_common`) -> `sys.path` insertion -> any consuming Python process | New code-execution surface: any file placed under this directory becomes importable/executable by whichever widget process inserts it into `sys.path` (Plan 03-02 for Codex; Phase 4 for Claude widget) |
| `usage_widget_common`'s own unit tests | No network, no external input, no filesystem access beyond the repo itself — pure functions/dataclasses only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Tampering | `sys.path` insertion of a sibling directory (consumed by Plan 03-02 and Phase 4) | accept | Single-user local Windows desktop app; `usage_widget_common` is a git-tracked sibling repo controlled by the same developer — same trust level as the existing single-file `import probe_wham_usage` pattern already in this codebase. No new privilege boundary is crossed; no remote/untrusted input reaches this package. |
| T-3-02 | Information Disclosure | Redaction denylist drift across separately-versioned packages | mitigate | `redact()`'s `keys` parameter is supplied entirely by the caller — the Codex-specific denylist (field names like `user_id`/`account_id`/`email`) is NOT copied into this shared package (verified: `grep -c REDACT_KEYS usage_widget_common/redaction.py` == 0). Only the generic traversal + fixed `"token"`-substring rule + `eyJ`/`@` post-check are centralized. A future payload-schema change cannot silently lose redaction because the domain-specific list is never centralized across widgets. |
| T-3-03 | Repudiation / Consistency | A bug in the shared `fetch_with_retry_once` silently affects every future consumer (Codex now, Claude in Phase 4) | mitigate | `fetch_with_retry_once` has its own direct unit tests (Task 2) asserting exact attempt counts and error-message chaining, independent of any widget; Plan 03-02 additionally re-runs the full pre-existing 64-test Codex regression suite against the migrated code. |
| T-3-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager installs in this plan — `usage_widget_common` is stdlib-only, verified by the grep-based import audit in Task 2's acceptance criteria; no `requirements.txt`/`pyproject.toml` with dependencies is created. |
</threat_model>

<verification>
- `cd /d/00_Projects/usage_widget_common && py -3 -m unittest discover -s tests -v` — exit 0, full new-repo test suite (>=19 tests across 4 files)
- `git -C /d/00_Projects/usage_widget_common log --oneline` — 4 commits, independent of `codex_balance_widget`'s own git history
- `git -C /d/00_Projects/codex_balance_widget status --porcelain -- probe_wham_usage.py json_usage_provider.py codex_balance_widget_chrome.py` — empty; this plan touches ONLY the sibling repo, zero changes to any Codex production file
</verification>

<success_criteria>
- Roadmap Phase 3 Success Criterion 1: shared package exists, exposes retry-once/error-classification/redaction/fetch-decision primitives, importable independently of any widget — satisfied.
- Success Criterion 4: package imports are stdlib-only — satisfied and verified via grep audit.
- Success Criterion 5: `decide_fetch_source` parameterized by generic `primary_ok`/`fallback_ok`/`has_existing_data`, not hardcoded to json/chrome — satisfied.
- Success Criteria 2 and 3 (Codex file migration, 64 existing tests passing) are OUT OF SCOPE for this plan — delivered by Plan 03-02, which depends on this plan.
</success_criteria>

<output>
Create `.planning/phases/03-shared-library-extraction-codex-migration/03-01-SUMMARY.md` when done. This SUMMARY (written inside `codex_balance_widget`'s own `.planning/`) MUST explicitly record the sibling repo's git commit hashes (from `git -C /d/00_Projects/usage_widget_common log --oneline`), since they will never appear in `codex_balance_widget`'s own `git log`.
</output>
