---
phase: 01-json-endpoint-probe
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - wham_usage_fixture.json
autonomous: false
requirements: [PROBE-01, PROBE-03]
must_haves:
  truths:
    - "py -3 probe_wham_usage.py завершает exit 0 и печатает HTTP status: 200 с живым токеном из ~/.codex/auth.json — без Chrome"
    - "Фикстура wham_usage_fixture.json существует и не содержит подстрок eyJ и @ (нет токенов и email)"
    - "git status показывает только новые файлы: рабочий код виджета и лончеры не изменены ни на строку"
    - "Пользователь подтвердил: извлечённые поля соответствуют текущему Balance виджета (5h может отсутствовать на plus — валидно)"
  artifacts:
    - path: wham_usage_fixture.json
      provides: "редактированный сырой ответ wham/usage — базовая фикстура для парсера Phase 2"
      contains: "rate_limit"
  key_links:
    - from: wham_usage_fixture.json
      to: probe_wham_usage.py
      via: "генерируется write_fixture() при живом запуске"
      pattern: "<redacted>"
---

<objective>
Живой запуск пробы: подтвердить HTTP 200 от `https://chatgpt.com/backend-api/wham/usage` с реальным токеном, зафиксировать редактированную фикстуру ответа и доказать чистоту git (только новые файлы). Финальное человеческое ревью вывода и фикстуры.

Purpose: закрыть Success Criteria 1-3 и 5 фазы (живой статус + pretty JSON, извлечённые поля, фикстура без секретов, нетронутый рабочий код) — [LOCKED] решения CONTEXT.md соблюдаются автоматической проверкой git.

Output: `wham_usage_fixture.json` (единственный новый артефакт этого плана) + подтверждённый пользователем результат пробы.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-json-endpoint-probe/01-RESEARCH.md
@.planning/phases/01-json-endpoint-probe/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Живой запуск пробы, фикстура и проверка чистоты git</name>
  <files>wham_usage_fixture.json</files>
  <read_first>
    - probe_wham_usage.py — собранный в плане 01-01 скрипт (флаги --fixture/--timeout, формат вывода)
    - .planning/phases/01-json-endpoint-probe/01-RESEARCH.md — раздел «Ответ (живой, 2026-07-17...)» для сверки формы полученного JSON и «Семантика ошибок» на случай не-200
  </read_first>
  <action>
    Выполнить `py -3 probe_wham_usage.py --fixture wham_usage_fixture.json` из корня репо (реальный токен уже в ~/.codex/auth.json, auth_mode=chatgpt — подтверждено RESEARCH Environment Availability). Ожидаемо: exit 0, в stdout «HTTP status: 200», pretty JSON редактированного ответа, блок «Extracted fields (Balance)» и путь фикстуры.

    Затем три автопроверки:
    1. Фикстура: файл wham_usage_fixture.json существует, валидный JSON, содержит ключ rate_limit, НЕ содержит подстрок "eyJ" и "@" (PROBE-03)
    2. Чистота git ([LOCKED], Success Criteria 5): `git status --porcelain codex_balance_widget_chrome.py codex_balance_widget_launcher.pyw install.bat run.bat run_hidden.vbs requirements.txt` — пустой вывод; общий `git status --porcelain` содержит только "??"-строки (новые файлы) помимо .planning/
    3. Сверка схемы: форма полученного JSON совпадает с Endpoint Contract из RESEARCH (rate_limit.primary_window.limit_window_seconds присутствует); расхождения зафиксировать в SUMMARY как schema drift — это результат фазы, не ошибка

    Если запрос вернул не-200: НЕ править рабочий код, диагностика скрипта сама укажет причину (401 -> запустить codex; 403 HTML -> Cloudflare, повторить позже). Повторить один раз; при устойчивом сбое остановиться и вернуть блокер с текстом диагностики (без токена).
  </action>
  <verify>
    <automated>cd /d/00_Projects/codex_balance_widget && py -3 probe_wham_usage.py --fixture wham_usage_fixture.json && py -3 -c "import json,sys;s=open('wham_usage_fixture.json',encoding='utf-8').read();d=json.loads(s);sys.exit(1 if ('eyJ' in s or '@' in s or 'rate_limit' not in d) else 0)" && test -z "$(git status --porcelain codex_balance_widget_chrome.py codex_balance_widget_launcher.pyw install.bat run.bat run_hidden.vbs requirements.txt)"</automated>
  </verify>
  <acceptance_criteria>
    - `py -3 probe_wham_usage.py --fixture wham_usage_fixture.json` — exit 0; stdout содержит "HTTP status: 200" и "Extracted fields" (PROBE-01)
    - wham_usage_fixture.json: json.loads успешен, ключ rate_limit присутствует, подстроки "eyJ" и "@" отсутствуют (PROBE-03)
    - `git status --porcelain` по шести файлам рабочего кода (codex_balance_widget_chrome.py, codex_balance_widget_launcher.pyw, install.bat, run.bat, run_hidden.vbs, requirements.txt) — пустой вывод
    - `git status --porcelain` в целом: probe_wham_usage.py, test_probe_wham_usage.py, wham_usage_fixture.json видны как новые ("??") либо уже закоммичены — модифицированных (" M") файлов вне .planning/ нет
  </acceptance_criteria>
  <done>Живой HTTP 200 получен, фикстура записана и прошла автопроверку редакции, рабочий код доказуемо не тронут.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Ревью вывода пробы и фикстуры</name>
  <files>none — ревью без изменения файлов</files>
  <read_first>
    - wham_usage_fixture.json — артефакт Task 1, предмет ревью
  </read_first>
  <action>
    Предъявить пользователю результат фазы на подтверждение: команду живого запуска, где искать блок «Extracted fields (Balance)», путь к фикстуре и вывод git status. Ничего не изменять — только собрать подтверждение по шагам из how-to-verify. Реализация уже выполнена (Task 1 и план 01-01); чекпоинт закрывает Success Criteria 1-2 фазы человеческим ревью соответствия полей модели Balance.
  </action>
  <what-built>Standalone stdlib-проба `probe_wham_usage.py`: один HTTP GET к chatgpt.com/backend-api/wham/usage с токеном из ~/.codex/auth.json — без Chrome. Напечатаны статус, редактированный JSON, извлечённые поля Balance; редактированная фикстура wham_usage_fixture.json сохранена; unit-тесты зелёные; рабочий код виджета не изменён (проверено git status).</what-built>
  <how-to-verify>
    1. В корне репо выполните: `py -3 probe_wham_usage.py --no-fixture`
    2. Убедитесь: напечатан «HTTP status: 200» и pretty JSON без вашего email и без длинных строк, начинающихся на "eyJ"
    3. В блоке «Extracted fields (Balance)» сверьте значения с тем, что показывает текущий виджет: weekly % (остаток), weekly reset — время в вашем локальном поясе, credits. Строка «five_hour_percent: отсутствует в ответе» на плане plus — ОЖИДАЕМОЕ поведение (5h-окно сейчас не приходит), не ошибка
    4. Откройте wham_usage_fixture.json: значения email/user_id/account_id заменены на "<redacted>", структура ответа сохранена
    5. Выполните `git status`: только новые файлы (probe_wham_usage.py, test_probe_wham_usage.py, wham_usage_fixture.json), рабочие файлы виджета не в списке изменённых
  </how-to-verify>
  <verify>
    <human-check>Пользователь прошёл шаги 1-5 из how-to-verify и ответил "approved"</human-check>
  </verify>
  <done>Получен ответ "approved": статус 200 и поля подтверждены, фикстура без PII, рабочий код не тронут.</done>
  <resume-signal>Напишите "approved" или опишите, что не так (например, проценты не совпадают с виджетом или в фикстуре виден email)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| живой запуск → stdout терминала | вывод может попасть в скриншоты/логи сессии — печатается только редактированный JSON |
| wham_usage_fixture.json → git-репозиторий | фикстура коммитится — PII/токены обязаны быть вырезаны до записи |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-05 | Information Disclosure | wham_usage_fixture.json в git | mitigate | автопроверка Task 1 (нет "eyJ"/"@") — блокирует до коммита; человеческое ревью файла в Task 2 |
| T-1-06 | Information Disclosure | stdout живого запуска | mitigate | скрипт печатает только redact()-payload (реализовано в 01-01, T-1-01); ревьюер в Task 2 подтверждает отсутствие email/JWT в выводе |
| T-1-07 | Tampering | рабочий код виджета ([LOCKED]) | mitigate | автопроверка `git status --porcelain` по шести файлам рабочего кода — пустой вывод обязателен |
| T-1-SC | Tampering | npm/pip/cargo installs | accept | установок нет — stdlib-only |
</threat_model>

<verification>
- `py -3 probe_wham_usage.py --fixture wham_usage_fixture.json` — exit 0, «HTTP status: 200»
- Автопроверка фикстуры: валидный JSON с rate_limit, без "eyJ"/"@"
- `git status --porcelain` по файлам рабочего кода — пусто
- Пользователь подтвердил соответствие извлечённых полей модели Balance
</verification>

<success_criteria>
- Success Criteria 1-5 фазы закрыты: живой статус + pretty JSON (1), извлечённые поля / честный список отсутствующих (2), фикстура с редакцией (3), диагностика без traceback — покрыта планом 01-01 (4), git только с новыми файлами (5)
- Зафиксирован фактический вид схемы (drift относительно RESEARCH, если есть, описан в SUMMARY)
</success_criteria>

<output>
Create `.planning/phases/01-json-endpoint-probe/01-02-SUMMARY.md` when done
</output>
