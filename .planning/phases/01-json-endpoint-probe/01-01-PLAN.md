---
phase: 01-json-endpoint-probe
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - probe_wham_usage.py
  - test_probe_wham_usage.py
autonomous: true
requirements: [PROBE-02, PROBE-04]
must_haves:
  truths:
    - "py -3 -m unittest test_probe_wham_usage -v проходит без сети (exit 0)"
    - "Окна классифицируются по limit_window_seconds (18000 -> 5h, 604800 -> weekly), никогда по позиции primary/secondary"
    - "Запуск probe_wham_usage.py с CODEX_HOME на пустую папку печатает русскую диагностику с упоминанием auth.json и завершает exit 1 без traceback"
    - "Access token и PII не печатаются в stdout ни в одном режиме, включая --debug"
  artifacts:
    - path: probe_wham_usage.py
      provides: "stdlib-проба wham/usage: load_tokens, jwt_exp, collect_windows, pick_window, extract_fields, redact, redaction_clean, fetch_usage, write_fixture, main"
      min_lines: 150
    - path: test_probe_wham_usage.py
      provides: "unittest-набор на чистые функции, без сети и без реального auth.json"
      contains: "unittest"
  key_links:
    - from: test_probe_wham_usage.py
      to: probe_wham_usage.py
      via: "прямой импорт модуля"
      pattern: "import probe_wham_usage|from probe_wham_usage import"
    - from: probe_wham_usage.py
      to: "$CODEX_HOME/auth.json"
      via: "os.environ.get('CODEX_HOME') с фолбэком Path.home()/'.codex'"
      pattern: "CODEX_HOME"
    - from: probe_wham_usage.py
      to: "https://chatgpt.com/backend-api/wham/usage"
      via: "urllib.request.Request с заголовком Authorization: Bearer"
      pattern: "backend-api/wham/usage"
---

<objective>
Создать standalone stdlib-скрипт `probe_wham_usage.py` (Python 3.14, Windows, только urllib/json/base64/pathlib/argparse) и unittest-файл `test_probe_wham_usage.py`, доказывающие извлекаемость usage-полей Codex из `GET https://chatgpt.com/backend-api/wham/usage` с Bearer-токеном из `~/.codex/auth.json`.

Purpose: снять главный риск проекта — недокументированную схему wham/usage — кодом, который Phase 2 переиспользует как провайдер. Реализует [LOCKED] решения из CONTEXT.md: рабочий код виджета не трогаем, только новые файлы (проба + тест).

Output: `probe_wham_usage.py` (чистое ядро + HTTP + CLI-диагностика) и `test_probe_wham_usage.py` (unit-тесты без сети). Живой запуск и фикстура — план 01-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-json-endpoint-probe/01-RESEARCH.md
@.planning/phases/01-json-endpoint-probe/01-PATTERNS.md
@d:/00_Projects/claude_balance_widget_v1/claude_balance_widget.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Чистое ядро пробы + unittest-набор (без сети)</name>
  <files>probe_wham_usage.py, test_probe_wham_usage.py</files>
  <read_first>
    - .planning/phases/01-json-endpoint-probe/01-RESEARCH.md — разделы «Endpoint Contract», «Token / auth.json Contract», «Field Mapping → Balance», «Code Examples» (готовые проверенные сниппеты collect_windows/pick/jwt_exp), «Common Pitfalls» 1,3,4,6,7
    - .planning/phases/01-json-endpoint-probe/01-PATTERNS.md — паттерн `_load_token` и defensive-парсинг `.get() or {}`, аналог теста test_core.py
    - d:/00_Projects/claude_balance_widget_v1/claude_balance_widget.py — строки 240-256 (_load_token) и 297-305 (defensive-парсинг) как структурный образец
    - D:/00_Projects/codex_balance_widget/codex_balance_widget_chrome.py — строки 194-204 (dataclass Balance: five_hour_percent, weekly_percent, credits, five_hour_reset_text, weekly_reset_text — всё str|None, проценты = ОСТАТОК). READ-ONLY, файл не изменять [LOCKED]
  </read_first>
  <behavior>
    - collect_windows: payload с primary_window (604800) и secondary_window (18000) -> 2 окна; payload с rate_limit=null -> []; payload без ключа rate_limit -> []; окна из additional_rate_limits[].rate_limit.primary_window попадают в список с пометкой limit_name
    - pick_window: target=18000, окно 18000 -> найдено; только 604800 -> None; допуск ±1200 (17500 -> найдено как 5h)
    - extract_fields на dict-фикстуре из живого ответа RESEARCH.md (только weekly-окно, used_percent=16, reset_at=1784792381, credits.balance="0"): weekly_percent == "84", five_hour_percent is None, credits == "0", weekly_reset_text содержит год 20xx (fromtimestamp в секундах, без деления/умножения на 1000), missing-список содержит five_hour
    - redact: dict с email/user_id/account_id/access_token/refresh_token/id_token/individual_limit -> все значения заменены на "<redacted>"; вложенность сохраняется; исходный payload не мутируется
    - redaction_clean: строка с "eyJ" -> False; строка с "@" -> False; чистая строка -> True
    - jwt_exp: валидный JWT c exp -> datetime; строка "garbage" -> None; JWT без exp -> None
    - load_tokens: несуществующий путь -> ProbeError с текстом, содержащим "auth.json"; JSON с auth_mode="apikey" и tokens=null -> ProbeError с текстом, содержащим "codex login"; валидный tokens -> кортеж (access_token, account_id); account_id отсутствует -> (access_token, None)
  </behavior>
  <action>
    Создать `test_probe_wham_usage.py` (stdlib unittest, классы TestWindows / TestExtract / TestRedact / TestJwt / TestLoadTokens) с тестами из блока behavior — тесты работают на inline dict-фикстурах и tempfile.TemporaryDirectory, БЕЗ сети и БЕЗ чтения реального ~/.codex/auth.json. Затем реализовать в `probe_wham_usage.py` чистое ядро до зелёных тестов.

    Модуль probe_wham_usage.py: `from __future__ import annotations`; только stdlib (json, os, sys, base64, argparse, urllib.request, urllib.error, datetime, pathlib). Константы (UPPER_CASE, конвенция репо): USAGE_URL = "https://chatgpt.com/backend-api/wham/usage", FIVE_HOUR_SECONDS = 18000, WEEKLY_SECONDS = 604800, WINDOW_TOLERANCE_SECONDS = 1200, REQUEST_TIMEOUT_SECONDS = 15, REDACT_KEYS (frozenset: user_id, account_id, email, chatgpt_user_id, session_id, individual_limit) + правило «любой ключ, содержащий подстроку token, регистронезависимо». Класс ошибок: `class ProbeError(RuntimeError)` — русское сообщение = единственный вывод при сбое.

    Функции ядра (сигнатуры фиксированы — их импортирует тест и план 01-02):
    - `auth_json_path() -> Path` — Path(os.environ.get("CODEX_HOME", Path.home() / ".codex")) / "auth.json" (уважать CODEX_HOME per RESEARCH Token Contract)
    - `load_tokens(path: Path) -> tuple[str, str | None]` — по образцу _load_token из claude_balance_widget.py:240-256: FileNotFoundError -> ProbeError «Не найден файл auth.json (<путь>). Установите/запустите Codex CLI и выполните codex login.»; OSError/JSONDecodeError -> ProbeError про нечитаемый файл; tokens отсутствует/null (auth_mode=apikey, Pitfall 6) -> ProbeError «auth.json без ChatGPT-токенов (auth_mode=apikey). Выполните codex login через ChatGPT-аккаунт.»; пустой/не-строка access_token -> ProbeError. Возвращает (tokens.access_token, tokens.account_id или None). Файл открывать только на чтение; refresh_token не читать в переменные и нигде не использовать (анти-паттерн «Refresh токена из пробы»)
    - `jwt_exp(token: str) -> datetime | None` — сниппет из RESEARCH Code Examples: base64.urlsafe_b64decode payload с паддингом, claims["exp"] -> datetime.fromtimestamp; любая ошибка -> None. Подпись НЕ верифицировать
    - `collect_windows(payload: dict) -> list[dict]` и `pick_window(windows, target_seconds, tol=WINDOW_TOLERANCE_SECONDS) -> dict | None` — сниппет «Классификация окон» из RESEARCH дословно (defensive `.get() or {}` на каждом уровне, Pitfall 3)
    - `extract_fields(payload: dict) -> dict` — маппинг в термины Balance (codex_balance_widget_chrome.py:194-204): ключи five_hour_percent / weekly_percent / credits / five_hour_reset_text / weekly_reset_text (значения str | None, процент остатка = str(100 - used_percent)), credits из (payload.get("credits") or {}).get("balance") с учётом has_credits/unlimited, reset-тексты через datetime.fromtimestamp(reset_at) БЕЗ множителей (Pitfall 4, секунды epoch) в формате "%Y-%m-%d %H:%M"; плюс служебные ключи windows (все найденные окна с их limit_window_seconds и limit_name) и missing (список отсутствующих полей — отсутствующее 5h-окно это валидный результат, Pitfall 1, не ошибка)
    - `redact(obj)` — рекурсивный обход dict/list, значения ключей из REDACT_KEYS и ключей с подстрокой "token" заменять строкой "<redacted>", возвращать новый объект (не мутировать вход)
    - `redaction_clean(text: str) -> bool` — True, если в тексте нет "eyJ" и нет "@" (пост-проверка per RESEARCH Pattern 2)

    Стиль: snake_case, type hints на публичных функциях, 4 пробела — конвенции из .planning/codebase/CONVENTIONS.md.
  </action>
  <verify>
    <automated>cd /d/00_Projects/codex_balance_widget && py -3 -m unittest test_probe_wham_usage -v</automated>
  </verify>
  <acceptance_criteria>
    - `py -3 -m unittest test_probe_wham_usage -v` завершает exit 0, минимум 12 тестов, 0 failures/errors
    - test_probe_wham_usage.py не содержит urllib.request.urlopen и не читает Path.home() (grep по файлу: нет "urlopen", нет "Path.home")
    - probe_wham_usage.py: grep находит "CODEX_HOME", "limit_window_seconds", "backend-api/wham/usage", "class ProbeError"; не находит "requests" (импорт третьесторонних пакетов отсутствует)
    - extract_fields на фикстуре живого ответа возвращает weekly_percent == "84" (100-16) — assert в тесте
    - redact не мутирует входной dict — assert в тесте (исходный dict после вызова всё ещё содержит email)
  </acceptance_criteria>
  <done>Оба новых файла существуют; unit-тесты зелёные без сети; классификация окон, редакция и разбор auth.json покрыты тестами на все ветки из behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HTTP-слой, запись фикстуры и CLI-диагностика</name>
  <files>probe_wham_usage.py, test_probe_wham_usage.py</files>
  <read_first>
    - probe_wham_usage.py — текущее состояние после Task 1 (сигнатуры ядра)
    - .planning/phases/01-json-endpoint-probe/01-RESEARCH.md — разделы «Endpoint Contract» (заголовки запроса, «Семантика ошибок» — таблица 401/403-JSON/403-HTML/429), «Pitfall 5» (Cloudflare HTML), «Pitfall 7» (stdout UTF-8)
    - d:/00_Projects/claude_balance_widget_v1/claude_balance_widget.py — строки 258-295 (fetch: маппинг HTTPError/URLError/TimeoutError/JSONDecodeError в русские RuntimeError) — копировать структуру, заменив тексты и заголовки
  </read_first>
  <behavior>
    - write_fixture: payload с email -> файл создан, в файле "<redacted>", нет "@" и нет "eyJ"; payload, где redact по какой-то причине не вычистил "eyJ"-строку (симуляция: значение с "eyJ" под ключом вне denylist), -> ProbeError, файл НЕ записан
    - build_headers: с account_id -> есть ключ "ChatGPT-Account-Id"; без account_id -> ключа нет; всегда есть "Authorization" со схемой "Bearer " и "Accept: application/json"
  </behavior>
  <action>
    Дописать в test_probe_wham_usage.py класс TestFixtureAndHeaders с тестами из behavior (tempfile для файлов). Затем дописать в probe_wham_usage.py HTTP-слой и CLI:

    - `build_headers(access_token: str, account_id: str | None) -> dict[str, str]` — Authorization: "Bearer <token>", Accept: "application/json", User-Agent: "probe-wham-usage/0.1"; ChatGPT-Account-Id добавлять только при непустом account_id (per RESEARCH Endpoint Contract, слать всегда когда есть)
    - `fetch_usage(access_token: str, account_id: str | None, timeout: float) -> tuple[int, dict]` — urllib.request.Request(USAGE_URL, headers=build_headers(...)), urlopen с timeout; по образцу claude_balance_widget.py:258-295 маппировать в ProbeError: 401 -> «Токен Codex истёк или недействителен (HTTP 401). Запустите любую команду codex — CLI обновит auth.json — и повторите.»; 403 с Content-Type text/html (exc.headers.get("Content-Type"), тело exc.read()) -> «Cloudflare-заслон (HTTP 403, HTML). Повторите позже.» + первые 200 символов тела БЕЗ заголовков запроса; 403 с JSON -> подсказка про ChatGPT-Account-Id/доступность эндпоинта; 429 -> «Слишком много запросов (HTTP 429). Повторите позже.»; прочие HTTPError -> «wham/usage вернул HTTP <код>.»; URLError -> «Нет соединения с chatgpt.com: <reason>»; TimeoutError -> «chatgpt.com не ответил за <timeout> с.»; не-JSON тело при 200 -> «Ответ не является JSON (Content-Type: <ct>).». Токен и заголовки запроса НЕ включать ни в одно сообщение (T-1-01)
    - `write_fixture(payload: dict, path: Path) -> None` — redact(payload) -> json.dumps(ensure_ascii=False, indent=2, sort_keys=True) -> если not redaction_clean(текст) -> ProbeError «Редакция фикстуры не прошла пост-проверку (eyJ/@) — файл не записан.» и НЕ создавать файл; иначе записать UTF-8
    - `main(argv: list[str] | None = None) -> int` — argparse: --fixture PATH (default "wham_usage_fixture.json"), --no-fixture (не писать файл), --timeout FLOAT (default REQUEST_TIMEOUT_SECONDS), --debug (полный traceback при ошибке). Последовательность: sys.stdout.reconfigure(encoding="utf-8", errors="replace") (Pitfall 7); load_tokens(auth_json_path()); jwt_exp(access_token) — если exp в прошлом, напечатать предупреждение «JWT истёк <дата> — запрос всё равно будет выполнен» (сам токен не печатать); fetch_usage; напечатать «HTTP status: <код>»; напечатать pretty JSON РЕДАКТИРОВАННОГО payload (json.dumps(redact(payload), ensure_ascii=False, indent=2) — сырой payload с email/id в stdout не выводить, T-1-01/T-1-02); напечатать блок «Extracted fields (Balance)»: five_hour_percent / weekly_percent / credits / five_hour_reset_text / weekly_reset_text со значением либо «отсутствует в ответе», плюс строку на каждое найденное окно вида «window <limit_window_seconds>s: used <used_percent>%, reset <локальное время>» (печатать ВСЕ окна — Open Question 1 из RESEARCH); если не --no-fixture — write_fixture и напечатать путь фикстуры; return 0. Обработка ошибок уровня main: except ProbeError -> print(str(exc)), при --debug дополнительно traceback.print_exc(), return 1. Точка входа: if __name__ == "__main__": sys.exit(main())
  </action>
  <verify>
    <automated>cd /d/00_Projects/codex_balance_widget && py -3 -m unittest test_probe_wham_usage -v && tmp=$(mktemp -d) && CODEX_HOME="$tmp" py -3 probe_wham_usage.py --no-fixture > "$tmp/out.txt" 2>&1; rc=$?; grep -q "auth.json" "$tmp/out.txt" && ! grep -q "Traceback" "$tmp/out.txt" && test $rc -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `py -3 -m unittest test_probe_wham_usage -v` — exit 0, включая новые тесты write_fixture/build_headers
    - Запуск с CODEX_HOME=<пустая папка> завершает exit 1; вывод содержит "auth.json" и НЕ содержит "Traceback" (PROBE-04, ветка «нет файла»)
    - grep по probe_wham_usage.py: присутствуют "--fixture", "--no-fixture", "--timeout", "--debug", "ensure_ascii=False", "reconfigure"; строка "Bearer" встречается только в build_headers (не в print/сообщениях об ошибках)
    - grep по probe_wham_usage.py: отсутствует печать access_token — нет f-строк, интерполирующих переменную token/access_token в print (проверка: `grep -n "print" probe_wham_usage.py | grep -i token` пусто)
    - write_fixture при непройденной пост-проверке не оставляет файла — assert в тесте (path.exists() is False)
  </acceptance_criteria>
  <done>Проба полностью собрана: одна команда `py -3 probe_wham_usage.py` готова к живому запуску (план 01-02); все ветки ошибок дают одну русскую строку + exit 1; юнит-тесты зелёные.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| скрипт → chatgpt.com/backend-api | наружу уходит Bearer-токен; внутрь приходит недоверенный JSON/HTML |
| скрипт → stdout/фикстура | ответ содержит PII (email, user_id, account_id) и потенциально токеноподобные строки |
| скрипт → ~/.codex/auth.json | файл сессии Codex CLI — чтение только, порча = разлогин CLI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Information Disclosure | main/fetch_usage: stdout, сообщения об ошибках, --debug | mitigate | токен живёт только в build_headers; в print/ошибки/traceback заголовки не попадают; pretty JSON печатается только после redact(); acceptance-grep «print…token пусто» |
| T-1-02 | Information Disclosure | write_fixture: PII в фикстуре | mitigate | redact() по denylist (email, user_id, account_id, *token*) + пост-проверка redaction_clean (нет "eyJ"/"@") с отказом записи; unit-тест на отказ |
| T-1-03 | Denial of Service | load_tokens: сессия Codex CLI | mitigate | auth.json только read (read_text); refresh_token не используется; OAuth-refresh не реализуется (одноразовые refresh-токены, анти-паттерн из RESEARCH) |
| T-1-04 | Tampering | fetch_usage: недоверенный ответ (HTML/битый JSON) | mitigate | проверка Content-Type до json.loads; defensive `.get() or {}` на nullable-каскадах; неизвестные ключи игнорируются |
| T-1-SC | Tampering | npm/pip/cargo installs | accept | установок нет — stdlib-only (Package Legitimacy Audit в RESEARCH: не применим) |
</threat_model>

<verification>
- `py -3 -m unittest test_probe_wham_usage -v` — exit 0 (полный unit-набор, без сети)
- `CODEX_HOME=<пустая папка> py -3 probe_wham_usage.py --no-fixture` — exit 1, русская диагностика, без traceback
- `git status --porcelain codex_balance_widget_chrome.py codex_balance_widget_launcher.pyw install.bat run.bat run_hidden.vbs requirements.txt` — пустой вывод ([LOCKED]: рабочий код не тронут)
</verification>

<success_criteria>
- probe_wham_usage.py и test_probe_wham_usage.py существуют в корне репо; других изменений нет
- Unit-тесты покрывают: классификацию окон (оба/только weekly/null/additional), redact + пост-проверку, jwt_exp, разбор auth.json (нет файла / apikey / валидный), write_fixture (успех/отказ), build_headers
- Скрипт готов к живому запуску: `py -3 probe_wham_usage.py` печатает статус, редактированный pretty JSON, блок Extracted fields и пишет фикстуру
</success_criteria>

<output>
Create `.planning/phases/01-json-endpoint-probe/01-01-SUMMARY.md` when done
</output>
