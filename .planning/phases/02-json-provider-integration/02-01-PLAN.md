---
phase: 02-json-provider-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - probe_wham_usage.py
  - test_probe_wham_usage.py
  - json_usage_provider.py
  - test_json_usage_provider.py
autonomous: true
requirements: [JSONPROV-02]
must_haves:
  truths:
    - "py -3 -m unittest test_probe_wham_usage -v проходит без сети, включая новые тесты retryable-классификации HTTP-ошибок (exit 0)"
    - "py -3 -m unittest test_json_usage_provider -v проходит без сети (exit 0)"
    - "ProbeError, поднятый fetch_usage при HTTP 429 / urllib.error.URLError / TimeoutError, имеет retryable=True; при HTTP 401/403/прочих кодах и невалидном JSON — retryable=False (по умолчанию)"
    - "JsonUsageProvider.fetch() делает ровно один повторный запрос при retryable-ошибке (D-03) и ни одного повтора при 401/403 (D-02), прежде чем вернуть JsonFetchResult(status='error')"
  artifacts:
    - path: json_usage_provider.py
      provides: "JsonUsageProvider (async fetch), JsonFetchResult — тонкая обёртка над probe_wham_usage с ретраем по D-02/D-03, без Tk/UI-зависимостей"
      min_lines: 40
    - path: test_json_usage_provider.py
      provides: "unittest.IsolatedAsyncioTestCase набор на JsonUsageProvider.fetch, без сети, без реального auth.json"
      contains: "IsolatedAsyncioTestCase"
    - path: probe_wham_usage.py
      provides: "ProbeError.retryable — обратно совместимое расширение (существующие 23 теста Phase 1 продолжают проходить)"
      contains: "retryable"
  key_links:
    - from: json_usage_provider.py
      to: probe_wham_usage.py
      via: "import probe_wham_usage; вызовы load_tokens/auth_json_path/fetch_usage/extract_fields"
      pattern: "import probe_wham_usage"
    - from: "json_usage_provider.JsonUsageProvider._fetch_with_retry"
      to: "probe_wham_usage.ProbeError.retryable"
      via: "ветвление retry-once по exc.retryable"
      pattern: "\\.retryable"
---

<objective>
Расширить `probe_wham_usage.py` (Phase 1) флагом `ProbeError.retryable` и построить новый модуль `json_usage_provider.py` — async-обёртку с логикой "один ретрай на транзиентных ошибках, иначе сразу отдать ошибку вызывающей стороне" (D-02/D-03 из 02-CONTEXT.md).

Purpose: дать `codex_balance_widget_chrome.py` (Plan 02-03) готовый, протестированный, async-совместимый источник JSON-данных usage, не трогая сам widget-файл в этом плане и не переписывая уже проверенную логику HTTP-классификации ошибок из `probe_wham_usage.py`.

Output: `probe_wham_usage.py` с полем `retryable` на `ProbeError` (отмечены 429/URLError/TimeoutError); `json_usage_provider.py` с `JsonUsageProvider.fetch()` (async, retry-once) и `JsonFetchResult`; тесты на оба файла.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-json-provider-integration/02-CONTEXT.md
@.planning/phases/02-json-provider-integration/02-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ProbeError.retryable в probe_wham_usage.py</name>
  <files>probe_wham_usage.py, test_probe_wham_usage.py</files>
  <read_first>
    - D:/00_Projects/codex_balance_widget/probe_wham_usage.py — строки 51-52 (класс `ProbeError(RuntimeError)`) и строки 260-321 (`fetch_usage`, все раздельные `raise ProbeError(...)` на 401/403-html/403-json/429/прочие HTTPError/URLError/TimeoutError/не-JSON content-type/JSONDecodeError). READ-ONLY смысл: не менять текст русских сообщений и не менять сигнатуры существующих функций — только добавить keyword-only `retryable`.
    - D:/00_Projects/codex_balance_widget/test_probe_wham_usage.py — существующие 23 теста (классы TestWindows/TestExtract/TestRedact/TestJwt/TestLoadTokens/TestFixtureAndHeaders) — конвенция: stdlib unittest, `unittest.mock` для сети, без реального auth.json, tempfile для файлов.
    - D:/00_Projects/codex_balance_widget/.planning/phases/02-json-provider-integration/02-CONTEXT.md — секции D-02 (401/403 → сразу fallback, без ретраев) и D-03 (429/сетевая ошибка/таймаут → один быстрый повтор)
  </read_first>
  <behavior>
    - ProbeError("msg") без retryable -> `exc.retryable is False` (обратная совместимость с существующими 23 тестами, которые вызывают `ProbeError(text)` без kwargs)
    - ProbeError("msg", retryable=True) -> `exc.retryable is True`, `str(exc) == "msg"`
    - fetch_usage при мок-urlopen, кидающем HTTPError(code=401) -> ProbeError с retryable=False, текст содержит "401"
    - fetch_usage при HTTPError(code=403, Content-Type="text/html") -> ProbeError retryable=False, текст содержит "Cloudflare"
    - fetch_usage при HTTPError(code=403, Content-Type="application/json") -> ProbeError retryable=False, текст содержит "403"
    - fetch_usage при HTTPError(code=429) -> ProbeError retryable=True, текст содержит "429"
    - fetch_usage при HTTPError(code=500) -> ProbeError retryable=False (D-02/D-03 называют ретраябельными только 429/URLError/timeout — прочие HTTP-коды по умолчанию НЕ ретраятся)
    - fetch_usage при urllib.error.URLError("no route") -> ProbeError retryable=True, текст содержит "chatgpt.com"
    - fetch_usage при TimeoutError() -> ProbeError retryable=True
    - fetch_usage при успешном ответе (status=200, Content-Type="application/json", валидный JSON-body) -> возвращает (200, dict), исключение не поднимается
  </behavior>
  <action>
    В `probe_wham_usage.py` изменить класс `ProbeError(RuntimeError)` (строки 51-52): добавить `def __init__(self, message: str, *, retryable: bool = False) -> None`, внутри `super().__init__(message)` и `self.retryable = retryable`. Не менять docstring по смыслу (можно расширить одной строкой про `retryable`).

    В `fetch_usage` (строки 260-321) добавить `retryable=True` ТОЛЬКО на трёх raise-сайтах: `exc.code == 429` (строка ~304), `except urllib.error.URLError as exc` (строка ~307), `except TimeoutError as exc` (строка ~309). Все остальные `raise ProbeError(...)` в этой функции (401, 403-html, 403-json, прочие HTTPError-коды, не-JSON content-type, JSONDecodeError, невалидный формат payload) оставить без `retryable` — используется default `False`. Текст ни одного русского сообщения не менять.

    В `test_probe_wham_usage.py` дописать класс `TestProbeErrorRetryable(unittest.TestCase)` с двумя тестами из первого пункта behavior (ProbeError без/с retryable), и класс `TestFetchUsageErrorClassification(unittest.TestCase)` с одним тестом на каждый оставшийся пункт behavior — мокать `probe_wham_usage.urllib.request.urlopen` через `unittest.mock.patch`, для HTTPError-кейсов конструировать `urllib.error.HTTPError(url, code, msg, hdrs, fp)` где `hdrs` — `email.message.Message()` (с `add_header("Content-Type", ...)` для 403-кейсов), `fp` — `io.BytesIO(body_bytes)`; для успешного кейса мокать `urlopen(...)` так, чтобы `with urlopen(...) as response:` отдавал объект с `.status`, `.headers.get("Content-Type")` и `.read()`, возвращающий байты `json.dumps({...}).encode("utf-8")`. Добавить `import io`, `import urllib.error`, `from email.message import Message`, `from unittest.mock import patch` в начало файла, если их ещё нет.
  </action>
  <verify>
    <automated>cd /d/00_Projects/codex_balance_widget && py -3 -m unittest test_probe_wham_usage -v</automated>
  </verify>
  <acceptance_criteria>
    - `py -3 -m unittest test_probe_wham_usage -v` — exit 0, не менее 31 теста (23 существующих + минимум 8 новых), 0 failures/errors
    - grep по `probe_wham_usage.py`: `"retryable=True"` встречается ровно 3 раза (429, URLError, TimeoutError) — `grep -c "retryable=True" probe_wham_usage.py` равен 3
    - Существующие тесты (TestWindows/TestExtract/TestRedact/TestJwt/TestLoadTokens/TestFixtureAndHeaders) не изменены по количеству assert'ов и продолжают проходить — регресса нет
  </acceptance_criteria>
  <done>`ProbeError.retryable` добавлен без изменения публичных сигнатур и без изменения русских сообщений; ретраябельные классы ошибок (429/URLError/timeout) размечены и покрыты тестами; все остальные классы явно НЕ ретраябельны и тоже покрыты тестами.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: json_usage_provider.py — async-обёртка с retry-once</name>
  <files>json_usage_provider.py, test_json_usage_provider.py</files>
  <read_first>
    - D:/00_Projects/codex_balance_widget/probe_wham_usage.py — публичные сигнатуры после Task 1: `auth_json_path() -> Path`, `load_tokens(path) -> tuple[str, str|None]`, `fetch_usage(access_token, account_id, timeout) -> tuple[int, dict]`, `extract_fields(payload) -> dict`, `ProbeError.retryable`
    - D:/00_Projects/codex_balance_widget/.planning/phases/02-json-provider-integration/02-PATTERNS.md — секция "Async-wrapping of a blocking urllib call" (Gap — нет `asyncio.to_thread` нигде в репо, это первое применение) и структурный аналог `CodexUsageBrowser.fetch()` (codex_balance_widget_chrome.py:844-856, паттерн "try primary, log, fall through")
    - D:/00_Projects/codex_balance_widget/.planning/phases/02-json-provider-integration/02-CONTEXT.md — D-02, D-03, D-04 (виджет НЕ ждёт и не инициирует refresh токена — только одна попытка + один ретрай, никакого поллинга свежести токена)
  </read_first>
  <behavior>
    - `load_tokens` кидает ProbeError -> `fetch()` возвращает `JsonFetchResult(status="error", error=<текст>)`, `fetch_usage` НЕ вызван ни разу (mock.call_count == 0)
    - `fetch_usage` кидает ProbeError(retryable=False) -> `fetch()` возвращает `status="error"`, `fetch_usage` вызван ровно 1 раз, `retried is False`
    - `fetch_usage` первый раз кидает ProbeError(retryable=True), второй раз возвращает `(200, payload)` -> `fetch()` возвращает `status="ok"`, `fields` соответствует `extract_fields(payload)`, `retried is True`, `fetch_usage` вызван ровно 2 раза
    - `fetch_usage` оба раза кидает ProbeError(retryable=True) -> `fetch()` возвращает `status="error"`, `retried is True`, `fetch_usage` вызван ровно 2 раза (не 3 и не 1)
    - `fetch_usage` сразу возвращает `(200, payload)` -> `fetch()` возвращает `status="ok"`, `retried is False`, `fetch_usage` вызван 1 раз
  </behavior>
  <action>
    Создать `json_usage_provider.py` (`from __future__ import annotations`, только stdlib `asyncio` + `dataclasses` + `import probe_wham_usage`). Константа `RETRY_DELAY_SECONDS = 1.0`.

    Датакласс `JsonFetchResult`: поля `status: str` ("ok"|"error"), `fields: dict | None = None`, `error: str | None = None`, `retried: bool = False`.

    Класс `JsonUsageProvider`: `__init__(self, timeout: float = probe_wham_usage.REQUEST_TIMEOUT_SECONDS, retry_delay: float = RETRY_DELAY_SECONDS) -> None` сохраняет `self.timeout`, `self.retry_delay`.

    `async def fetch(self) -> JsonFetchResult`: оборачивает `probe_wham_usage.load_tokens(probe_wham_usage.auth_json_path())` через `await asyncio.to_thread(...)`; при `probe_wham_usage.ProbeError` — вернуть `JsonFetchResult("error", error=str(exc))` немедленно (никакого ретрая на этом шаге — не входит в D-03). При успехе вызвать `self._fetch_with_retry(access_token, account_id)`.

    `async def _fetch_with_retry(self, access_token, account_id) -> JsonFetchResult`: первая попытка через `self._fetch_payload(...)`; при успехе — `JsonFetchResult("ok", fields=probe_wham_usage.extract_fields(payload))`. При `probe_wham_usage.ProbeError` — если `not exc.retryable`, вернуть `JsonFetchResult("error", error=str(exc))` без ретрая (D-02: 401/403 сразу fallback). Если `exc.retryable` — `await asyncio.sleep(self.retry_delay)`, затем ровно одна повторная попытка через `self._fetch_payload(...)`; при успехе — `JsonFetchResult("ok", fields=probe_wham_usage.extract_fields(payload), retried=True)`; при повторном `ProbeError` (любом, включая нового исключения) — `JsonFetchResult("error", error=str(exc2), retried=True)`. Второго ретрая нет ни при каких условиях (D-03: "один быстрый повтор").

    `async def _fetch_payload(self, access_token, account_id) -> dict`: `_, payload = await asyncio.to_thread(probe_wham_usage.fetch_usage, access_token, account_id, self.timeout)`; вернуть `payload`.

    Создать `test_json_usage_provider.py` (stdlib `unittest.IsolatedAsyncioTestCase`, `unittest.mock.patch` на `"probe_wham_usage.load_tokens"` и `"probe_wham_usage.fetch_usage"`, без реального asyncio.sleep-ожидания — конструировать `JsonUsageProvider(retry_delay=0)` в каждом тесте) с одним тестовым методом на каждый пункт behavior. Для payload-фикстуры использовать тот же формат, что и `TestExtract.test_extract_fields_live_fixture` в `test_probe_wham_usage.py` (weekly-only rate_limit, used_percent=16) — переиспользовать значения, чтобы `fields["weekly_percent"] == "84"` было проверяемым assert'ом на успешный кейс.
  </action>
  <verify>
    <automated>cd /d/00_Projects/codex_balance_widget && py -3 -m unittest test_probe_wham_usage test_json_usage_provider -v</automated>
  </verify>
  <acceptance_criteria>
    - `py -3 -m unittest test_probe_wham_usage test_json_usage_provider -v` — exit 0, 0 failures/errors, минимум 5 новых тестов в `test_json_usage_provider.py`
    - `json_usage_provider.py` не импортирует ничего из `codex_balance_widget_chrome.py` (grep: нет `codex_balance_widget_chrome`) — модуль независим от Tk/widget-кода
    - grep по `json_usage_provider.py`: присутствуют `"asyncio.to_thread"`, `"class JsonUsageProvider"`, `"class JsonFetchResult"`, `".retryable"`
    - Тест на "оба раза retryable-ошибка" проверяет `fetch_usage`/мок вызван ровно 2 раза (не 1, не 3) — assert на `call_count`
  </acceptance_criteria>
  <done>`json_usage_provider.py` существует, async, без Tk-зависимостей, реализует ровно один ретрай на транзиентных ошибках и немедленный fallback-сигнал на 401/403; полностью покрыт unit-тестами без сети.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| json_usage_provider.py → probe_wham_usage.fetch_usage → chatgpt.com/backend-api | наружу уходит Bearer-токен; внутрь приходит недоверенный JSON (тот же контур, что и в Phase 1) |
| json_usage_provider.py → probe_wham_usage.load_tokens → ~/.codex/auth.json | файл сессии Codex CLI — только чтение, без refresh_token (D-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-01 | Denial of Service | JsonUsageProvider._fetch_with_retry — неограниченный ретрай при постоянных 429 | mitigate | ровно один ретрай, потом немедленный `status="error"` и fallback на Chrome — никакого цикла/backoff-роста |
| T-2-02 | Information Disclosure | JsonFetchResult.error — текст ошибки, попадающий в лог виджета (Plan 02-03) | accept | `error=str(exc)` — те же готовые русские сообщения из `probe_wham_usage.ProbeError` (Phase 1 уже гарантирует отсутствие токена в тексте, T-1-01) |
| T-2-03 | Elevation of Privilege | load_tokens/refresh_token | mitigate | json_usage_provider.py не читает и не использует `refresh_token`, не инициирует refresh (D-04) — использует только уже протестированный `probe_wham_usage.load_tokens` |
| T-2-SC | Tampering | npm/pip/cargo installs | accept | новых зависимостей не добавлено — json_usage_provider.py и его тесты используют только stdlib (`asyncio`, `dataclasses`, `unittest`) |
</threat_model>

<verification>
- `py -3 -m unittest test_probe_wham_usage test_json_usage_provider -v` — exit 0, весь набор (Phase 1 + новые тесты Phase 2) без сети
- `grep -c "retryable=True" probe_wham_usage.py` == 3
- `git status --porcelain codex_balance_widget_chrome.py` — пустой вывод (widget-файл в этом плане не тронут — трогается только в Plan 02-03)
</verification>

<success_criteria>
- `probe_wham_usage.py` расширен полем `ProbeError.retryable` без изменения текста сообщений и без регресса 23 тестов Phase 1
- `json_usage_provider.py` существует, async, реализует D-02 (401/403 → без ретрая) и D-03 (429/сеть/таймаут → один ретрай) поверх уже проверенной HTTP-классификации из `probe_wham_usage.fetch_usage`
- Оба новых файла полностью покрыты unit-тестами без сети и без реального `~/.codex/auth.json`
</success_criteria>

<output>
Create `.planning/phases/02-json-provider-integration/02-01-SUMMARY.md` when done
</output>
