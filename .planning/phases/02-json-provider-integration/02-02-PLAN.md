---
phase: 02-json-provider-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - codex_balance_widget_chrome.py
  - test_codex_balance_widget_chrome.py
autonomous: true
requirements: [JSONPROV-01, JSONPROV-03]
must_haves:
  truths:
    - "parse_reset_datetime корректно разбирает ISO-строку '%Y-%m-%d %H:%M' (формат probe_wham_usage._reset_text) в абсолютную дату, а не в 'ближайшее время сегодня/завтра'"
    - "build_balance_from_json_fields строит Balance с теми же именами полей, что и BalanceParser.parse, из словаря extract_fields()"
    - "plan_fetch_outcome для успешного JSON-результата возвращает source='json', заполненный Balance и log_line с подстрокой 'source: json'"
    - "plan_fetch_outcome при неудачном JSON + успешном Chrome-фолбэке добавляет пометку 'Chrome-фолбэк'/'Chrome fallback' к статусному сообщению и log_line с подстрокой 'source: chrome'"
    - "plan_fetch_outcome сохраняет существующее поведение 'показать последние данные при сбое обновления' (has_existing_data) для обеих веток — JSON и Chrome"
  artifacts:
    - path: codex_balance_widget_chrome.py
      provides: "ISO-ветка parse_reset_datetime, build_balance_from_json_fields, FetchOutcome, plan_fetch_outcome — чистые функции без Tk/asyncio, готовые для Plan 02-03"
      contains: "plan_fetch_outcome"
    - path: test_codex_balance_widget_chrome.py
      provides: "unittest-набор на новые чистые функции виджета, без Tkinter, без Chrome"
      contains: "class Test"
  key_links:
    - from: "codex_balance_widget_chrome.build_balance_from_json_fields"
      to: "codex_balance_widget_chrome.Balance"
      via: "прямой маппинг ключей extract_fields() на поля Balance"
      pattern: "build_balance_from_json_fields"
    - from: "codex_balance_widget_chrome.plan_fetch_outcome"
      to: "codex_balance_widget_chrome.BalanceParser.parse"
      via: "вызов при chrome_status == 'ok' и наличии chrome_text"
      pattern: "BalanceParser\\.parse"
---

<objective>
Добавить в `codex_balance_widget_chrome.py` три чистые, Tk-независимые функции — фундамент для врезки JSON-провайдера в `fetch_once` (Plan 02-03): (1) ISO-ветку в `parse_reset_datetime` для reset-дат из `probe_wham_usage.extract_fields`, (2) `build_balance_from_json_fields` — маппинг JSON-полей на `Balance`, (3) `plan_fetch_outcome`/`FetchOutcome` — чистую decision-функцию "что показать пользователю и что записать в лог", покрывающую JSON-успех, JSON-провал+Chrome-успех, JSON-провал+Chrome-провал, сохраняющую все существующие статусные сообщения `fetch_once`.

Purpose: `fetch_once` в Plan 02-03 станет тонкой async-обвязкой вокруг уже протестированной чистой логики — по конвенции репозитория ("Prefer pure unit tests for helpers... Avoid launching Tkinter... test through extracted helpers", `.planning/codebase/TESTING.md`) и без риска регресса существующих статусных сообщений Chrome-пути.

Output: `codex_balance_widget_chrome.py` с новыми функциями (без изменения `fetch_once`/`__init__` — это Plan 02-03); `test_codex_balance_widget_chrome.py` — первый unit-тест на этот файл в репозитории.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-json-provider-integration/02-CONTEXT.md
@.planning/phases/02-json-provider-integration/02-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ISO reset-дата + build_balance_from_json_fields</name>
  <files>codex_balance_widget_chrome.py, test_codex_balance_widget_chrome.py</files>
  <read_first>
    - D:/00_Projects/codex_balance_widget/codex_balance_widget_chrome.py — строки 194-205 (`Balance`), 331-336 (`normalize_reset_text`), 338-436 (`parse_reset_datetime` целиком — порядок веток: ru_match, en_match, relative_match, затем финальный time-only fallback на строке ~420, который сейчас ОШИБОЧНО матчит ISO-строки вида "2026-07-27 18:33" как "ближайшее время 18:33 сегодня/завтра", теряя дату), 559-565 (`BalanceParser.parse` — точная форма конструктора `Balance(...)`, референс для `build_balance_from_json_fields`)
    - D:/00_Projects/codex_balance_widget/probe_wham_usage.py — строки 157-166 (`_reset_text` — формат `"%Y-%m-%d %H:%M"` через `strftime`, это и есть источник ISO-строк) и строки 191-220 (`extract_fields` — точные имена ключей: five_hour_percent, weekly_percent, credits, five_hour_reset_text, weekly_reset_text)
    - D:/00_Projects/codex_balance_widget/.planning/phases/02-json-provider-integration/02-PATTERNS.md — секция "JSON→Balance glue" (объясняет несовпадение формата reset-текста между BalanceParser и extract_fields — эта задача его устраняет)
  </read_first>
  <behavior>
    - `parse_reset_datetime("2026-07-27 18:33")` -> `datetime(2026, 7, 27, 18, 33)` (не "ближайшее 18:33 сегодня/завтра" — регрессия, которую чинит эта задача)
    - `parse_reset_datetime("2026-01-01 00:05")` -> `datetime(2026, 1, 1, 0, 5)`
    - Существующие форматы не ломаются: `parse_reset_datetime("Reset Jun 7, 2026 18:33")` -> `datetime(2026, 6, 7, 18, 33)` (как в `.planning/codebase/TESTING.md` примере); `parse_reset_datetime("Сброс 20:48")` -> не None (ветка time-only по-прежнему работает для НЕ-ISO строк)
    - `parse_reset_datetime(None)` -> `None`; `parse_reset_datetime("мусор без даты")` -> `None`
    - `build_balance_from_json_fields({"five_hour_percent": None, "weekly_percent": "84", "credits": "0", "five_hour_reset_text": None, "weekly_reset_text": "2026-07-27 18:33", "windows": [], "missing": ["five_hour"]})` -> `Balance(five_hour_percent=None, weekly_percent="84", credits="0", five_hour_reset_text=None, weekly_reset_text="2026-07-27 18:33")`
    - `build_balance_from_json_fields` на словаре без лишних ключей типа "windows"/"missing" не падает (`.get()`, не прямой доступ по ключу)
  </behavior>
  <action>
    В `parse_reset_datetime` (строки 338-436) добавить новую ветку СРАЗУ ПОСЛЕ блока `en_match` (после его `return None` на строке ~395) и ПЕРЕД комментарием `# Relative English date` (строка ~397): `iso_match = re.search(r"(?P<year>\d{4})-(?P<month>\d{2})-(?P<day>\d{2})\s+(?P<hour>\d{1,2}):(?P<minute>\d{2})", lower)`; если найдено — вернуть `datetime(int(year), int(month), int(day), int(hour), int(minute))` внутри `try/except ValueError: return None`, по образцу уже существующих ru_match/en_match блоков (тот же стиль try/except). Порядок важен: ветка должна идти строго до финального time-only fallback (строка ~420), иначе тот перехватит "18:33" из середины ISO-строки первым и потеряет дату.

    Добавить `build_balance_from_json_fields(fields: dict) -> Balance` СРАЗУ ПОСЛЕ `format_compact_countdown` (строка 501) и ПЕРЕД `class BalanceParser:` (строка 504) — по конвенции `.planning/codebase/CONVENTIONS.md` ("Add parser and formatting helpers near the existing helper block... before BalanceParser"). Реализация: `Balance(five_hour_percent=fields.get("five_hour_percent"), weekly_percent=fields.get("weekly_percent"), credits=fields.get("credits"), five_hour_reset_text=normalize_reset_text(fields.get("five_hour_reset_text")), weekly_reset_text=normalize_reset_text(fields.get("weekly_reset_text")))` — та же форма конструктора, что и `BalanceParser.parse` (строки 559-565), с `normalize_reset_text` на обоих reset-полях для консистентности с Chrome-путём.

    Создать `test_codex_balance_widget_chrome.py` (stdlib unittest, `from codex_balance_widget_chrome import parse_reset_datetime, build_balance_from_json_fields, Balance`) с классами `TestParseResetDatetimeIso` и `TestBuildBalanceFromJsonFields`, покрывающими все пункты behavior. НЕ импортировать/не конструировать `tkinter`, `CodexBalanceWidget`, `CodexUsageBrowser` — только чистые функции и датаклассы верхнего уровня модуля (импорт модуля `codex_balance_widget_chrome` уже тянет `playwright.async_api` на уровне модуля — это нормально и не требует запуска Chrome/Tk).
  </action>
  <verify>
    <automated>cd /d/00_Projects/codex_balance_widget && py -3 -m unittest test_codex_balance_widget_chrome -v</automated>
  </verify>
  <acceptance_criteria>
    - `py -3 -m unittest test_codex_balance_widget_chrome -v` — exit 0, минимум 6 тестов, 0 failures/errors
    - `parse_reset_datetime("2026-07-27 18:33") == datetime(2026, 7, 27, 18, 33)` — assert в тесте (не "ближайшее время")
    - `parse_reset_datetime("Reset Jun 7, 2026 18:33") == datetime(2026, 6, 7, 18, 33)` — assert в тесте, подтверждает отсутствие регресса на существующем формате
    - grep по `codex_balance_widget_chrome.py`: `"def build_balance_from_json_fields"` найдено ровно 1 раз, расположена между строкой `format_compact_countdown` и `class BalanceParser` (проверяется по номеру строки: `grep -n "def build_balance_from_json_fields\|^class BalanceParser" codex_balance_widget_chrome.py` — build_balance_from_json_fields имеет меньший номер строки)
  </acceptance_criteria>
  <done>ISO reset-дата больше не искажается до "ближайшее время сегодня/завтра"; существующие форматы разбора дат не сломаны; `build_balance_from_json_fields` даёт `Balance` из словаря `extract_fields()` без text-парсинга; всё покрыто тестами.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FetchOutcome / plan_fetch_outcome</name>
  <files>codex_balance_widget_chrome.py, test_codex_balance_widget_chrome.py</files>
  <read_first>
    - D:/00_Projects/codex_balance_widget/codex_balance_widget_chrome.py — строки 2073-2119 (`fetch_once` — ТЕКУЩАЯ логика, которую эта функция описывает декларативно: порядок веток, все 6 существующих `tr(...)` строк статуса, `is_weekly_limit_exhausted`, `has_usage_data`). READ-ONLY в этой задаче — `fetch_once` не меняется, только читается как источник истины для веток.
    - D:/00_Projects/codex_balance_widget/codex_balance_widget_chrome.py — строки 110-111 (`tr`), 319-321 (`is_weekly_limit_exhausted`), 202-204 (`Balance.has_usage_data`) — уже определены до места вставки
    - D:/00_Projects/codex_balance_widget/.planning/phases/02-json-provider-integration/02-CONTEXT.md — D-05 (лог источника только на успешные обновления; пометка фолбэка в статусной строке ТОЛЬКО когда Chrome реально активен)
  </read_first>
  <behavior>
    - json_status="ok", json_fields даёт weekly_percent="0" (истощён) -> `FetchOutcome(source="json", balance=<Balance с weekly_percent="0">, status_message=tr(language,"Codex unavailable: weekly limit exhausted","Codex недоступен: недельный лимит исчерпан"), log_line содержит "source: json")`
    - json_status="ok", weekly_percent="50" (не истощён) -> `status_message == tr(language,"Data is up to date","Данные актуальны")`, `log_line` содержит "source: json"
    - json_status="error", chrome_attempted=False (self.browser is None) -> `source="none"`, `balance is None`, `status_message == tr(language,"Google Chrome not found. Set CHROME_PATH.","Google Chrome не найден. Укажите CHROME_PATH.")` (дословно как в текущем fetch_once), `log_line` НЕ содержит "source: json"/"source: chrome" (обновления не было)
    - json_status="error", chrome_attempted=True, chrome_status="ok", chrome_text даёт `has_usage_data=True` -> `source="chrome"`, `balance` заполнен, `status_message` заканчивается на `" · " + tr(language,"Chrome fallback","Chrome-фолбэк")`, `log_line` содержит "source: chrome"
    - json_status="error", chrome_attempted=True, chrome_status="ok", chrome_text даёт `has_usage_data=False`, has_existing_data=True -> `status_message == tr(language,"Showing last saved data · new data not recognized","Показаны последние данные · новые не распознаны") + " · " + tr(language,"Chrome fallback","Chrome-фолбэк")`, `balance is None`, `source="none"`
    - json_status="error", chrome_attempted=True, chrome_status="browser_error", has_existing_data=False -> `status_message == tr(language,"Chrome failed to start. See widget_launch.log","Chrome не запустился. Подробности в widget_launch.log")` БЕЗ суффикса "Chrome fallback" (сообщение уже упоминает Chrome — не дублировать пометку)
    - json_status="error", chrome_attempted=True, chrome_status="login_required" -> `status_message == tr(language,"Sign in to ChatGPT. Click Refresh to open Chrome.","Нужен вход в ChatGPT. Нажмите Обновить, чтобы открыть Chrome.")` БЕЗ суффикса
    - json_status="error", chrome_attempted=True, chrome_status="not_ready", has_existing_data=False -> `status_message == tr(language,"Usage data timed out. Click Refresh.","Не дождался данных Usage. Нажмите Обновить.") + " · " + tr(language,"Chrome fallback","Chrome-фолбэк")` (это сообщение не упоминает Chrome явно — суффикс добавляется)
    - json_status="error", chrome_attempted=True, любой chrome-провал, has_existing_data=True -> `status_message` начинается с `tr(language,"Showing last saved data · refresh failed","Показаны последние данные · обновить не удалось")` + суффикс фолбэка (has_existing_data имеет приоритет над конкретным chrome_status — как в текущем fetch_once, строки 2091-2092)
  </behavior>
  <action>
    Добавить датакласс `FetchOutcome` СРАЗУ ПОСЛЕ `build_balance_from_json_fields` (из Task 1) и ПЕРЕД `class BalanceParser:`: поля `source: str` ("json"|"chrome"|"none"), `balance: Balance | None`, `status_message: str`, `log_line: str`.

    Добавить функцию `plan_fetch_outcome` со следующей keyword-only сигнатурой: `json_status: str` ("ok"|"error"), `json_fields: dict | None`, `json_error: str | None`, `chrome_attempted: bool`, `chrome_status: str | None` (одно из "ok"/"chrome_not_found"/"browser_error"/"login_required"/"not_ready"/None), `chrome_error: str | None`, `chrome_text: str | None`, `has_existing_data: bool`, `language: str` -> `FetchOutcome`.

    Логика (порядок веток обязателен, точно воспроизводит текущий `fetch_once`):
    1. Если `json_status == "ok"`: `balance = build_balance_from_json_fields(json_fields)`; `message = tr(language,"Codex unavailable: weekly limit exhausted","Codex недоступен: недельный лимит исчерпан")` если `is_weekly_limit_exhausted(balance)`, иначе `tr(language,"Data is up to date","Данные актуальны")`; `log_line = "Balance updated (source: json)"`; вернуть `FetchOutcome("json", balance, message, log_line)`.
    2. Иначе если `not chrome_attempted`: `message = tr(language,"Google Chrome not found. Set CHROME_PATH.","Google Chrome не найден. Укажите CHROME_PATH.")`; `log_line = f"JSON fetch failed ({json_error}); Chrome unavailable"`; вернуть `FetchOutcome("none", None, message, log_line)`.
    3. Иначе (chrome_attempted=True) вычислить `suffix = f" · {tr(language, 'Chrome fallback', 'Chrome-фолбэк')}"` и `fallback_log = f"JSON fetch failed ({json_error}); Chrome fallback status={chrome_status} error={chrome_error or ''}"`:
       - Если `chrome_status == "ok"` и `chrome_text`: `balance = BalanceParser.parse(chrome_text)`; если `balance.has_usage_data` — `message = (weekly-exhausted или data-up-to-date, как в п.1, но по chrome-balance) + suffix`, `log_line = "Balance updated (source: chrome)"`, вернуть `FetchOutcome("chrome", balance, message, log_line)`; иначе если `has_existing_data` — `message = tr(language,"Showing last saved data · new data not recognized","Показаны последние данные · новые не распознаны") + suffix`, вернуть `FetchOutcome("none", None, message, fallback_log)`; иначе — `message = tr(language,"Data not recognized","Данные не распознаны") + suffix`, вернуть `FetchOutcome("none", None, message, fallback_log)`.
       - Иначе (`chrome_status != "ok"` или пустой `chrome_text`): если `has_existing_data` — `message = tr(language,"Showing last saved data · refresh failed","Показаны последние данные · обновить не удалось") + suffix`, вернуть `FetchOutcome("none", None, message, fallback_log)`; иначе если `chrome_status == "browser_error"` — `message = tr(language,"Chrome failed to start. See widget_launch.log","Chrome не запустился. Подробности в widget_launch.log")` (БЕЗ suffix), вернуть `FetchOutcome("none", None, message, fallback_log)`; иначе если `chrome_status == "login_required"` — `message = tr(language,"Sign in to ChatGPT. Click Refresh to open Chrome.","Нужен вход в ChatGPT. Нажмите Обновить, чтобы открыть Chrome.")` (БЕЗ suffix), вернуть `FetchOutcome("none", None, message, fallback_log)`; иначе — `message = tr(language,"Usage data timed out. Click Refresh.","Не дождался данных Usage. Нажмите Обновить.") + suffix`, вернуть `FetchOutcome("none", None, message, fallback_log)`.

    Дописать в `test_codex_balance_widget_chrome.py` класс `TestPlanFetchOutcome` с одним тестовым методом на каждый пункт behavior (минимум 9 методов), сравнивая `status_message` посимвольно с ожидаемой строкой и проверяя подстроки `"source: json"`/`"source: chrome"` в `log_line` через `assertIn`.
  </action>
  <verify>
    <automated>cd /d/00_Projects/codex_balance_widget && py -3 -m unittest test_codex_balance_widget_chrome -v</automated>
  </verify>
  <acceptance_criteria>
    - `py -3 -m unittest test_codex_balance_widget_chrome -v` — exit 0, минимум 15 тестов суммарно (Task 1 + Task 2), 0 failures/errors
    - grep по `codex_balance_widget_chrome.py`: `"def plan_fetch_outcome"` найдено ровно 1 раз, `"class FetchOutcome"` найдено ровно 1 раз, обе расположены до `class BalanceParser` (сравнение номеров строк через `grep -n`)
    - Тест на browser_error и тест на login_required проверяют ОТСУТСТВИЕ подстроки "Chrome fallback"/"Chrome-фолбэк" в `status_message` (`assertNotIn`) — суффикс не дублируется поверх сообщений, уже упоминающих Chrome
    - Тест на JSON-успех с истощённой неделей (weekly_percent="0") проверяет `status_message == tr(language,"Codex unavailable: weekly limit exhausted","Codex недоступен: недельный лимит исчерпан")` — совпадение с существующей строкой fetch_once дословно
  </acceptance_criteria>
  <done>`plan_fetch_outcome` — чистая функция, покрывающая все ветки текущего `fetch_once` (JSON-успех, JSON-провал без Chrome, JSON-провал+Chrome-успех/провал, keep-last-data) плюс новую пометку источника/фолбэка по D-05; готова к использованию в Plan 02-03 без дальнейших доработок логики.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plan_fetch_outcome → widget_launch.log (через log_line, записывается в Plan 02-03) | log_line строится только из уже нередактированных полей extract_fields()/ProbeError-сообщений (без PII, без токена — см. T-2-02 в Plan 02-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-04 | Tampering | parse_reset_datetime — новая ISO-ветка на недоверенном reset_text | accept | reset_text приходит из уже аутентифицированного ответа backend-api (тот же уровень доверия, что и у существующих ru/en веток); при несовпадении формата — `except ValueError: return None`, деградация, не падение |
| T-2-05 | Information Disclosure | plan_fetch_outcome.log_line на провальных ветках (json_error/chrome_error) | mitigate | `json_error`/`chrome_error` — уже отредактированные русские сообщения из `ProbeError`/`FetchResult.error` (Phase 1 гарантирует отсутствие токена, T-1-01); новых источников чувствительных данных не добавляется |
| T-2-SC | Tampering | npm/pip/cargo installs | accept | новых зависимостей не добавлено — только stdlib `unittest`/`re`/`dataclasses` |
</threat_model>

<verification>
- `py -3 -m unittest test_codex_balance_widget_chrome -v` — exit 0, весь новый набор без сети, без Tkinter
- `py -3 -m unittest test_probe_wham_usage -v` — exit 0 (регресс-проверка: этот план не трогает probe_wham_usage.py, но должен оставаться зелёным)
- `git status --porcelain json_usage_provider.py probe_wham_usage.py` — пустой вывод (Plan 02-02 не трогает файлы Plan 02-01)
</verification>

<success_criteria>
- `parse_reset_datetime` корректно разбирает ISO-даты из JSON-провайдера, не ломая существующие ru/en/relative форматы
- `build_balance_from_json_fields` и `plan_fetch_outcome` полностью покрыты unit-тестами и воспроизводят все статусные сообщения текущего `fetch_once` плюс новую пометку источника по D-05
- `fetch_once`/`__init__` НЕ изменены в этом плане — вся логика готова к чистой врезке в Plan 02-03
</success_criteria>

<output>
Create `.planning/phases/02-json-provider-integration/02-02-SUMMARY.md` when done
</output>
