---
phase: 04-claude-widget-adoption-bugfixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - d:/00_Projects/claude_balance_widget_v1/claude_balance_widget.py
  - d:/00_Projects/claude_balance_widget_v1/test_core.py
  - d:/00_Projects/claude_balance_widget_v1/claude_balance_widget_launcher.pyw
  - d:/00_Projects/claude_balance_widget_v1/install_dependencies.bat
  - d:/00_Projects/claude_balance_widget_v1/README_RU.md
autonomous: true
requirements: [CLAUDE-01, CLAUDE-02, CLAUDE-03]
must_haves:
  truths:
    - "claude_balance_widget.py imports FetchError and fetch_with_retry_once from usage_widget_common instead of any local duplicate retry loop (CLAUDE-01)"
    - "ClaudeUsageClient.fetch() raises FetchError with retryable=True only for HTTP 429, network (URLError), and timeout errors — every other failure (401, 403, other HTTP codes, malformed/missing credentials, malformed JSON, missing usage data) raises FetchError with the default retryable=False, mirroring Codex's established classification"
    - "Tray tooltip is truncated to 127 characters, never 160 — build_tray_tooltip() cannot raise pystray's 'string too long (maximum length 128)' ValueError regardless of input length (CLAUDE-02)"
    - "After any fetch failure, the widget's next attempt is scheduled using a 30s -> 60s -> 120s backoff instead of the full refresh_seconds (default 300s) interval; a subsequent success resets the schedule back to the normal interval (CLAUDE-03)"
    - "If the usage_widget_common sibling repo is missing, claude_balance_widget.py fails with a clean SystemExit + one-line diagnostic when launched (no raw traceback), and with a plain ModuleNotFoundError when imported as a module (e.g. by test_core.py) — mirroring Phase 3's iteration-3-hardened bootstrap pattern, not the original naive version"
    - "claude_balance_widget_launcher.pyw shows a Russian messagebox diagnostic for both the SystemExit and ModuleNotFoundError sibling-missing cases, with log-write failures never suppressing the dialog"
    - "install_dependencies.bat and README_RU.md document the new usage_widget_common sibling-repo dependency, matching the documentation parity Phase 3 had to retrofit for the Codex widget (CR-01)"
    - "py -3 test_core.py exits 0 in d:/00_Projects/claude_balance_widget_v1, covering the new retry/backoff/tooltip behaviors alongside the pre-existing Balance/format assertions"
  artifacts:
    - path: "d:/00_Projects/claude_balance_widget_v1/claude_balance_widget.py"
      provides: "sys.path bootstrap to usage_widget_common (hardened, SystemExit/ModuleNotFoundError split); FetchError-based error classification in ClaudeUsageClient; ClaudeUsageClient.fetch_with_retry() delegating to fetch_with_retry_once; compute_next_wait_seconds() backoff helper; build_tray_tooltip() truncated to 127"
      contains: "fetch_with_retry_once"
    - path: "d:/00_Projects/claude_balance_widget_v1/test_core.py"
      provides: "assertions covering FetchError retryable/non-retryable retry behavior, compute_next_wait_seconds() backoff values, and tooltip truncation"
      contains: "compute_next_wait_seconds"
    - path: "d:/00_Projects/claude_balance_widget_v1/claude_balance_widget_launcher.pyw"
      provides: "3-way except split (SystemExit / ModuleNotFoundError / Exception) with OSError-guarded log writes and messagebox diagnostics, mirroring codex_balance_widget_launcher.pyw"
      contains: "except ModuleNotFoundError"
    - path: "d:/00_Projects/claude_balance_widget_v1/install_dependencies.bat"
      provides: "warning check for a missing usage_widget_common sibling directory"
      contains: "usage_widget_common"
    - path: "d:/00_Projects/claude_balance_widget_v1/README_RU.md"
      provides: "Требования section documenting the usage_widget_common sibling-repo dependency"
      contains: "usage_widget_common"
  key_links:
    - from: "d:/00_Projects/claude_balance_widget_v1/claude_balance_widget.py"
      to: "usage_widget_common.errors.FetchError"
      via: "from usage_widget_common.errors import FetchError"
      pattern: "from usage_widget_common\\.errors import FetchError"
    - from: "d:/00_Projects/claude_balance_widget_v1/claude_balance_widget.py"
      to: "usage_widget_common.retry.fetch_with_retry_once"
      via: "outcome = asyncio.run(fetch_with_retry_once(_do_fetch, retry_delay=self.retry_delay))"
      pattern: "fetch_with_retry_once\\("
    - from: "ClaudeBalanceWidget._fetch_once_worker"
      to: "ClaudeUsageClient.fetch_with_retry"
      via: "balance, status, keys = self.client.fetch_with_retry()"
      pattern: "self\\.client\\.fetch_with_retry\\(\\)"
    - from: "ClaudeBalanceWidget._refresh_worker"
      to: "compute_next_wait_seconds"
      via: "self.refresh_wakeup.wait(timeout=compute_next_wait_seconds(self.consecutive_failures, self.refresh_seconds))"
      pattern: "compute_next_wait_seconds\\("
---

<objective>
**TWO-REPOSITORY PLAN — READ THIS FIRST.** Every file this plan modifies lives in `d:/00_Projects/claude_balance_widget_v1` — a *separate* git repository from `d:/00_Projects/codex_balance_widget` (where `.planning/` and this PLAN.md itself live). `claude_balance_widget_v1` was freshly git-initialized this milestone (baseline commit `e1c240b`, single `master` branch, no prior GSD-managed commits) — this plan's commit will be its first feature commit ever. All git commands for this plan's own changes MUST run with an explicit `cd /d/00_Projects/claude_balance_widget_v1` (or `-C d:/00_Projects/claude_balance_widget_v1`) first — the current repo's `gsd-sdk query commit` helper is scoped to `codex_balance_widget` and will NOT reach this sibling repo. A third repo, `d:/00_Projects/usage_widget_common` (built and code-reviewed in Phase 3), is consumed read-only via a `sys.path` bootstrap at import time — this plan must NOT modify anything inside it.

Wire `claude_balance_widget.py` onto the shared `usage_widget_common` package's retry-once wrapper and error classification (CLAUDE-01), fix the tray-tooltip truncation crash (CLAUDE-02), and fix the slow post-failure retry by adding an outer-loop backoff schedule (CLAUDE-03). Also close the same "sibling-repo-missing" failure-mode gap Phase 3 had to retrofit as CR-01/WR-01/WR-02/WR-03 (`.planning/phases/03-shared-library-extraction-codex-migration/03-REVIEW*.md`) — this plan adopts the shared package for the FIRST time in this repo, so it must ship with the hardened bootstrap pattern and matching docs/install-script updates from day one, not the naive version Codex started with.

**Discretion notes carried over from `04-CONTEXT.md`, resolved during planning (read `usage_widget_common/fetch_decision.py` and `redaction.py` — do not re-derive this from scratch):**
- `usage_widget_common.fetch_decision.decide_fetch_source` is **NOT** adopted here. It expresses a primary/fallback 3-way choice; `claude_balance_widget.py` has exactly one data source (the OAuth `/api/oauth/usage` endpoint) with no JSON/Chrome-style fallback, so the primitive has no second source to parameterize against. The widget's existing single-source "show last known data on failure" behavior (`self.current_balance.has_usage_data` check in `_fetch_once_worker`'s `failure()` closure) is left untouched.
- `usage_widget_common.redaction.redact`/`redaction_clean` is **NOT** adopted here either. Unlike `probe_wham_usage.py` (which prints/writes raw JSON payloads and therefore needs redaction), `claude_balance_widget.py` never logs or displays the raw API payload or the OAuth token — every error message is a hand-authored Russian diagnostic string with no interpolated token/payload content (verified by reading `ClaudeUsageClient.fetch()`/`_load_token()` in full before this plan was written). Converting these messages to `FetchError` in Task 1 does not change this property. See the threat model's T-4-03 disposition for the explicit audit trail.

Purpose: complete Roadmap Phase 4 (all 4 Success Criteria) and close REQUIREMENTS.md CLAUDE-01/02/03, using Phase 3's proven, reviewed shared package and its hardened sibling-repo-missing pattern.

Output: `claude_balance_widget.py` consumes `usage_widget_common` for retry/error-classification; tooltip truncation and post-failure backoff bugs fixed; launcher/install-script/README updated for the new sibling-repo dependency; `test_core.py` covers all new behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-claude-widget-adoption-bugfixes/04-CONTEXT.md
@.planning/todos/pending/claude-widget-tooltip-and-401-retry.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Adopt usage_widget_common's FetchError classification and retry-once wrapper (CLAUDE-01)</name>
  <files>d:/00_Projects/claude_balance_widget_v1/claude_balance_widget.py, d:/00_Projects/claude_balance_widget_v1/test_core.py</files>
  <read_first>
    - D:/00_Projects/claude_balance_widget_v1/claude_balance_widget.py — lines 9-25 (stdlib import block, currently alphabetical: atexit, json, os, re, threading, tkinter, traceback, urllib.error, urllib.request, webbrowser — Path already imported at line 23, no new import needed for it), lines 27-41 (msvcrt / pystray+PIL try/except blocks — bootstrap goes immediately after this, before line 44's APP_VERSION), lines 58-60 (REFRESH_SECONDS/COUNTDOWN_REFRESH_MS/REQUEST_TIMEOUT_SECONDS constants — new RETRY_DELAY_SECONDS constant goes here), lines 226-326 (class ClaudeUsageClient — _load_token at 240-256 has 3 raise RuntimeError(...) sites; fetch at 258-325 has 9 raise RuntimeError(...) sites, 12 total in the class), lines 1197-1239 (_fetch_once_worker — line 1203 "balance, status, keys = self.client.fetch()" is the call site to change)
    - D:/00_Projects/codex_balance_widget/codex_balance_widget_chrome.py — lines 49-91, the FINAL iteration-3-hardened bootstrap (verified working in 03-REVIEW-FIX.md / 03-REVIEW.iter3.md): the sibling existence-check + sys.path.insert + the "from usage_widget_common... import" statement are wrapped together in a try block, caught by "except ModuleNotFoundError as exc:", and INSIDE that except-handler it branches on "if __name__ == '__main__':" to raise SystemExit(str(exc)) from None (clean one-liner for direct/launcher invocation) versus a bare "raise" (preserving ModuleNotFoundError for unittest/import-time callers like test_codex_balance_widget_chrome.py). Mirror this exact two-branch structure — do NOT wrap only an "if __name__ == '__main__':" block at the bottom of the file; the failure happens at import time, before that block is ever reached.
    - D:/00_Projects/usage_widget_common/usage_widget_common/errors.py — class FetchError(RuntimeError) with __init__(self, message: str, *, retryable: bool = False)
    - D:/00_Projects/usage_widget_common/usage_widget_common/retry.py — async def fetch_with_retry_once(fetch_once, *, retry_delay: float = 1.0) -> RetryOutcome, where RetryOutcome has fields status ("ok"/"error"), value, error, retried; catches FetchError for classification, treats any other exception as non-retryable
    - D:/00_Projects/claude_balance_widget_v1/test_core.py — full file (22 lines) — plain module-level assert script, no unittest/pytest framework, no existing imports of ClaudeUsageClient
  </read_first>
  <behavior>
    - ClaudeUsageClient.fetch_with_retry() where the underlying fetch() raises FetchError("transient", retryable=True) on the first call then succeeds on the second call -> returns the second call's result, fetch() called exactly twice
    - ClaudeUsageClient.fetch_with_retry() where fetch() raises FetchError("auth expired", retryable=False) (e.g. simulating a 401) -> raises immediately, fetch() called exactly once, no retry
    - ClaudeUsageClient.fetch_with_retry() where fetch() raises a plain unexpected exception (not FetchError) -> treated as non-retryable per the shared package's own classification rule, fetch() called exactly once
    - compute_next_wait_seconds is out of scope for this task (belongs to Task 2) — do not implement it here
  </behavior>
  <action>
    Add "import asyncio" to the stdlib import block immediately before "import atexit" (alphabetically first: asyncio precedes atexit), and add "import sys" immediately after "import re" and before "import threading" (alphabetically between them). Path is already imported at line 23 — do not re-import it.

    Immediately after the pystray/PIL try/except block (ends at line 41) and before "APP_VERSION = \"v1-oauth-widget\"", insert the hardened sibling-repo bootstrap, mirroring codex_balance_widget_chrome.py's exact iteration-3 pattern (see read_first): compute "_SIBLING_COMMON = Path(__file__).resolve().parent.parent / \"usage_widget_common\""; inside a try block, raise ModuleNotFoundError with the message "usage_widget_common not found at {_SIBLING_COMMON}.\nClone it as a sibling of this repo (see README_RU.md) before running claude_balance_widget.py." if "not _SIBLING_COMMON.is_dir()"; otherwise (or after passing that check) do "if str(_SIBLING_COMMON) not in sys.path: sys.path.insert(0, str(_SIBLING_COMMON))" followed by "from usage_widget_common.errors import FetchError" and "from usage_widget_common.retry import fetch_with_retry_once". Catch with "except ModuleNotFoundError as exc:" and inside it: if __name__ == "__main__" then "raise SystemExit(str(exc)) from None", else a bare "raise" (preserving ModuleNotFoundError for test_core.py's own "from claude_balance_widget import (...)").

    Add "RETRY_DELAY_SECONDS = 1.0" immediately after "REQUEST_TIMEOUT_SECONDS = 20" (line 60) — matches json_usage_provider.py's established convention for the shared package's per-attempt retry delay (distinct from Task 2's outer 30/60/120 backoff schedule).

    In ClaudeUsageClient.__init__ (currently "def __init__(self, credentials_path: Path = CREDENTIALS_PATH):"), add a second parameter "retry_delay: float = RETRY_DELAY_SECONDS" and store it as "self.retry_delay = retry_delay".

    In _load_token (lines 240-256): convert all 3 "raise RuntimeError(...)" sites to "raise FetchError(...)" with identical message text, adding "from exc" chaining only for the sites that are inside an active except block; the missing/invalid-token site (not inside any except block) must raise FetchError(...) directly, with no "from exc" clause since no exc is in scope there. Use the default retryable=False (no keyword needed) for all 3 — a missing/unreadable credentials file or missing token will not resolve itself within a 1-second retry.

    In fetch (lines 258-325): convert all 9 "raise RuntimeError(...)" sites to "raise FetchError(...)" with identical message text and "from exc" chaining where present, setting retryable=True ONLY for the HTTP 429 branch (line ~283), the urllib.error.URLError branch (line ~286), and the TimeoutError branch (line ~288) — these three mirror Codex's probe_wham_usage.fetch_usage classification exactly (429/network/timeout are transient). Leave the default retryable=False (no keyword) for the remaining 6 branches: HTTP 401 (line ~277), HTTP 403 (line ~281), other non-2xx HTTP codes (line ~284), json.JSONDecodeError (line ~293), non-dict payload (line ~295), and missing-usage-data (line ~323, "if not balance.has_usage_data:"). After this conversion, "raise RuntimeError" must not appear anywhere left in the file (12 sites total converted, 0 remaining).

    Add a new method "ClaudeUsageClient.fetch_with_retry(self) -> tuple[Balance, int, list[str]]" placed immediately after fetch and before "class SettingsStore:". Implementation: define a local "async def _do_fetch() -> tuple[Balance, int, list[str]]: return self.fetch()" closure (the underlying fetch() remains fully synchronous — no asyncio.to_thread needed since this coroutine runs inside its own transient event loop with nothing else scheduled on it), then "outcome = asyncio.run(fetch_with_retry_once(_do_fetch, retry_delay=self.retry_delay))". If outcome.status == "ok", return outcome.value. Otherwise raise FetchError(outcome.error or "Unknown fetch error") (retryable defaults to False — nothing downstream re-inspects it).

    In _fetch_once_worker (line 1203), change "balance, status, keys = self.client.fetch()" to "balance, status, keys = self.client.fetch_with_retry()". No other line in _fetch_once_worker changes in this task (its "except Exception as exc:" handling already works unchanged since FetchError is a RuntimeError/Exception subclass).

    In test_core.py: extend the existing "from claude_balance_widget import (...)" line to also import ClaudeUsageClient and FetchError (re-exported from claude_balance_widget's own namespace after its "from usage_widget_common.errors import FetchError" line — no separate usage_widget_common import needed in the test file). Add module-level test code (same plain-assert-script style as the existing file, appended after the existing asserts, before the final "print(\"Core tests passed.\")") covering the three behavior cases above: construct "client = ClaudeUsageClient(retry_delay=0)" (zero delay keeps the test fast — no real asyncio.sleep), monkeypatch ClaudeUsageClient.fetch at the class level (assign a replacement function, call client.fetch_with_retry(), then restore the original via a try/finally) to simulate each of the three scenarios, asserting both the returned/raised outcome AND the exact call count via a mutable counter (e.g. a list appended to inside the fake fetch). Update the final print(...) message to also confirm the new coverage (e.g. "Core tests passed (incl. retry/error-classification).").
  </action>
  <verify>
    <automated>cd /d/00_Projects/claude_balance_widget_v1 && py -3 test_core.py</automated>
  </verify>
  <acceptance_criteria>
    - `cd /d/00_Projects/claude_balance_widget_v1 && py -3 test_core.py` exits 0
    - `cd /d/00_Projects/claude_balance_widget_v1 && py -3 -m py_compile claude_balance_widget.py` exits 0 (syntax check)
    - `grep -c "raise RuntimeError" claude_balance_widget.py` equals 0
    - `grep -c "raise FetchError" claude_balance_widget.py` equals 12
    - `grep -c "retryable=True" claude_balance_widget.py` equals 3
    - `grep -c "from usage_widget_common.errors import FetchError" claude_balance_widget.py` equals 1
    - `grep -c "from usage_widget_common.retry import fetch_with_retry_once" claude_balance_widget.py` equals 1
    - `grep -c "def fetch_with_retry" claude_balance_widget.py` equals 1
    - `grep -c "except ModuleNotFoundError as exc" claude_balance_widget.py` equals 1
    - `grep -c "self.client.fetch_with_retry()" claude_balance_widget.py` equals 1
  </acceptance_criteria>
  <done>claude_balance_widget.py bootstraps usage_widget_common with the hardened sibling-missing pattern, ClaudeUsageClient raises FetchError with correct retryable classification everywhere it previously raised RuntimeError, and _fetch_once_worker calls the new fetch_with_retry() method which delegates to the shared fetch_with_retry_once; test_core.py covers all three retry-classification behaviors and passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix tooltip truncation and add post-failure backoff schedule (CLAUDE-02, CLAUDE-03)</name>
  <files>d:/00_Projects/claude_balance_widget_v1/claude_balance_widget.py, d:/00_Projects/claude_balance_widget_v1/test_core.py</files>
  <read_first>
    - D:/00_Projects/claude_balance_widget_v1/claude_balance_widget.py — lines 688-709 (build_tray_tooltip, the "return tooltip[:160]" line to fix), lines 712-735 (ClaudeBalanceWidget.__init__ state attributes, lines 722-726 — new self.consecutive_failures counter goes here), lines 1191-1239 (_refresh_worker at 1191-1195 and _fetch_once_worker at 1197-1239 — after Task 1's edits, line ~1203 now reads self.client.fetch_with_retry(); the success() closure sets self.last_fetch_error = None around line 1205, the except Exception as exc branch sets self.last_fetch_error = message around line 1227 — both are plain worker-thread attribute writes, not routed through _run_on_ui, since only actual tkinter widget updates need the main-thread hop)
    - D:/00_Projects/codex_balance_widget/.planning/todos/pending/claude-widget-tooltip-and-401-retry.md — the exact bug report and suggested fix this task closes (tooltip [:127]; 30 -> 60 -> 120s backoff, then normal interval)
  </read_first>
  <behavior>
    - build_tray_tooltip(...) on any input (including a deliberately oversized status string) always returns a string of length &lt;= 127
    - compute_next_wait_seconds(0, 300) == 300 (no failures yet -> normal interval)
    - compute_next_wait_seconds(1, 300) == 30, compute_next_wait_seconds(2, 300) == 60, compute_next_wait_seconds(3, 300) == 120
    - compute_next_wait_seconds(10, 300) == 120 (backoff caps at the schedule's last value, never grows unbounded)
  </behavior>
  <action>
    Change "return tooltip[:160]" (end of build_tray_tooltip) to "return tooltip[:127]" — pystray's actual tooltip limit is 128 characters; [:127] matches the todo's exact suggested fix and leaves headroom under the limit.

    Add a module-level constant "RETRY_BACKOFF_SCHEDULE_SECONDS: tuple[int, ...] = (30, 60, 120)" near RETRY_DELAY_SECONDS (added in Task 1). Add a new top-level pure function "def compute_next_wait_seconds(consecutive_failures: int, refresh_seconds: int) -> int:" placed near the other module-level helper functions (e.g. next to format_percent/usage_color, well before the ClaudeUsageClient class) — NOT as a method on any class, so it stays trivially unit-testable. Implementation: if consecutive_failures &lt;= 0, return refresh_seconds; otherwise return RETRY_BACKOFF_SCHEDULE_SECONDS[min(consecutive_failures, len(RETRY_BACKOFF_SCHEDULE_SECONDS)) - 1] (1 -> 30, 2 -> 60, 3 or more -> 120, capped).

    In ClaudeBalanceWidget.__init__, add "self.consecutive_failures: int = 0" alongside the other worker-state attributes (self.refresh_in_progress, self.last_successful_update, self.last_fetch_error, self.last_http_status, self.last_response_keys).

    In _fetch_once_worker's success path, add "self.consecutive_failures = 0" directly beside the existing "self.last_fetch_error = None" line (same worker-thread, non-UI attribute-write style already used there). In the "except Exception as exc:" branch, add "self.consecutive_failures += 1" directly beside the existing "self.last_fetch_error = message" line.

    In _refresh_worker, change "self.refresh_wakeup.wait(timeout=self.refresh_seconds)" to "self.refresh_wakeup.wait(timeout=compute_next_wait_seconds(self.consecutive_failures, self.refresh_seconds))". This is read AFTER _fetch_once_worker() returns, in the same thread, so self.consecutive_failures always reflects the fetch attempt that just completed — no locking needed. manual_refresh()'s self.refresh_wakeup.set() behavior is unaffected (it wakes the wait immediately regardless of the computed timeout).

    In test_core.py, extend the import line to also pull in build_tray_tooltip and compute_next_wait_seconds. Add assertions (same plain-assert style) for all 4 behavior cases above: build a Balance instance and call build_tray_tooltip(balance, None, "x" * 500) (a deliberately oversized status string) and assert len(result) &lt;= 127; assert the 4 compute_next_wait_seconds value pairs exactly as listed in behavior. Update the closing print(...) message to mention the new coverage.
  </action>
  <verify>
    <automated>cd /d/00_Projects/claude_balance_widget_v1 && py -3 test_core.py</automated>
  </verify>
  <acceptance_criteria>
    - `cd /d/00_Projects/claude_balance_widget_v1 && py -3 test_core.py` exits 0
    - `grep -c "tooltip\[:160\]" claude_balance_widget.py` equals 0
    - `grep -c "tooltip\[:127\]" claude_balance_widget.py` equals 1
    - `grep -c "RETRY_BACKOFF_SCHEDULE_SECONDS" claude_balance_widget.py` is at least 2 (constant definition plus its use inside compute_next_wait_seconds)
    - `grep -c "def compute_next_wait_seconds" claude_balance_widget.py` equals 1
    - `grep -c "compute_next_wait_seconds(self.consecutive_failures, self.refresh_seconds)" claude_balance_widget.py` equals 1
    - `grep -c "self.consecutive_failures" claude_balance_widget.py` is at least 4 (init, success reset, failure increment, _refresh_worker read)
  </acceptance_criteria>
  <done>build_tray_tooltip never returns more than 127 characters; compute_next_wait_seconds implements the 30/60/120s backoff schedule and is wired into _refresh_worker via a consecutive_failures counter reset on success and incremented on failure; test_core.py covers both behaviors and passes.</done>
</task>

<task type="auto">
  <name>Task 3: Harden launcher for the sibling-missing failure mode; update install script and README; commit</name>
  <files>d:/00_Projects/claude_balance_widget_v1/claude_balance_widget_launcher.pyw, d:/00_Projects/claude_balance_widget_v1/install_dependencies.bat, d:/00_Projects/claude_balance_widget_v1/README_RU.md</files>
  <read_first>
    - D:/00_Projects/claude_balance_widget_v1/claude_balance_widget_launcher.pyw — full file (27 lines) — currently a single try/except Exception block with no messagebox import and no handling for SystemExit/ModuleNotFoundError specifically
    - D:/00_Projects/codex_balance_widget/codex_balance_widget_launcher.pyw — full file, the FINAL hardened reference (all of CR-01/WR-01/WR-02/WR-03 fixes applied and iteration-3-verified): "from tkinter import messagebox" import; "except SystemExit as exc:" (only shows a dialog if exc.code not in (None, 0), since a bare raise SystemExit already shows its own dialog); "except ModuleNotFoundError as exc:" (always shows a dialog — this is the sibling-missing case surfaced through claude_balance_widget.py's Task-1 bootstrap); "except Exception:" (generic crash dialog). In ALL THREE clauses, the write_log(...)/LOG_PATH.open(...) calls are wrapped in their own try/except OSError: pass so a log-write failure (disk full, locked file, etc.) can never suppress the messagebox.showerror(...) call that follows it (03-REVIEW.iter3.md WR-03 / 03-REVIEW-FIX.md).
    - D:/00_Projects/claude_balance_widget_v1/install_dependencies.bat — full file (22 lines) — already has "setlocal" + "cd /d \"%~dp0\"" at the top; the sibling-repo check goes right before the :done label
    - D:/00_Projects/codex_balance_widget/install.bat — full file, lines 6-14: the exact WARNING block to mirror ("if not exist \"%~dp0..\usage_widget_common\" (...)")
    - D:/00_Projects/claude_balance_widget_v1/README_RU.md — full file (108 lines) — Russian-only doc, no existing "Требования"/Requirements section; insert one immediately before the existing "## Установка" heading
  </read_first>
  <action>
    In claude_balance_widget_launcher.pyw: add "from tkinter import messagebox" to the imports. Replace the single "except Exception:" clause with three clauses, in this order, mirroring codex_balance_widget_launcher.pyw's finalized structure exactly but with Russian dialog text and this project's own filenames (claude_balance_widget.py, claude_balance_widget.log) and dialog title "Claude Code balance" (matching the title already used by messagebox.showinfo("Claude Code balance", ...) inside claude_balance_widget.py itself, line ~1569):
    1. "except SystemExit as exc:" — only act if exc.code not in (None, 0) (a bare raise SystemExit with no message already shows its own dialog elsewhere, e.g. the "already running" app-lock case in claude_balance_widget.py's main()); inside, wrap write_log(f"Widget exited during startup: {exc.code}") in try/except OSError: pass, then unconditionally call messagebox.showerror("Claude Code balance", f"Не удалось запустить виджет:\n\n{exc.code}") after the guarded log write (not inside its try).
    2. "except ModuleNotFoundError as exc:" — this is the sibling-repo-missing path surfaced by Task 1's bootstrap when claude_balance_widget.py is run via this launcher's runpy.run_path(..., run_name="__main__"). Wrap write_log(f"Widget exited during startup: {exc}") in try/except OSError: pass, then call messagebox.showerror("Claude Code balance", f"Не удалось запустить виджет:\n\n{exc}").
    3. "except Exception:" — wrap both the write_log("Widget crashed before startup:") call and the "with LOG_PATH.open(\"a\", encoding=\"utf-8\") as log_file: traceback.print_exc(file=log_file)" block together in one try/except OSError: pass, then call messagebox.showerror("Claude Code balance", "Виджет аварийно завершился при запуске. Подробности в claude_balance_widget.log.").

    In install_dependencies.bat: immediately after the ":done" label (before the existing "echo."/"pause" lines), insert the sibling-repo warning block, translated 1:1 from install.bat's lines 6-14 but referencing README_RU.md instead of README.md/README.ru.md: an "if not exist" check against "%~dp0..\usage_widget_common\" that echoes a multi-line WARNING (missing directory path, requirement explanation, "cannot be obtained via git clone", "See README_RU.md for how to get it") followed by a blank "echo." line — keep every line as a separate echo statement exactly as in the reference file (batch multi-line parenthesized blocks require one echo per line).

    In README_RU.md: insert a new "## Требования" section immediately before the existing "## Установка" heading, listing: Windows; Python 3.10+ with the py launcher; Claude Code установлен и авторизован; and a bullet describing the ../usage_widget_common sibling-directory requirement (local, unpublished package, no git remote or PyPI listing, git clone will not work, obtain it from wherever the maintainer shares it, install_dependencies.bat warns if missing) — translate the substance of install.bat's comment / README.md's Requirements bullet into natural Russian, matching this file's existing tone and terminology (it already says "Установите" for install-type instructions elsewhere).

    Finish this task (and the whole plan) with a single commit. Run "cd /d/00_Projects/claude_balance_widget_v1 && git add claude_balance_widget.py test_core.py claude_balance_widget_launcher.pyw install_dependencies.bat README_RU.md" (list files explicitly — do NOT use git add -A or git add . , since the working tree also has an unrelated pre-existing modification to claude_balance_widget_settings.json and several gitignored runtime artifacts that must NOT be part of this commit). Then commit with message: feat(phase4): adopt usage_widget_common retry/error-classification; fix tooltip truncation and post-failure backoff (CLAUDE-01, CLAUDE-02, CLAUDE-03).
  </action>
  <verify>
    <automated>cd /d/00_Projects/claude_balance_widget_v1 && py -3 -m py_compile claude_balance_widget_launcher.pyw</automated>
  </verify>
  <acceptance_criteria>
    - `cd /d/00_Projects/claude_balance_widget_v1 && py -3 -m py_compile claude_balance_widget_launcher.pyw` exits 0
    - Reproduction (script/direct-invocation path, expect clean SystemExit, no traceback): run a subprocess that monkeypatches `pathlib.Path.is_dir` to return False and then does `runpy.run_path('claude_balance_widget.py', run_name='__main__')` inside `d:/00_Projects/claude_balance_widget_v1` — returncode equals 1, stderr contains "usage_widget_common not found", stderr does NOT contain "Traceback"
    - Reproduction (module-import path, expect ModuleNotFoundError): same monkeypatch, but `import claude_balance_widget` instead of `runpy.run_path(..., run_name="__main__")` — process exits non-zero, stderr contains "ModuleNotFoundError" and "usage_widget_common not found"
    - `grep -c "except SystemExit as exc" claude_balance_widget_launcher.pyw` equals 1
    - `grep -c "except ModuleNotFoundError as exc" claude_balance_widget_launcher.pyw` equals 1
    - `grep -c "from tkinter import messagebox" claude_balance_widget_launcher.pyw` equals 1
    - `grep -c "usage_widget_common" install_dependencies.bat` is at least 1
    - `grep -c "usage_widget_common" README_RU.md` is at least 1
    - `git -C /d/00_Projects/claude_balance_widget_v1 log --oneline` shows exactly 2 commits total (baseline `e1c240b` plus this plan's new commit), and `git -C /d/00_Projects/claude_balance_widget_v1 status --porcelain -- claude_balance_widget_settings.json` still shows it as the only other outstanding change (untouched by this plan's commit)
  </acceptance_criteria>
  <done>claude_balance_widget_launcher.pyw shows a clean Russian messagebox diagnostic for both the SystemExit and ModuleNotFoundError sibling-missing cases (log-write failures cannot suppress the dialog); install_dependencies.bat warns when usage_widget_common is missing; README_RU.md documents the new sibling-repo requirement; all five changed files are committed as this repo's first feature commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `claude_balance_widget.py` -> `usage_widget_common` (sibling repo, via `sys.path` insertion) | New code-execution boundary for this widget, mirroring the boundary Phase 3 already introduced and hardened for the Codex widget — same trust level, same developer-controlled sibling repo |
| `claude_balance_widget.py` -> `api.anthropic.com/api/oauth/usage` (unchanged) | Existing external network call carrying a Bearer OAuth token; this plan changes the exception TYPE raised on failure (RuntimeError -> FetchError) but not the message text, the token handling, or the request itself |
| `claude_balance_widget_launcher.pyw` -> `claude_balance_widget.py` (local process launch, via `runpy.run_path`) | New exception types (`SystemExit`, `ModuleNotFoundError`) can now propagate from the launched script at import time; the launcher must surface a diagnostic for each without silently losing it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-01 | Tampering | `sys.path` bootstrap in `claude_balance_widget.py` (Task 1) | mitigate | Idempotent check (`if str(path) not in sys.path`), fixed relative sibling location derived from `__file__` only (no user/network input), identical pattern to Phase 3's `codex_balance_widget_chrome.py` (verified there via `03-REVIEW.iter3.md`'s independent reproduction). |
| T-4-02 | Denial of Service | Sibling repo (`usage_widget_common`) missing on a fresh clone of `claude_balance_widget_v1` — equivalent to Phase 3's CR-01 (unhandled hard dependency, raw traceback on fresh installs) | mitigate | Task 1 ships the bootstrap with the iteration-3-hardened pattern from day one (`ModuleNotFoundError`/`SystemExit` split by `__name__ == "__main__"`, never the naive iteration-1 version); Task 3 hardens the launcher's three except-clauses (with OSError-guarded log writes so a log failure can never suppress the user-facing dialog — Phase 3's WR-03) and updates `install_dependencies.bat` + `README_RU.md` so a fresh install is warned/documented instead of silently broken. |
| T-4-03 | Information Disclosure | Converting 12 `RuntimeError` raises to `FetchError` in `ClaudeUsageClient` could, in principle, introduce a message that echoes the OAuth token or raw API payload | accept | Audited during planning (see Objective's discretion notes): every one of the 12 messages is a hand-authored Russian diagnostic string with no token/payload interpolation, both before and after this plan's change — only the exception class changes, not the message content. `usage_widget_common.redaction` is deliberately not adopted here because there is nothing to redact in this widget's own log/status output (unlike `probe_wham_usage.py`, which prints raw JSON and needs it). |
| T-4-04 | Repudiation / Consistency | A bug in the shared `fetch_with_retry_once` now also affects the Claude widget, not only Codex | accept | `usage_widget_common`'s own unit tests (Phase 3) already cover `fetch_with_retry_once`'s attempt-count/chaining behavior directly; Task 1 additionally adds widget-level tests asserting the retryable/non-retryable classification specific to Claude's own HTTP-status-to-`FetchError` mapping, so a regression in either layer is caught independently. |
| T-4-SC | Tampering | npm/pip/cargo installs | mitigate | No new package-manager installs in this phase — `usage_widget_common` is stdlib-only (audited in Phase 3), and `requirements.txt` (`pystray`, `Pillow`) is unchanged; verified via `git diff --stat -- requirements.txt` showing no change and a grep of import statements in Task 1's acceptance criteria. |

</threat_model>

<verification>
- `cd /d/00_Projects/claude_balance_widget_v1 && py -3 test_core.py` — exit 0, covers Balance/format assertions (pre-existing) plus FetchError retry classification, tooltip truncation, and backoff schedule (this plan)
- `cd /d/00_Projects/claude_balance_widget_v1 && py -3 -m py_compile claude_balance_widget.py claude_balance_widget_launcher.pyw` — exit 0, both modified `.py`/`.pyw` files parse cleanly
- `git -C /d/00_Projects/claude_balance_widget_v1 log --oneline` — 2 commits total (baseline `e1c240b` + this plan's commit)
- `git -C /d/00_Projects/usage_widget_common status --porcelain` — empty; this plan touches ONLY `claude_balance_widget_v1`, zero changes in the sibling shared-package repo
</verification>

<success_criteria>
- Roadmap Phase 4 Success Criterion 1: `claude_balance_widget.py` calls the shared package's retry-once wrapper and error classification for its usage-fetch calls instead of any local duplicate — satisfied (Task 1).
- Success Criterion 2: tray tooltip truncated to 127 characters, no more `ValueError: string too long` — satisfied (Task 2).
- Success Criterion 3: after a 401/transient error, the widget retries using a 30s -> 60s -> 120s backoff instead of waiting the full `refresh_seconds` — satisfied (Task 2).
- Success Criterion 4: the widget runs end-to-end with the new wiring, no behavioral regression versus prior behavior — supported by `test_core.py`'s full pass plus the sibling-missing reproduction checks in Task 3; a live manual smoke-check against real Claude Code credentials is optional follow-up, not a blocking gate, since every new code path (retry classification, backoff schedule, tooltip truncation, bootstrap failure modes) is covered by an automated check in this plan.
- REQUIREMENTS.md CLAUDE-01, CLAUDE-02, CLAUDE-03 all satisfied; Traceability table status can move from "Pending" to "Complete" for all three.
</success_criteria>

<output>
Create `.planning/phases/04-claude-widget-adoption-bugfixes/04-01-SUMMARY.md` when done. Since this plan's actual code changes and commit live in `d:/00_Projects/claude_balance_widget_v1` (not this repo), the SUMMARY must explicitly record that sibling repo's resulting commit hash (from `git -C /d/00_Projects/claude_balance_widget_v1 log --oneline -1`), since it will never appear in `codex_balance_widget`'s own `git log`.
</output>
