---
milestone: v1.0
audited: 2026-07-21T13:30:00Z
status: tech_debt
scores:
  requirements: 7/7
  phases: 2/2
  integration: 6/6
  flows: 1/1
gaps: []
tech_debt:
  - phase: 01-json-endpoint-probe
    items:
      - "WARNING (01-REVIEW.md CR-01): stdout print of redact()ed payload has no redaction_clean() post-check, unlike write_fixture(). No observed leak, but not defense-in-depth against unknown future field names."
      - "WARNING (01-REVIEW.md CR-02): collect_windows() assumes rate_limit/additional_rate_limits[] entries are dicts once truthy — a non-dict value raises an uncaught AttributeError. Only reachable on a malformed/unexpected payload shape, not exercised by any declared success criteria."
      - "WARNING (01-REVIEW.md WR-01): main() only catches ProbeError; other exception types (including CR-02's AttributeError) bypass the single-diagnostic contract and would print a raw traceback."
      - "INFO (01-REVIEW.md WR-02): weak test assertion (assertIn('20', ...)) that would pass even on a formatting regression."
      - "INFO (01-REVIEW.md WR-03): no test coverage for fetch_usage() HTTP-error branches (401/403/429/HTML) or main() end-to-end."
  - phase: 02-json-provider-integration
    items:
      - "INFO (02-REVIEW.md IN-04/IN-06): diagnostics don't disambiguate JSON vs Chrome source in the UI; last_usage_text_length is always None on JSON success with a stale Chrome-specific label."
      - "INFO (02-REVIEW.md IN-05): _log_manual_refresh_exception's future parameter is type-annotated asyncio.Future but the actual runtime type from run_coroutine_threadsafe is concurrent.futures.Future."
      - "INFO (02-REVIEW.md IN-07): intentionally-dead trailing return in the collapsed _fetch_with_retry loop."
nyquist:
  compliant_phases: 0
  partial_phases: 1
  missing_phases: 1
  overall: "partial — Phase 1 has VALIDATION.md (nyquist_compliant: true but wave_0_complete: false); Phase 2 has no VALIDATION.md"
---

# Milestone v1.0 — Audit Report

**Audited:** 2026-07-21T13:30:00Z
**Status:** tech_debt
**Phases in scope:** 01-json-endpoint-probe, 02-json-provider-integration

## Summary

Both phases passed their own goal-backward verification (9/9 and 3/3 observable truths respectively). All 7 requirements across the milestone (PROBE-01..04, JSONPROV-01..03) are satisfied with independent evidence, not just claimed. Cross-phase integration checker found 6/6 expected wiring points intact and the single E2E flow (widget startup → refresh loop → JSON-first fetch → Chrome fallback → display + log) fully connected, live-verified against `widget_launch.log` on the real running app. No blockers. No orphaned or unsatisfied requirements.

The only outstanding item is accumulated code-review tech debt: 3 warnings from Phase 1 (never fixed — no fix commits found after Phase 1's own review) and 3 info-level items from Phase 2 (explicitly deferred as out-of-scope during that phase's 3-iteration auto-fix cycle, which did resolve all critical/warning findings raised there).

## Requirements Coverage

| Requirement | Phase | Description | Status | Evidence Source |
|---|---|---|---|---|
| PROBE-01 | 1 | Live run reaches wham/usage, HTTP 200 | SATISFIED | 01-VERIFICATION.md |
| PROBE-02 | 1 | Redaction covered by unit tests | SATISFIED | 01-VERIFICATION.md |
| PROBE-03 | 1 | Fixture written, valid, no PII | SATISFIED | 01-VERIFICATION.md |
| PROBE-04 | 1 | Token isolated, clean diagnostics | SATISFIED | 01-VERIFICATION.md |
| JSONPROV-01 | 2 | Normal cycle never launches Chrome | SATISFIED | 02-VERIFICATION.md + integration checker + live log |
| JSONPROV-02 | 2 | JSON failure falls back to Chrome without crash | SATISFIED | 02-VERIFICATION.md + integration checker + live log |
| JSONPROV-03 | 2 | Log records source per successful update | SATISFIED | 02-VERIFICATION.md + integration checker + live log |

No orphaned requirements (all 7 declared in ROADMAP.md are claimed by exactly the plans that satisfy them; project has no separate REQUIREMENTS.md — ROADMAP.md is the requirement source, cross-checked against SUMMARY.md `requirements-completed` frontmatter and VERIFICATION.md tables in both phases).

## Phase Verification Summary

| Phase | Status | Score | Anti-patterns |
|---|---|---|---|
| 01-json-endpoint-probe | passed | 9/9 truths | 3 WARNING, 2 INFO (unresolved, see tech debt) |
| 02-json-provider-integration | passed | 3/3 truths | 0 CRITICAL, 0 WARNING (all fixed), 4 INFO (deferred by design) |

## Integration & E2E Flows

- **Wiring:** 6/6 (load_tokens, auth_json_path, fetch_usage, extract_fields, ProbeError, ProbeError.retryable — all consumed correctly by Phase 2 with no signature drift)
- **Flows:** 1/1 complete (widget startup → refresh_loop → fetch_once → json_provider.fetch() → success/fallback → display + log), no broken links, no dead branches
- **Regression check:** Phase 2's only touch to Phase 1's `probe_wham_usage.py` was adding the `retryable` field (commit `1442999`) — `main()`/CLI/fixture behavior unaffected, confirmed via `py -3 probe_wham_usage.py --help`
- **Orphaned exports:** none in the E2E-relevant surface (`redact`/`redaction_clean`/`write_fixture` remain Phase-1-CLI-only by design, not a gap)

## Tech Debt Detail

### Phase 1 (3 warnings, never fixed)
- CR-01: no `redaction_clean()` post-check on stdout print path
- CR-02: `collect_windows()` can raise uncaught `AttributeError` on malformed payload
- WR-01: `main()` only catches `ProbeError`, not other exceptions

### Phase 2 (3 info items, explicitly deferred)
- Diagnostics don't disambiguate JSON vs Chrome source in UI text
- Type-annotation nit on `_log_manual_refresh_exception`'s `future` parameter
- Dead trailing `return` in collapsed `_fetch_with_retry` loop

None of these block any Success Criteria or requirement. All are pre-existing/deferred robustness gaps, not incomplete features.

## Nyquist Coverage

| Phase | VALIDATION.md | Compliant | Action |
|---|---|---|---|
| 01-json-endpoint-probe | exists | `nyquist_compliant: true`, `wave_0_complete: false` (partial) | `/gsd-validate-phase 1` (optional) |
| 02-json-provider-integration | missing | — | `/gsd-validate-phase 2` (optional) |

Discovery only — not auto-invoked.

---

*Audited: 2026-07-21T13:30:00Z*
*Auditor: Claude (gsd-audit-milestone)*
