---
milestone: v1.0
audited: 2026-07-21T14:00:00Z
status: tech_debt
scores:
  requirements: 7/7
  phases: 3/3
  integration: 6/6
  flows: 1/1
gaps: []
tech_debt:
  - phase: 01-json-endpoint-probe
    items:
      - "RESOLVED by Phase 01.1 (commit 7da5a44): CR-01 stdout redaction post-check."
      - "RESOLVED by Phase 01.1 (commit 7da5a44): CR-02 collect_windows() non-dict AttributeError."
      - "RESOLVED by Phase 01.1 (commits 7da5a44, 35ccbc7): WR-01 main() broad exception handling + WR-01a redaction guard on the new handler's own message."
      - "DEFERRED (backlog, reviewed and explicitly out of Phase 01.1's locked scope): WR-02 load_tokens() doesn't catch UnicodeDecodeError on non-UTF-8 auth.json."
      - "DEFERRED (backlog): WR-03 _remaining_percent() uses `100 - int(used)` instead of `int(100 - used)`, overstates remaining quota for fractional used_percent."
      - "DEFERRED (backlog): WR-04 _credits_text() unlimited check uses bare truthiness with no type guard."
      - "DEFERRED (backlog): WR-05 redact() REDACT_KEYS exact-match is case-sensitive while substring match is case-insensitive — inconsistent denylist coverage."
      - "DEFERRED (backlog, pre-existing INFO from original 01-REVIEW.md): weak test assertion (assertIn('20', ...)); no test coverage for fetch_usage() HTTP-error branches or main() end-to-end fixture path."
  - phase: 02-json-provider-integration
    items:
      - "INFO (02-REVIEW.md IN-04/IN-06): diagnostics don't disambiguate JSON vs Chrome source in the UI."
      - "INFO (02-REVIEW.md IN-05): type-annotation nit on _log_manual_refresh_exception's future parameter."
      - "INFO (02-REVIEW.md IN-07): dead trailing return in collapsed _fetch_with_retry loop."
  - phase: 01.1-address-phase-1-tech-debt-redaction-post-check-malformed-pay
    items: []
nyquist:
  compliant_phases: 0
  partial_phases: 1
  missing_phases: 2
  overall: "partial — Phase 1 has VALIDATION.md (nyquist_compliant: true, wave_0_complete: false); Phase 2 and Phase 01.1 have no VALIDATION.md (both used research_enabled=false path where Nyquist artifacts are not required)"
---

# Milestone v1.0 — Audit Report (Re-audit after Phase 01.1)

**Audited:** 2026-07-21T14:00:00Z
**Status:** tech_debt
**Phases in scope:** 01-json-endpoint-probe, 01.1-address-phase-1-tech-debt-redaction-post-check-malformed-pay, 02-json-provider-integration

## Summary

Since the prior audit (2026-07-21T13:30:00Z), the 3 escalated Phase 1 warnings (CR-01, CR-02, WR-01) were closed via an inserted cleanup phase (01.1), which passed its own goal-backward verification (4/4 must-haves, 64/64 tests) and additionally caught and fixed a regression in its own new code (WR-01a) via a targeted code-review pass. All requirements remain satisfied (7/7), all phases pass (3/3), and integration/flows remain fully wired (unchanged from prior audit — phase 01.1 only touched `probe_wham_usage.py`'s `main()`/`collect_windows()`, functions not consumed by any cross-phase integration point).

Remaining tech debt is now smaller and explicitly triaged: 4 pre-existing warnings (WR-02..05) plus a handful of INFO items across Phase 1 and Phase 2, all deliberately scoped OUT of Phase 01.1 (confirmed via diff that these functions predate and are untouched by the cleanup phase) and documented for a future backlog decision rather than silently dropped.

## Requirements Coverage

Unchanged from prior audit — 7/7 satisfied (PROBE-01..04, JSONPROV-01..03). Phase 01.1 carries no formal REQ-IDs (roadmap tag: `tech-debt-closure`), consistent with its nature as an inserted hardening phase.

## Phase Verification Summary

| Phase | Status | Score | Anti-patterns |
|---|---|---|---|
| 01-json-endpoint-probe | passed | 9/9 truths | 3 WARNING RESOLVED (via 01.1), 2 INFO deferred |
| 01.1-tech-debt-closure | passed | 4/4 truths | 0 CRITICAL, 1 WARNING found+fixed (WR-01a), 4 WARNING + 3 INFO deferred to backlog (pre-existing, out of scope) |
| 02-json-provider-integration | passed | 3/3 truths | 0 CRITICAL, 0 WARNING, 4 INFO deferred |

## Integration & E2E Flows

No change from prior audit — 6/6 wiring points, 1/1 E2E flow, no regressions. Phase 01.1's changes (`collect_windows()`, `main()`) are not on the JSON-provider→widget integration path (that path uses `load_tokens`, `fetch_usage`, `extract_fields`, `ProbeError`, `ProbeError.retryable` — none touched by 01.1).

## Tech Debt Detail (Residual, post-01.1)

### Phase 1 / probe_wham_usage.py (4 warnings, pre-existing, explicitly deferred)
- WR-02: `load_tokens()` doesn't catch `UnicodeDecodeError`
- WR-03: `_remaining_percent()` rounding order overstates remaining quota
- WR-04: `_credits_text()` unlimited-check lacks type guard
- WR-05: `redact()` denylist case-sensitivity inconsistency

### Phase 2 (3 info items, explicitly deferred by design during its own review)
- UI diagnostics don't disambiguate source
- Type-annotation nit
- Dead trailing return

None of these block any Success Criteria or requirement — all are robustness/polish gaps in code that already passes its stated goals.

## Nyquist Coverage

| Phase | VALIDATION.md | Compliant | Action |
|---|---|---|---|
| 01-json-endpoint-probe | exists | partial (`wave_0_complete: false`) | `/gsd-validate-phase 1` (optional) |
| 02-json-provider-integration | missing | — | `/gsd-validate-phase 2` (optional) |
| 01.1-tech-debt-closure | missing (no research phase, Nyquist not required for this run) | N/A | none needed |

Discovery only — not auto-invoked.

---

*Audited: 2026-07-21T14:00:00Z*
*Auditor: Claude (gsd-audit-milestone)*
