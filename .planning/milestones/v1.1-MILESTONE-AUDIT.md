---
milestone: v1.1
audited: 2026-07-22T22:30:00Z
status: tech_debt
scores:
  requirements: 8/8
  phases: 2/2
  integration: 6/6
  flows: 2/2
gaps: []
tech_debt:
  - phase: 03-shared-library-extraction-codex-migration
    items:
      - "INFO (03-REVIEW.md, deferred): sys.path bootstrap block duplicated across 3 Codex files, structurally identical but requires 3-way sync for future changes."
      - "INFO (03-REVIEW.md, deferred): ProbeError = FetchError changes traceback identity (points at usage_widget_common, not probe_wham_usage) — commented, functionally inert."
      - "INFO (03-REVIEW.md, deferred): E402 import-ordering unavoidable given sys.path technique, unsuppressed."
      - "INFO (03-REVIEW.md, deferred): no dedicated regression test for the bootstrap-failure code path itself."
      - "INFO (03-REVIEW.md, deferred): misleading example comment in launcher's except SystemExit clause."
  - phase: 04-claude-widget-adoption-bugfixes
    items:
      - "INFO (04-REVIEW.md, deferred): os.sys.executable should be sys.executable (module already imported at top of file)."
      - "INFO (04-REVIEW.md, deferred): tray tooltip 127-char truncation limit is a bare magic number, duplicated in test_core.py."
      - "INFO (04-REVIEW.md, deferred): sys.path bootstrap only catches ModuleNotFoundError, not broader ImportError — pre-existing, fleet-wide pattern shared with Codex, not a Phase-4 regression."
      - "INFO (04-REVIEW.md, deferred): 5xx retryable check assumes exc.code is always int — defensive-coding nitpick, not practically reachable."
      - "WARNING (integration-checker, non-blocking): REDACTED_PLACEHOLDER exported by usage_widget_common but neither widget imports it by name — both hardcode the literal string, fragile if the shared constant's value ever changes."
      - "HUMAN VERIFICATION DEFERRED (04-VERIFICATION.md): no live run against real Claude Code credentials occurred this session (machine has no .credentials.json) — user explicitly chose to defer rather than block. All automated coverage (test_core.py, code reads, reproduction scripts) passed; this is an evidentiary gap, not a known defect."
nyquist:
  compliant_phases: 0
  partial_phases: 0
  missing_phases: 2
  overall: "not applicable — both phases skipped research (well-understood extraction/adoption work), so Nyquist artifacts were never required for this milestone"
---

# Milestone v1.1 — Audit Report

**Audited:** 2026-07-22T22:30:00Z
**Status:** tech_debt
**Phases in scope:** 03-shared-library-extraction-codex-migration, 04-claude-widget-adoption-bugfixes

## Summary

Both phases passed their own goal-backward verification (Phase 3: 5/5 truths passed; Phase 4: 8/9, with the 1 gap explicitly routed to human-verification and deliberately deferred by the user rather than blocking). All 8 requirements across the milestone (SHARED-01..04, CODEX-01, CLAUDE-01..03) are satisfied with independent evidence. The integration checker found 6/6 expected cross-**repo** wiring points intact (this milestone's "cross-phase" wiring is literally cross-repo: `usage_widget_common` ↔ `codex_balance_widget` ↔ `claude_balance_widget_v1`) and both E2E flows (Codex refresh loop, Claude refresh loop) fully traced through real code, not just claimed. No blockers. No orphaned or unsatisfied requirements.

Notably: this milestone required TWO full code-review + auto-fix cycles (Phase 3: 3 iterations, closing 1 critical + 5 warnings across the passes; Phase 4: 2 iterations, closing 2 warnings) — both ended clean (0 critical/warning remaining), with only INFO-level polish items deliberately deferred. The reviewer caught a real, would-have-shipped defect in Phase 3 (CR-01: the migration silently introduced a hard, undocumented cross-repo dependency that crashed with no user feedback if the sibling repo was missing) — this was found and fixed before the milestone closed, not left as debt.

## Requirements Coverage

| Requirement | Phase | Description | Status | Evidence Source |
|---|---|---|---|---|
| SHARED-01 | 3 | Async retry-once wrapper for transient errors | SATISFIED | 03-VERIFICATION.md + integration checker |
| SHARED-02 | 3 | Error classification with retryable flag | SATISFIED | 03-VERIFICATION.md + integration checker |
| SHARED-03 | 3 | Redaction denylist + post-check | SATISFIED | 03-VERIFICATION.md + integration checker |
| SHARED-04 | 3 | Parameterized fetch-decision skeleton | SATISFIED | 03-VERIFICATION.md + integration checker |
| CODEX-01 | 3 | Codex files consume shared package, no behavior change | SATISFIED | 03-VERIFICATION.md + integration checker (0 .py diffs during Phase 4, confirming stability) |
| CLAUDE-01 | 4 | Claude widget adopts shared retry/error-classification | SATISFIED | 04-VERIFICATION.md + integration checker (same package, not a fork) |
| CLAUDE-02 | 4 | Tooltip truncated to 127 chars | SATISFIED | 04-VERIFICATION.md (live reproduction with 500-char string) |
| CLAUDE-03 | 4 | Fast retry/backoff after 401 | SATISFIED | 04-VERIFICATION.md + integration checker (30/60/120s schedule traced into refresh loop) |

No orphaned requirements — all 8 declared in ROADMAP.md are claimed by exactly the plans that satisfy them, cross-checked against SUMMARY.md frontmatter and VERIFICATION.md tables in both phases.

## Phase Verification Summary

| Phase | Status | Score | Anti-patterns |
|---|---|---|---|
| 03-shared-library-extraction-codex-migration | passed | 5/5 truths | 0 CRITICAL, 0 WARNING (all fixed across 3 iterations), 5 INFO deferred |
| 04-claude-widget-adoption-bugfixes | human_needed (deferred by user) | 8/9 truths | 0 CRITICAL, 0 WARNING (all fixed across 2 iterations), 4 INFO deferred |

## Integration & E2E Flows (Cross-Repo)

- **Wiring:** 6/6 — shared retry engine, error classification, redaction, fetch-decision, sys.path bootstrap parity, and bugfix wiring all confirmed genuinely shared (not forked/duplicated) between `codex_balance_widget` and `claude_balance_widget_v1`, both consuming the same `d:/00_Projects/usage_widget_common` package.
- **Flows:** 2/2 complete — Codex refresh loop (unaffected by Phase 4, 0 `.py` diffs confirmed via git history) and Claude refresh loop (new wiring, backoff schedule traced end-to-end into the actual wait-timeout mechanism).
- **Regression check:** `usage_widget_common` confirmed untouched during Phase 4 (git log shows a ~1h26m gap between Phase 3's last commit and Phase 4's first, zero new commits) — the shared package was consumed strictly read-only as designed.
- **Orphaned exports:** 1 minor (`REDACTED_PLACEHOLDER` constant unused-by-name, both consumers hardcode the literal string instead) — flagged as tech debt, not a gap.

## Tech Debt Detail

### Phase 3 (5 info items, deferred by design during its own 3-iteration review)
- Duplicated 3-file sys.path bootstrap block
- `ProbeError = FetchError` traceback-identity change (cosmetic)
- Unsuppressed E402 import ordering
- No dedicated test for the bootstrap-failure path itself
- Minor stale comment in launcher

### Phase 4 (4 info items + 1 integration-checker warning, all deferred)
- `os.sys.executable` vs `sys.executable` typo
- Magic number `127` (tooltip limit) not centralized
- `ModuleNotFoundError`-only catch (doesn't catch broader `ImportError`) — shared pattern with Codex, not new
- 5xx check assumes `exc.code` is always `int` (defensive nitpick)
- `REDACTED_PLACEHOLDER` unused-by-name in both consumers (new, found by integration checker)

### Human Verification Deferred (not tech debt, but tracked)
- No live run against real Claude Code credentials occurred this session. All automated evidence (20-assertion test suite, direct code reads, reproduction scripts for both bootstrap failure modes) passed. User explicitly chose "continue without validation" rather than block the milestone on this — recommend a live spot-check next time the widget is actually run on a machine with real credentials.

None of these items block any Success Criterion or requirement.

## Nyquist Coverage

Not applicable this milestone — both phases explicitly skipped research (well-understood extraction/adoption work per user's own scope confirmation), and per the workflow's own rule, Nyquist artifacts are not required when research is skipped and no `--research` flag was used. No VALIDATION.md exists for either phase, and none was expected.

---

*Audited: 2026-07-22T22:30:00Z*
*Auditor: Claude (gsd-audit-milestone)*
