---
phase: 02-json-provider-integration
plan: 03
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - codex_balance_widget_chrome.py
autonomous: false
requirements: [JSONPROV-01, JSONPROV-02, JSONPROV-03]
must_haves:
  truths:
    - "Обычный успешный цикл fetch_once не вызывает self.browser.fetch() — JSON основной источник (Success Criterion 1)"
    - "При ошибке JSON-пути fetch_once вызывает существующий self.browser.fetch() как фолбэк без падения приложения (Success Criterion 2)"
    - "widget_launch.log получает строку с 'source: json' на каждое успешное обновление через JSON и 'source: chrome' — через Chrome-фолбэк (Success Criterion 3 / D-05)"
    - "Chrome не держится прогретым в фоне — self.browser сконструирован как раньше, вызывается по требованию только внутри fallback-ветки (D-01)"
    - "Живой запуск виджета подтверждён человеком: обычный цикл не открывает Chrome, лог содержит source-маркер, фолбэк реально срабатывает при недоступном JSON"
  artifacts:
    - path: codex_balance_widget_chrome.py
      provides: "self.json_provider в __init__, переписанный fetch_once (JSON первично через plan_fetch_outcome, Chrome — только фолбэк), import json_usage_provider"
      contains: "self.json_provider.fetch()"
  key_links:
    - from: "CodexBalanceWidget.fetch_once"
      to: "json_usage_provider.JsonUsageProvider.fetch"
      via: "await self.json_provider.fetch() — первая попытка, до self.browser.fetch()"
      pattern: "self\\.json_provider\\.fetch\\(\\)"
    - from: "CodexBalanceWidget.fetch_once"
      to: "CodexUsageBrowser.fetch"
      via: "await self.browser.fetch() только внутри ветки, где json_result.status != 'ok'"
      pattern: "self\\.browser\\.fetch\\(\\)"
    - from: "CodexBalanceWidget.fetch_once"
      to: "write_log"
      via: "write_log(outcome.log_line) — источник каждого успешного обновления"
      pattern: "write_log\\(outcome\\.log_line\\)"
---

<objective>
Врезать `JsonUsageProvider` (Plan 02-01) и `plan_fetch_outcome` (Plan 02-02) в `__init__`/`fetch_once` — сделать JSON основным источником данных виджета с фолбэком на существующий `CodexUsageBrowser` (Chrome), как того требует Roadmap Phase 2. Единственный план фазы, трогающий "рабочий код виджета" в его текущей форме исполнения (`fetch_once`), поэтому единственный с человеческой проверкой живого запуска.

Purpose: закрыть все три Success Criteria фазы 2 — обычный цикл без Chrome, автоматический фолбэк без падений, лог источника — в самом виджете, а не только в изолированных модулях.

Output: `codex_balance_widget_chrome.py` с `self.json_provider`, переписанным `fetch_once`; живое человеческое подтверждение, что JSON-путь и Chrome-фолбэк реально работают на этой машине.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-json-provider-integration/02-CONTEXT.md
@.planning/phases/02-json-provider-integration/02-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: __init__ + fetch_once — JSON первично, Chrome фолбэк</name>
  <files>codex_balance_widget_chrome.py</files>
  <read_first>
    - D:/00_Projects/codex_balance_widget/codex_balance_widget_chrome.py — строки 1271-1293 (`CodexBalanceWidget.__init__` — где создаётся `self.browser`), строки 2073-2119 (текущий `fetch_once`, ПОЛНОСТЬЮ переписывается этой задачей), строки 37-46 (блок `try: import pystray...` — якорь для нового `import json_usage_provider` СРАЗУ ПОСЛЕ него)
    - D:/00_Projects/codex_balance_widget/json_usage_provider.py — публичный интерфейс из Plan 02-01: `JsonUsageProvider().fetch() -> JsonFetchResult(status, fields, error, retried)` (async)
    - D:/00_Projects/codex_balance_widget/codex_balance_widget_chrome.py — новые функции из Plan 02-02: `plan_fetch_outcome(...)` (keyword-only: json_status, json_fields, json_error, chrome_attempted, chrome_status, chrome_error, chrome_text, has_existing_data, language) -> `FetchOutcome(source, balance, status_message, log_line)`
    - D:/00_Projects/codex_balance_widget/.planning/phases/02-json-provider-integration/02-CONTEXT.md — D-01 (Chrome не держится прогретым — не менять `CodexUsageBrowser`), D-04 (виджет не ждёт refresh токена — не добавлять никакого ожидания вокруг `self.json_provider.fetch()`)
  </read_first>
  <action>
    Добавить `import json_usage_provider` СРАЗУ ПОСЛЕ строки `TRAY_IMPORT_ERROR = f"{type(exc).__name__}: {exc}"` (конец блока `try: import pystray...`) и ПЕРЕД `APP_VERSION = "v13-zero-seconds-weekly-block"`.

    В `CodexBalanceWidget.__init__` (строка 1274, сразу после `self.browser = CodexUsageBrowser(...)`) добавить `self.json_provider = json_usage_provider.JsonUsageProvider()`. Не менять `self.browser`, `self.refresh_seconds`, `self.chrome_path` — они остаются ровно как есть (D-01: Chrome запускается по требованию, как сейчас).

    Переписать `fetch_once` (строки 2073-2119) целиком, сохранив: guard `if self.refresh_in_progress: ...; return` в начале (без изменений) и `try/finally: self.refresh_in_progress = False` вокруг всего тела (без изменений). Внутри `try`:
    1. `json_result = await self.json_provider.fetch()`.
    2. Если `json_result.status == "ok"`: `outcome = plan_fetch_outcome(json_status="ok", json_fields=json_result.fields, json_error=None, chrome_attempted=False, chrome_status=None, chrome_error=None, chrome_text=None, has_existing_data=self.current_balance.has_usage_data, language=self.language)`; `self.last_fetch_status = "ok"`; `self.last_fetch_error = None`; `self.last_usage_text_length = None`. `self.browser.fetch()` в этой ветке НЕ вызывается ни при каких условиях (Success Criterion 1).
    3. Иначе, если `not self.browser`: `outcome = plan_fetch_outcome(json_status="error", json_fields=None, json_error=json_result.error, chrome_attempted=False, chrome_status="chrome_not_found", chrome_error=None, chrome_text=None, has_existing_data=self.current_balance.has_usage_data, language=self.language)`; `self.last_fetch_status = "chrome_not_found"`; `self.last_fetch_error = tr(self.language, "Google Chrome not found", "Google Chrome не найден")` (как в текущем коде, строка 2082).
    4. Иначе (JSON провалился, Chrome доступен): `result = await self.browser.fetch()` (существующий вызов, без изменений сигнатуры); `self.last_fetch_status = result.status`; `self.last_fetch_error = result.error`; `self.last_usage_text_length = len(result.text) if result.text else None` (как в текущем коде, строки 2087-2089); `outcome = plan_fetch_outcome(json_status="error", json_fields=None, json_error=json_result.error, chrome_attempted=True, chrome_status=result.status, chrome_error=result.error, chrome_text=result.text, has_existing_data=self.current_balance.has_usage_data, language=self.language)`.
    5. После вычисления `outcome` (во всех трёх ветках, единая точка): если `outcome.balance is not None` — `self.last_successful_update = datetime.now()`; `self.update_balance_ui(outcome.balance)`. Всегда: `self.set_status(outcome.status_message)`; `write_log(outcome.log_line)`.

    Не менять `refresh_loop` (строки 2116-2119) — он уже вызывает `self.fetch_once()` и не знает о деталях источника.
  </action>
  <verify>
    <automated>cd /d/00_Projects/codex_balance_widget && py -3 -c "import codex_balance_widget_chrome" && py -3 -m unittest test_probe_wham_usage test_json_usage_provider test_codex_balance_widget_chrome -v && py -3 -c "
import re
src = open('codex_balance_widget_chrome.py', encoding='utf-8').read()
i = src.index('async def fetch_once')
j = src.index(chr(10)+'    async def refresh_loop', i)
body = src[i:j]
assert 'self.json_provider.fetch()' in body, 'json_provider.fetch() not called in fetch_once'
assert body.index('self.json_provider.fetch()') < body.index('self.browser.fetch()'), 'browser.fetch() called before json_provider.fetch()'
assert 'plan_fetch_outcome(' in body, 'plan_fetch_outcome not used in fetch_once'
print('fetch_once structure OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - `py -3 -c "import codex_balance_widget_chrome"` завершает без исключений (импорт `json_usage_provider` резолвится, синтаксис корректен)
    - `py -3 -m unittest test_probe_wham_usage test_json_usage_provider test_codex_balance_widget_chrome -v` — exit 0, регресса нет ни в одном из трёх наборов
    - Python-скрипт из `<automated>` печатает "fetch_once structure OK" — подтверждает, что `self.json_provider.fetch()` вызывается раньше `self.browser.fetch()` внутри `fetch_once`, и что `self.browser.fetch()` находится в теле функции только один раз (не дублирован)
    - grep: `self.json_provider = json_usage_provider.JsonUsageProvider()` найдено ровно 1 раз в `__init__`
    - grep: `write_log(outcome.log_line)` найдено ровно 1 раз в `fetch_once` (единая точка логирования на все три ветки, не три отдельных write_log-вызова)
  </acceptance_criteria>
  <done>`fetch_once` пробует JSON первым, вызывает Chrome только при неудаче JSON, обновляет UI и статус через единый `FetchOutcome`, пишет источник в лог; ни один существующий тест не сломан; `CodexUsageBrowser`/`refresh_loop`/`self.refresh_seconds` не изменены.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Живая проверка — JSON основной источник, Chrome фолбэк, лог источника</name>
  <files>none — ревью без изменения файлов</files>
  <read_first>
    - D:/00_Projects/codex_balance_widget/widget_launch.log — куда смотреть за строками "source: json"/"source: chrome"
  </read_first>
  <action>
    Предъявить пользователю результат Task 1 на подтверждение: обычный запуск виджета (JSON-путь, без Chrome) и форсированный сбой JSON-пути (фолбэк на Chrome). Ничего не изменять в коде — только собрать подтверждение по шагам из how-to-verify. Реализация уже выполнена в Task 1; чекпоинт закрывает Success Criteria 1-3 фазы живым ревью поведения виджета, которое не покрывается unit-тестами (Tkinter/asyncio/реальный Chrome вне scope автотестов по `.planning/codebase/TESTING.md`).
  </action>
  <what-built>JSON-эндпоинт `wham/usage` — основной источник данных виджета; существующий Chrome-путь остаётся автоматическим фолбэком; источник каждого успешного обновления пишется в `widget_launch.log`.</what-built>
  <how-to-verify>
    1. Убедиться, что `~/.codex/auth.json` существует и содержит свежий токен (при сомнении выполнить любую команду `codex` в терминале — CLI обновит файл).
    2. В папке `D:/00_Projects/codex_balance_widget` запустить `py -3 codex_balance_widget_chrome.py`.
    3. Убедиться, что окно виджета показывает 5h%/Weekly%/Credits, и что окно Chrome НЕ открывается в процессе обычного обновления (ни видимого, ни в диспетчере задач как новый процесс chrome.exe от этого запуска).
    4. Открыть `D:/00_Projects/codex_balance_widget/widget_launch.log`, найти свежую строку `Balance updated (source: json)` после первого успешного обновления.
    5. Закрыть виджет. Временно сломать JSON-путь — например, установить переменную окружения `CODEX_HOME` на пустую временную папку перед запуском (`$env:CODEX_HOME="<пустая папка>"` в PowerShell) — и снова запустить `py -3 codex_balance_widget_chrome.py`.
    6. Убедиться, что виджет НЕ падает, и что происходит переход на существующий Chrome-путь: либо открывается Chrome для входа/фонового обновления (как раньше, до этой фазы), либо статус-строка показывает существующее сообщение про Chrome. Если Chrome в итоге успешно распознал данные — статусная строка должна содержать пометку "Chrome-фолбэк"/"Chrome fallback", а `widget_launch.log` — строку `Balance updated (source: chrome)`.
    7. Убрать переменную `CODEX_HOME` (`$env:CODEX_HOME=$null` в PowerShell) и подтвердить, что обычный запуск снова работает через JSON без Chrome.
  </how-to-verify>
  <verify>
    <human-check>Пользователь прошёл шаги 1-7 из how-to-verify и ответил "approved"</human-check>
  </verify>
  <done>Получен ответ "approved": обычный цикл работает через JSON без Chrome, лог содержит "source: json", форсированный сбой JSON вызывает Chrome-фолбэк без падения приложения, статус и лог фолбэка соответствуют ожиданиям.</done>
  <resume-signal>Напишите "approved", если все 7 пунктов подтвердились, либо опишите, что пошло не так.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CodexBalanceWidget.fetch_once → JsonUsageProvider.fetch → chatgpt.com/backend-api | тот же контур, что и в Plan 02-01, теперь достижимый из живого виджета при каждом цикле обновления |
| CodexBalanceWidget.fetch_once → write_log(outcome.log_line) → widget_launch.log | лог теперь ежецикл содержит маркер источника — файл на диске, не защищён от локального чтения (как и раньше, тот же уровень доверия что и остальной widget_launch.log) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-06 | Denial of Service | fetch_once — JSON-провал на каждом цикле держит виджет без данных | mitigate | `JsonUsageProvider` уже гарантирует максимум 1 ретрай (Plan 02-01) и всегда возвращает `JsonFetchResult` (никогда не бросает исключение наружу) — `fetch_once` не может зависнуть или упасть на JSON-ветке |
| T-2-07 | Repudiation | Отсутствие видимого различия JSON/Chrome для пользователя при обычной работе | accept | по D-05 постоянный UI-бейдж не запрошен — только лог и статус-пометка при активном фолбэке; осознанный компромисс из 02-CONTEXT.md |
| T-2-SC | Tampering | npm/pip/cargo installs | accept | новых зависимостей не добавлено — `json_usage_provider` уже stdlib-only (Plan 02-01) |
</threat_model>

<verification>
- `py -3 -m unittest test_probe_wham_usage test_json_usage_provider test_codex_balance_widget_chrome -v` — exit 0, полный регресс-набор фазы 1+2
- Живой запуск (Task 2, human-verify): обычный цикл без Chrome + лог `source: json`; форсированный сбой JSON → автоматический Chrome-фолбэк без падения + лог `source: chrome` или явное сообщение об обоюдном провале
</verification>

<success_criteria>
- Success Criterion 1 (Roadmap Phase 2): обычный цикл обновления не запускает Chrome — подтверждено кодом (`self.json_provider.fetch()` раньше `self.browser.fetch()`, вызывается только при провале JSON) и живой проверкой
- Success Criterion 2: при ошибке JSON-пути виджет падает обратно на текущий Chrome-механизм без изменения `CodexUsageBrowser` — подтверждено кодом и живой проверкой
- Success Criterion 3: лог фиксирует источник (`json`|`chrome`) каждого успешного обновления — подтверждено `write_log(outcome.log_line)` и живой проверкой
</success_criteria>

<output>
Create `.planning/phases/02-json-provider-integration/02-03-SUMMARY.md` when done
</output>
