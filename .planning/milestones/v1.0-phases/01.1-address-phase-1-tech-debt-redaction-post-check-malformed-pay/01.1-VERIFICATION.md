---
phase: 01.1-address-phase-1-tech-debt-redaction-post-check-malformed-pay
verified: 2026-07-21T00:00:00Z
status: passed
score: 4/4 must-haves verified
overrides_applied: 0
---

# Phase 01.1: Address Phase 1 Tech-Debt (Redaction Post-Check, Malformed Payload, Broad Except) Verification Report

**Phase Goal:** Close CR-01 (stdout redaction lacks the redaction_clean() post-check that write_fixture() has), CR-02 (collect_windows() crashes on non-dict rate_limit/additional_rate_limits entries), and WR-01 (main() only catches ProbeError) in probe_wham_usage.py, exactly as identified in 01-REVIEW.md / 01-VERIFICATION.md.

**Verified:** 2026-07-21
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | collect_windows() no longer raises AttributeError on non-dict rate_limit / additional_rate_limits entries (CR-02) | VERIFIED | `probe_wham_usage.py:138-154` — `rate_limit` coerced to `{}` unless `isinstance(rate_limit, dict)` (line 139-140); `additional_rate_limits` loop guards `if not isinstance(extra, dict): continue` (line 146-147) then coerces `inner` unless `isinstance(inner, dict)` (line 149-150). Matches existing None/missing-key graceful-degradation style. |
| 2 | main()'s stdout JSON print is verified by redaction_clean() before printing (CR-01) | VERIFIED | `probe_wham_usage.py:386-392` — `text = json.dumps(redacted_payload, ...)`; `if not redaction_clean(text): raise ProbeError(...)`; `print(text)` only reached after the check passes. Mirrors `write_fixture()`'s identical pattern at lines 351-357. |
| 3 | main() converts ANY exception (not just ProbeError) into the single clean-diagnostic contract (WR-01) | VERIFIED | `probe_wham_usage.py:417-432` — `except ProbeError as exc:` (417-421) followed by `except Exception as exc:` (422-432), both `print()` once, both respect `args.debug -> traceback.print_exc()`, both `return 1`. Exception clause positioned strictly after ProbeError clause (does not swallow it). |
| 4 | Full unit test suite passes, no network access | VERIFIED | `py -3 -m unittest test_probe_wham_usage test_json_usage_provider test_codex_balance_widget_chrome` → `Ran 64 tests ... OK`. No live HTTP calls (urllib.request.urlopen is mocked throughout). |

**Score:** 4/4 truths verified

### WR-01a Follow-up Fix (post-review hardening)

A code-review pass (01.1-REVIEW.md) found that the new WR-01 `except Exception` handler printed `str(exc)` with no redaction guard, unlike every other output path in the module. This was judged in-scope (fallout from this phase's own new code) and fixed in commit `35ccbc7`.

**Verified:** `probe_wham_usage.py:422-429` — `message = f"Непредвиденная ошибка: {exc}"`; `if not redaction_clean(message): message = "Непредвиденная ошибка (сообщение скрыто по соображениям безопасности)."`; `print(message)`. This closes the gap: any future exception whose `str()` embeds a secret-shaped substring (`eyJ`/`@`) is now caught by the same post-check used everywhere else in the file, rather than printed raw. Confirmed present in current file, not merely claimed in REVIEW-FIX.md.

### Out-of-Scope Judgment Verification (WR-02/03/04/05 + 3 Info items)

Confirmed via `git diff 713756f^..35ccbc7 -- probe_wham_usage.py` (the full range from before Task 1's first commit through the WR-01a fix commit): the only functions touched by this phase's diff are `collect_windows()` (CR-02) and `main()` (CR-01 redaction-clean guard, WR-01 except-Exception clause, WR-01a redaction guard on that clause). The diff shows no changes to:

- `load_tokens()` (WR-02's `UnicodeDecodeError` gap) — untouched, predates this phase (introduced in commit `5681f8c`/`6bb3ca7`, Phase 1).
- `_remaining_percent()` (WR-03's truncation-order bug) — untouched, predates this phase.
- `_credits_text()` (WR-04's missing `isinstance` guard on `unlimited`) — untouched, predates this phase.
- `redact()` (WR-05's case-sensitivity gap in `REDACT_KEYS` matching) — untouched, predates this phase.
- `test_extract_fields_live_fixture` (IN-01), `main()`'s fixture-writing branch test coverage (IN-02), `auth_json_path()` test coverage (IN-03) — all pre-existing test-file gaps not created or modified by this phase's diff.

**Judgment confirmed sound:** all 4 deferred warnings and 3 info items concern code paths this phase's diff never touches. Deferring them to backlog (as documented in 01.1-REVIEW-FIX.md) is the correct scope discipline — expanding into them would have violated the phase's locked CONTEXT.md scope (CR-01/CR-02/WR-01 only, plus fallout from the phase's own new code).

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `probe_wham_usage.py` | collect_windows() dict-validated, main() redaction_clean stdout guard + broadened except Exception | VERIFIED | `isinstance(rate_limit, dict)` present (line 139), `isinstance(extra, dict)` present (line 146), `redaction_clean(text)` present at 2 call sites (write_fixture line 353, main line 388), `except Exception as exc` present after `except ProbeError as exc` (line 422). |
| `test_probe_wham_usage.py` | regression tests for CR-01/CR-02/WR-01 | VERIFIED | `test_collect_windows_non_dict_rate_limit` (line 93), `test_collect_windows_non_dict_additional_rate_limit_entry` (line 99), `TestMainErrorHandling` class (line 383) with `test_main_stdout_redaction_post_check_blocks_leak` (line 386) and `test_main_catches_non_probeerror_exception` (line 406). |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| test_probe_wham_usage.py | probe_wham_usage.py | direct import + unittest.mock.patch on load_tokens/fetch_usage | WIRED | New tests patch `probe_wham_usage.load_tokens` / `probe_wham_usage.fetch_usage` and call `probe_wham_usage.main(["--no-fixture"])`, exercising the real fixed code paths (not re-implementations). Confirmed via grep of test class and full-suite pass. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| collect_windows tolerates non-dict rate_limit | `py -3 -m unittest test_probe_wham_usage.TestWindows.test_collect_windows_non_dict_rate_limit -v` (included in full run) | pass | PASS |
| collect_windows tolerates non-dict additional_rate_limits entry | `py -3 -m unittest test_probe_wham_usage.TestWindows.test_collect_windows_non_dict_additional_rate_limit_entry -v` (included in full run) | pass | PASS |
| Full project test suite (3 modules) | `py -3 -m unittest test_probe_wham_usage test_json_usage_provider test_codex_balance_widget_chrome` | `Ran 64 tests ... OK` | PASS |
| Scope isolation — no other Phase 1/2 files touched | `git diff 713756f^..35ccbc7 -- probe_wham_usage.py` reviewed in full | only collect_windows()/main() changed | PASS |

### Anti-Patterns Found

None. No TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER markers in the diffed regions. No stub returns, no empty handlers. The out-of-scope findings (WR-02/03/04/05) are pre-existing code, correctly documented as deferred rather than silently ignored.

### Requirements Coverage

No formal REQUIREMENTS.md in this project (roadmap tag `tech-debt-closure`, no REQ-IDs). Coverage assessed against the phase's own must_haves (all 4 verified above) and the code-review closure loop (CR-01, CR-02, WR-01 fixed; WR-01a follow-up fixed; WR-02/03/04/05 + 3 info items soundly deferred).

### Human Verification Required

None. All truths are verifiable via static code inspection, diff review, and automated test execution.

### Gaps Summary

No gaps. All 3 original findings (CR-01, CR-02, WR-01) are genuinely fixed in `probe_wham_usage.py`, the WR-01a follow-up fix from the code-review pass is correctly applied, the out-of-scope judgment on WR-02/03/04/05 and 3 info items is sound (confirmed via diff — those functions are untouched and predate this phase), and the full 64-test suite passes with no network access.

---

_Verified: 2026-07-21_
_Verifier: Claude (gsd-verifier)_
