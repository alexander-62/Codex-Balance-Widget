---
phase: 01.1-address-phase-1-tech-debt-redaction-post-check-malformed-pay
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [probe_wham_usage.py, test_probe_wham_usage.py]
autonomous: true
requirements: [tech-debt-closure]

must_haves:
  truths:
    - "collect_windows() no longer raises AttributeError when rate_limit or an additional_rate_limits[] entry is a truthy non-dict value (CR-02) — it treats the malformed entry as absent, exactly like the existing None/missing-key handling."
    - "main()'s stdout JSON print is verified by redaction_clean() before printing, mirroring write_fixture()'s existing guard — if the check fails, ProbeError is raised and nothing unredacted reaches stdout (CR-01)."
    - "main() converts ANY exception raised inside its try block (not just ProbeError) into the single clean-diagnostic contract — exit 1, one printed message, no raw traceback unless --debug (WR-01)."
    - "Full unit test suite (33 pre-existing + 4 new = 37 tests) passes with exit 0, no network access."
  artifacts:
    - path: "probe_wham_usage.py"
      provides: "collect_windows() dict-validated, main() redaction_clean stdout guard + broadened except Exception"
      contains: "isinstance(rate_limit, dict)"
    - path: "test_probe_wham_usage.py"
      provides: "regression tests for CR-01/CR-02/WR-01"
      contains: "test_main_catches_non_probeerror_exception"
  key_links:
    - from: "test_probe_wham_usage.py"
      to: "probe_wham_usage.py"
      via: "direct import + unittest.mock.patch on load_tokens/fetch_usage"
      pattern: "probe_wham_usage\\.(collect_windows|main)"
---

<objective>
Close exactly the 3 open code-review findings carried from Phase 1 into the v1.0 milestone audit — CR-01 (stdout redaction has no post-check), CR-02 (collect_windows crashes on non-dict rate_limit/additional_rate_limits entries), WR-01 (main() only catches ProbeError) — in `probe_wham_usage.py`, with regression tests in `test_probe_wham_usage.py`. No new features, no scope beyond these 3 items (per 01.1-CONTEXT.md).

Purpose: Harden the standalone probe script's defense-in-depth (redaction, malformed-payload tolerance, single-diagnostic-message contract) before Phase 2's `json_usage_provider.py` continues to build on top of it.
Output: Modified `probe_wham_usage.py` (collect_windows, main), extended `test_probe_wham_usage.py` (regression tests proving each fix), both existing + new tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.1-address-phase-1-tech-debt-redaction-post-check-malformed-pay/01.1-CONTEXT.md
@.planning/phases/01-json-endpoint-probe/01-REVIEW.md
@.planning/phases/01-json-endpoint-probe/01-VERIFICATION.md
@probe_wham_usage.py
@test_probe_wham_usage.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing regression tests for CR-01, CR-02, WR-01</name>
  <files>test_probe_wham_usage.py</files>
  <read_first>
    probe_wham_usage.py (whole file — especially collect_windows() lines 130-149, redact()/redaction_clean() lines 232-254, main() lines 354-410)
    test_probe_wham_usage.py (whole file — especially existing TestWindows class lines 34-90 and test_write_fixture_redaction_failure_no_file_written lines 228-237, which is the precedent pattern for asserting a redaction post-check blocks output)
    .planning/phases/01-json-endpoint-probe/01-REVIEW.md (CR-01 section lines 33-54, CR-02 section lines 56-91, WR-01 section lines 95-111 — exact original finding text and suggested fixes)
  </read_first>
  <action>
    Add `import contextlib` to the test file's import block (alongside existing `io`, `json`, `unittest.mock.patch`).

    In the existing `TestWindows` class, add two new test methods (this is the codebase's established "test-first" convention seen in Phase 1/2 commit history — these tests MUST fail against the current, unfixed `probe_wham_usage.py` before Task 2 runs):
    - `test_collect_windows_non_dict_rate_limit`: payload `{"rate_limit": "not-a-dict"}`; assert `probe_wham_usage.collect_windows(payload) == []`. Currently raises `AttributeError` (CR-02, original REVIEW line 129/138 in current file) because `rate_limit.get(slot)` is called on a string.
    - `test_collect_windows_non_dict_additional_rate_limit_entry`: payload `{"additional_rate_limits": ["not-a-dict", {"limit_name": "gpt-5-pro", "rate_limit": {"primary_window": {"limit_window_seconds": 18000, "used_percent": 1, "reset_at": 3}}}]}`; assert `len(collect_windows(payload)) == 1` and `windows[0]["limit_name"] == "gpt-5-pro"`. Currently raises `AttributeError` on `(extra or {}).get("rate_limit")` when `extra` is the string `"not-a-dict"` (CR-02, original REVIEW lines 134-139/143-144 in current file).

    Add a new `TestMainErrorHandling(unittest.TestCase)` class with two methods, each patching `probe_wham_usage.load_tokens` and `probe_wham_usage.fetch_usage` (via `@patch("probe_wham_usage.load_tokens")` / `@patch("probe_wham_usage.fetch_usage")`, mirroring the `@patch("probe_wham_usage.urllib.request.urlopen")` pattern already used in `TestFetchUsageErrorClassification`), and capturing stdout via `contextlib.redirect_stdout(io.StringIO())` around a call to `probe_wham_usage.main(["--no-fixture"])`:
    - `test_main_stdout_redaction_post_check_blocks_leak`: `mock_load_tokens.return_value = ("tok", None)`; `mock_fetch_usage.return_value = (200, {"weird_field": "eyJhbGciOiJIUzI1NiJ9"})` — a key outside `REDACT_KEYS` and not containing "token", so `redact()` alone lets it through unredacted (same setup shape as `test_write_fixture_redaction_failure_no_file_written`). Assert `main()` returns `1` and the captured stdout does NOT contain the substring `"eyJhbGciOiJIUzI1NiJ9"` (CR-01, original REVIEW lines 365-366 / current file lines 380-381).
    - `test_main_catches_non_probeerror_exception`: `mock_load_tokens.return_value = ("tok", None)`; `mock_fetch_usage.side_effect = KeyError("boom")` (any non-`ProbeError` exception). Assert `main()` returns `1` and captured stdout does NOT contain `"Traceback"` (WR-01, original REVIEW lines 391-395 / current file lines 406-410).

    Do not modify `probe_wham_usage.py` in this task — these tests are expected to fail (RED) against the current implementation.
  </action>
  <verify>
    <automated>py -3 -m unittest test_probe_wham_usage.TestWindows.test_collect_windows_non_dict_rate_limit test_probe_wham_usage.TestWindows.test_collect_windows_non_dict_additional_rate_limit_entry test_probe_wham_usage.TestMainErrorHandling -v</automated>
  </verify>
  <acceptance_criteria>
    - `py -3 -m unittest test_probe_wham_usage.TestWindows.test_collect_windows_non_dict_rate_limit -v` FAILS with `AttributeError` (proves the test exercises CR-02, not a tautology).
    - `py -3 -m unittest test_probe_wham_usage.TestWindows.test_collect_windows_non_dict_additional_rate_limit_entry -v` FAILS with `AttributeError`.
    - `py -3 -m unittest test_probe_wham_usage.TestMainErrorHandling.test_main_stdout_redaction_post_check_blocks_leak -v` FAILS (assertion that the leaked string is absent from stdout fails, proving the leak is real today).
    - `py -3 -m unittest test_probe_wham_usage.TestMainErrorHandling.test_main_catches_non_probeerror_exception -v` FAILS (either an uncaught `KeyError` propagates out of `main()`, or the `"Traceback"` assertion fails).
    - `py -3 -m unittest test_probe_wham_usage -v` at end of task: 37 tests collected (33 pre-existing + 4 new), 4 failing, 33 passing — no `ImportError`/`SyntaxError` in the test file itself.
  </acceptance_criteria>
  <done>4 new test methods exist in test_probe_wham_usage.py, each fails for the documented reason against the unfixed probe_wham_usage.py, confirming they correctly capture CR-01/CR-02/WR-01.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix CR-01, CR-02, WR-01 in probe_wham_usage.py</name>
  <files>probe_wham_usage.py</files>
  <read_first>
    probe_wham_usage.py (collect_windows() lines 130-149, redact()/redaction_clean() lines 232-254, main() lines 354-410 — exact current line numbers)
    test_probe_wham_usage.py (the 4 new tests added in Task 1 — read their exact assertions before implementing)
    .planning/phases/01-json-endpoint-probe/01-REVIEW.md (suggested fixes: CR-01 lines 45-54, CR-02 lines 81-91, WR-01 lines 99-111)
    json_usage_provider.py lines 68-78 (`_fetch_with_retry`'s `except ProbeError` / `except Exception` pair — the established Phase-2 precedent for broadening exception handling at a boundary; mirror this shape, not a different one)
  </read_first>
  <behavior>
    - collect_windows({"rate_limit": "not-a-dict"}) returns [] instead of raising AttributeError
    - collect_windows({"additional_rate_limits": ["not-a-dict", {valid entry}]}) skips the non-dict entry and returns only the valid window
    - main() with a payload containing an unredactable field (outside REDACT_KEYS, no "token" substring) returns 1 and never prints that field's raw value to stdout
    - main() with any non-ProbeError exception raised inside the try block (e.g. KeyError from fetch_usage) returns 1, prints one diagnostic line, no "Traceback" substring, and traceback.print_exc() still fires when --debug is passed
  </behavior>
  <action>
    CR-02 fix in `collect_windows()` (currently lines 130-149): replace `rate_limit = payload.get("rate_limit") or {}` with an isinstance-validated assignment — fetch `payload.get("rate_limit")` then coerce to `{}` unless `isinstance(rate_limit, dict)`. In the `additional_rate_limits` loop, replace `inner = (extra or {}).get("rate_limit") or {}` with an explicit `if not isinstance(extra, dict): continue` guard before accessing `extra.get("rate_limit")`, then coerce that result to `{}` unless it is a dict. This keeps `collect_windows()`'s existing graceful-degradation behavior (treat malformed as absent, matching its current None/missing-key handling) rather than raising — consistent with the function's own docstring ("Defensive against nulls/missing keys at every level") and with the REVIEW's suggested fix shape.

    CR-01 fix in `main()` (currently lines 380-381): after `redacted_payload = redact(payload)`, build the JSON text once (`json.dumps(redacted_payload, ensure_ascii=False, indent=2)`), call `redaction_clean(text)` on it, and if it returns `False` raise `ProbeError("Редакция вывода не прошла пост-проверку (eyJ/@) — вывод скрыт.")` before any `print()` — do not print the unverified text. Only `print(text)` after the check passes. This mirrors `write_fixture()`'s existing `redaction_clean()` call site exactly (same helper, same fail-closed behavior, no divergent second redaction scheme).

    WR-01 fix in `main()`'s except block (currently lines 406-410, only `except ProbeError as exc`): add a second `except Exception as exc:` clause after the existing `except ProbeError` clause, printing a Russian diagnostic (`f"Непредвиденная ошибка: {exc}"`), still respecting `args.debug` -> `traceback.print_exc()`, and `return 1` — same shape as `json_usage_provider.py`'s `_fetch_with_retry` `except ProbeError` / `except Exception` pair. Do not let this new clause swallow `ProbeError` (it must stay below the existing `except ProbeError` clause, never above it).

    Do not touch any other function, do not modify `codex_balance_widget_chrome.py`, `json_usage_provider.py`, or any Phase 2 file.
  </action>
  <verify>
    <automated>py -3 -m unittest test_probe_wham_usage -v</automated>
  </verify>
  <acceptance_criteria>
    - `py -3 -m unittest test_probe_wham_usage -v` -> `Ran 37 tests` ... `OK` (exit 0) — all 33 pre-existing tests plus all 4 new Task-1 tests pass, zero regressions.
    - `grep -n "isinstance(rate_limit, dict)" probe_wham_usage.py` matches (CR-02 fix present).
    - `grep -n "isinstance(extra, dict)" probe_wham_usage.py` matches (CR-02 additional_rate_limits guard present).
    - `grep -n "redaction_clean(text)" probe_wham_usage.py` matches inside `main()`, i.e. a second call site to `redaction_clean` beyond the one already in `write_fixture()` — `grep -c "redaction_clean(" probe_wham_usage.py` returns at least 2 (was 1 before this task).
    - `grep -n "except Exception as exc" probe_wham_usage.py` matches inside `main()`, positioned after `except ProbeError as exc` in source order.
    - `grep -n "print" probe_wham_usage.py | grep -i token` returns empty (token-isolation invariant from Phase 1 still holds — no new print of the token was introduced).
  </acceptance_criteria>
  <done>collect_windows() tolerates non-dict rate_limit/additional_rate_limits entries without raising; main()'s stdout print is gated behind a redaction_clean() post-check identical in spirit to write_fixture()'s; main() catches any Exception (not just ProbeError) and converts it to the single clean-diagnostic contract. Full 37-test suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `chatgpt.com/backend-api/wham/usage` response -> `probe_wham_usage.py` process | The JSON payload returned by the remote endpoint is untrusted/service-controlled input; this phase only hardens how the local process handles unexpected shapes of that input. No new network-facing surface is introduced (no new listeners, no new outbound calls). |
| `probe_wham_usage.py` process -> stdout (terminal/logs) | Output crossing from process memory to a potentially-logged/shared surface; the existing redaction contract (deny-list + post-check) is this boundary's control. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-01 | Tampering | `collect_windows()` (payload parsing) | mitigate | Validate `rate_limit` and each `additional_rate_limits[]` entry with `isinstance(..., dict)` before attribute/key access; malformed/unexpected shapes are treated as absent instead of crashing the process (CR-02). |
| T-01.1-02 | Information Disclosure | `main()` stdout print path | mitigate | Apply the existing `redaction_clean()` post-check (already used by `write_fixture()`) to the stdout JSON text before printing; fail closed (raise `ProbeError`, print nothing) if an unredacted secret-shaped substring (`eyJ`/`@`) is detected (CR-01). |
| T-01.1-03 | Denial of Service (local, low severity) | `main()` exception handling | mitigate | Broaden `main()`'s `except` clause to also catch bare `Exception`, converting any unanticipated error into the single clean-diagnostic contract instead of a raw traceback reaching the end user by default (WR-01). `--debug` still surfaces the full traceback for the developer. |

No package-manager installs are introduced by this phase (no new imports beyond stdlib already in use), so the Package Legitimacy Gate does not apply.
</threat_model>

<verification>
- `py -3 -m unittest test_probe_wham_usage -v` -> 37 tests, OK, exit 0.
- `git status --porcelain codex_balance_widget_chrome.py codex_balance_widget_launcher.pyw install.bat run.bat run_hidden.vbs requirements.txt json_usage_provider.py` -> empty (Phase 1/2 files remain untouched).
- `grep -c "redaction_clean(" probe_wham_usage.py` -> 2 (write_fixture + main).
- `grep -n "except Exception as exc" probe_wham_usage.py` -> present inside main(), after `except ProbeError as exc`.
</verification>

<success_criteria>
- CR-01, CR-02, WR-01 (and no other findings) are closed in `probe_wham_usage.py`.
- All 33 pre-existing tests plus 4 new regression tests (37 total) pass with no network access.
- `codex_balance_widget_chrome.py`, launchers, and `json_usage_provider.py` remain byte-for-byte unmodified.
- No new features, no behavior change beyond the 3 named findings.
</success_criteria>

<output>
Create `.planning/phases/01.1-address-phase-1-tech-debt-redaction-post-check-malformed-pay/01.1-01-SUMMARY.md` when done
</output>
